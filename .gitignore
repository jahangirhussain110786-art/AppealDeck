# Dependencies
node_modules/
.pnp/
.pnp.js

# Build outputs
dist/
.next/
out/
build/
*.tsbuildinfo

# Environment / secrets — never commit (this exact gap caused the prior leak)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
credentials.json
service-account*.json
*service*key*.json

# Supabase local dumps
*.dump
supabase/.temp/

# Logs
npm-debug.log*
*.log

# Editor / OS
.vscode/
.idea/
.DS_Store

# Test / coverage
coverage/

# Playwright MCP browser snapshots (debug artifacts)
.playwright-mcp/

# Zips are a credential-leak vector — never commit
*.zip

# MCP runtime artifacts
.drive-mcp.pid
.drive-mcp.log
.*-mcp.pid
.*-mcp.log
.mcp-bridge-*.pid
.mcp-bridge-*.log

# Kilo auto-generated lock file (regenerated from .agents/skills/)
skills-lock.json

# Playwright test outputs
/playwright-report/
/test-results/

# Stray workspace artifacts
.mcp-bridge-*.cmd
appealdeck.md

