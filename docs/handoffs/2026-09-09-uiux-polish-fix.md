# 2026-09-09 — UI/UX polish FIX pass (AA-30) — evidence log

Prompt: `docs/handoffs/2026-09-09-uiux-polish-fix-prompt.md`. Baseline commit `a10335d` (last commit of the polish pass). Audit that produced this pass: `docs/handoffs/2026-09-09-uiux-polish-audit.md`. Original specification: `docs/handoffs/2026-09-09-uiux-polish-prompt.md`.

## Resume pointer

- Task: F8 · Status: **done** (10 Sep 2026). Pass complete: F0 `cc1fe95` · F1 `34557af` · F2 `cca6f38` · F3 `16044b3` · F4 `5ed270a` · F5 `d2e6bf6` · F6 `aa9cd81` · F7 `12b6a89` (code final) · chores `60923f8`, `c477255` · F8 = the docs commit carrying this log.
- Last green gate: the full §5 block at `c477255` (code unchanged since `12b6a89`): typecheck · lint 0 warnings · lint:copy PASS (five passes) · format:check exit 0 · vitest 309/309 in 31 files · build 30 routes / 10 static / middleware 27.1 kB · Playwright 45 passed, 0 failed · Lighthouse 1.0 in every category on 7 pages; `/decode` accessibility **0.96** (two pre-existing audits — see the F8 rows; NOT green, recorded).
- Files open for this sub-step: none.
- Next command: none for this pass. Next pass = visual refresh v3, `docs/handoffs/2026-09-09-visual-refresh-prompt.md` Task V0 (`SESSION-START-PROMPT.md` already repointed; its start condition is this pass's `docs(uiux-polish-fix/task-8)` commit on top).
- Context usage at last update: ~30 %

## Founder sign-off (surfaced, not performed)

1. Look at the 12 public captures in `docs/handoffs/screenshots/2026-09-10/` (`/` and `/terms#independence`, 375/768/1280, light + dark).
2. The auth-gated proof needs one session with dev credentials or the founder's own browser: `/case` (saved-to-vault line, failure Alert), `/compose` (as-pasted preview open, print preview via Ctrl+P), the vault create form (loss warning before typing), `/vault` with `idleMs={20_000}` (warning at ~10 s, lock + toast at ~20 s; no warning while scrolling). With `DEV_LOGIN_EMAIL` / `DEV_LOGIN_PASSWORD` set, `MSYS_NO_PATHCONV=1 SCREENSHOT_OUT=docs/handoffs/screenshots/2026-09-10 npx playwright test --project=screenshots` captures the five authed routes at 3 × 2.
3. Decisions: the rendered Terms "Last updated" (`src/content/legal.ts`, 2026-08-25) vs `legal/terms.md` (9 Sep); a gloss for "Seller Central" (none approved; one `app.ts` line if wanted); `.gitattributes` `eol=lf` (the CRLF flake will recur on every checkout while `core.autocrlf=true`); `disconnected-chat!.txt`; pushing `master` (`origin/master` is at `048d311`, so CI has not run on any polish or fix commit — the Windows `lint-copy` finding means local and CI results only agree from `aa9cd81` on).
4. The two `/decode` Lighthouse audits ride the visual pass (shell → skip-link target; `EmptyState` heading level → suggest adding to Task V4).

## Baselines at `a10335d` (reviewing AI, 9 Sep 2026)

| Gate         | Command                                               | Output line                                                                                                                                                                                                                            | Exit  |
| ------------ | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| typecheck    | `npm run typecheck 2>&1 \| tail -6`                   | (no errors)                                                                                                                                                                                                                            | 0     |
| lint         | `npm run lint 2>&1 \| tail -10`                       | `✔ No ESLint warnings or errors`                                                                                                                                                                                                       | 0     |
| lint:copy    | `npm run lint:copy 2>&1 \| tail -4`                   | `lint-copy: banned soft list` · `banned numbers` · `colour gate` · `lint-copy: PASS` (3 passes)                                                                                                                                        | 0     |
| format:check | `npm run format:check 2>&1 \| tail -6`                | `[warn] src/app/(app)/billing/page.tsx` · `Code style issues found`                                                                                                                                                                    | **1** |
| vitest       | `npm test 2>&1 \| tail -8`                            | `Test Files 31 passed (31)` · `Tests 308 passed (308)`                                                                                                                                                                                 | 0     |
| build        | `npm run build 2>&1 \| tail -50` + manifests          | `ƒ Middleware 27.1 kB` · `app routes total: 30` · `static prerendered routes: 10`                                                                                                                                                      | 0     |
| playwright   | `npx playwright test --reporter=dot 2>&1 \| tail -30` | `77 passed (4.9m)` · `4 failed` (all `Test timeout of 30000ms exceeded`, blank page snapshot) · `30 skipped`; targeted re-run of the four with `--retries=1` → `3 passed`, `1 flaky` — cold-compile flakes, not regressions (audit §4) | 0     |

## Evidence log

| Task | Claim                                                        | Command                                                                                                                                                                                                                        | Output line                                                                 | Commit                                                                                         |
| ---- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| F0   | no `<commit>` placeholders remain in old log                 | `Select-String -Path docs/handoffs/2026-09-09-uiux-polish.md -Pattern "<commit>"                                                                                                                                               | Measure-Object                                                              | Select-Object -ExpandProperty Count`                                                           | `0`     | cc1fe95 |
| F0   | no `46bf6dd` in Task 3 rows of old log                       | `Select-String -Path docs/handoffs/2026-09-09-uiux-polish.md -Pattern "46bf6dd"                                                                                                                                                | Measure-Object                                                              | Select-Object -ExpandProperty Count`                                                           | `0`     | cc1fe95 |
| F0   | old log Task 3 rows use `9955e73`, Task 5 rows use `461c3b0` | `git log --oneline --format="%h %s" 461c3b0 9955e73 9acdc9a`                                                                                                                                                                   | `461c3b0 fix(uiux-polish/task-5)...` · `9955e73 fix(uiux-polish/task-3)...` | cc1fe95                                                                                        |
| F0   | resume pointer updated to Task 11 superseded                 | `Select-String -Path docs/handoffs/2026-09-09-uiux-polish.md -Pattern "superseded 9 Sep"                                                                                                                                       | Select-Object -ExpandProperty Line`                                         | `- Task: 11 · Status: superseded 9 Sep 2026 — see docs/handoffs/2026-09-09-uiux-polish-fix.md` | cc1fe95 |
| F0   | deviation appended to old log                                | `Select-String -Path docs/handoffs/2026-09-09-uiux-polish.md -Pattern "Tasks 6–11: see the 9 Sep audit"                                                                                                                        | `Tasks 6–11: see the 9 Sep audit; repaired by the fix pass.`                | cc1fe95                                                                                        |
| F0   | all committed files UTF-8 without BOM                        | `Get-Content -Encoding Byte -TotalCount 2 <each file>                                                                                                                                                                          | ForEach-Object { "{0:x2} {1:x2}" -f $_[0], $_[1] }`                         | `23 20` (hash space) for every file — no `ff fe`/`ef bb`                                       | cc1fe95 |
| F0   | staged files: index+worktree LF                              | `git ls-files --eol <6 files>`                                                                                                                                                                                                 | `i/lf w/lf attr/` × 6                                                       | cc1fe95                                                                                        |
| F0   | no secrets in staged diff                                    | `git diff --cached                                                                                                                                                                                                             | Select-String -Pattern "password                                            | passphrase                                                                                     | secret  | token   | license_key | Dev-"` | matches are documentation text only (CLAUDE.md, fix-prompt) — no credentials | cc1fe95 |
| F0   | staged file set matches §1                                   | `git diff --cached --stat`                                                                                                                                                                                                     | `6 files changed, 416 insertions(+), 17 deletions(-)`                       | cc1fe95                                                                                        |
| F1   | one input ref each, one camera input/button (coarse only)    | `grep -c "cameraInputRef" src/components/FileDropZone.tsx` · `grep -c 'ref={inputRef}' FileDropZone.tsx` · `grep -c 'style={{' FileDropZone.tsx` · `grep -c "pointer:coarse" FileDropZone.tsx`                                 | 3 · 1 · 0 · 1                                                               | 34557af                                                                                        |
| F1   | addFileToVault: opts signature, no re-list, no raw slot      | `grep -n "records.find\|encryptedBadge\|slot?:" src/lib/vault/addFileToVault.ts` · `grep -rn "vaultSlot" addFileToVault.ts src/content/app.ts` · `grep -rn "vault.add(" src/components`                                        | 0 · 2 · 0                                                                   | 34557af                                                                                        |
| F1   | findByPlaintext oldest — new test                            | `grep -n "orderBy.*createdAt" src/core/vault/vault.ts` · `npx vitest run 2>&1 \| tail -3` · `npx tsc --noEmit`                                                                                                                 | `orderBy("createdAt")` (no `.reverse()`) · `Tests 309 passed` · exit 0      | 34557af                                                                                        |
| F2   | idle timers stored in refs, stable listeners, no stale phase | `grep -c "clearTimeout" src/components/VaultGate.tsx` · `grep -n "warnTimer\|phaseRef\|onLockedRef" VaultGate.tsx` · `grep -n "AlertTitle" VaultGate.tsx` · `npx tsc --noEmit` · `npx eslint VaultGate.tsx` · `npx vitest run` | 2 · 12 · 2 · exit 0 · 0 warnings · 309 passed                               | cca6f38                                                                                        |
| F2   | VaultView.tsx unchanged (temp props reverted)                | `git diff --name-only`                                                                                                                                                                                                         | `src/components/VaultGate.tsx` only                                         | cca6f38                                                                                        |
| F2   | manual check deferred — no interactive browser available     | (Vault unlock requires login + passphrase entry; cannot be automated in CLI)                                                                                                                                                   | N/A — code matches spec shape verbatim; `clearTimers()` in effect cleanup   | cca6f38                                                                                        |
| F3   | one `@media print` block; no link-URL rule, no class-substring or escaped-dot selectors | `grep -c "@media print" src/app/globals.css` · `grep -n 'a\[href\]:after\|sticky\\\\\|class\*=' src/app/globals.css` | `1` · 0 hits | 16044b3 |
| F3   | `data-no-print` on AppHeader root, interview fixed bar, dashboard reply `Card` | `grep -rn "data-no-print" src/components src/app --include=*.tsx` | `AppHeader.tsx:64` · `DashboardClient.tsx:403` · `InterviewFlow.tsx:596` (3 hits) | 16044b3 |
| F3   | compose prints the plain POA text under a dated header | `grep -n "print-only" src/components/ComposeView.tsx` · `grep -n "print:" src/content/app.ts` | `243: <div className="print-only hidden whitespace-pre-wrap font-serif text-sm">` · `208: print: {` (1 hit) | 16044b3 |
| F3   | gates | `npx tsc --noEmit` · `npm run lint` · `npm run lint:copy` · `npx vitest run` · `npm run format:check` | exit 0 · `✔ No ESLint warnings or errors` · `lint-copy: PASS` · `Tests 309 passed (309)` · **exit 1** (`billing/page.tsx` CRLF — Task F7) | 16044b3 |
| F3   | staged files UTF-8 without BOM, LF in index and working copy; no secrets | `head -c 3 <file> \| od -An -tx1` × 6 · `git ls-files --eol <6 files>` · `git diff --cached \| grep -inE "password\|passphrase\|secret\|token\|license_key\|Dev-"` | no `ef bb bf` / `ff fe` · `i/lf w/lf` × 6 · no matches | 16044b3 |
| F4   | spell-check on the three seller-authored textareas, off on the two pasted-Amazon-text textareas and the three email inputs | `grep -rn "spellCheck" src --include=*.tsx \| grep -v VaultGate` | 8 hits: `InterviewFlow.tsx:706` (short_text, on) · `InterviewFlow.tsx:873` (decline, on) · `PoaSection.tsx:83` (on) · `DecodeClient.tsx:146` (`{false}`) · `DashboardClient.tsx:416` (`{false}`) · `login/page.tsx:158`, `signup/page.tsx:152`, `forgot-password/page.tsx:81` (`{false}`, step 2) — see Deviations | 5ed270a |
| F4   | wrong attributes removed from the answer textarea | `grep -rn 'autoCorrect="false"\|autoComplete="off"' src/components/InterviewFlow.tsx` | 0 hits | 5ed270a |
| F4   | phone keyboards on email and number fields | `grep -rn 'inputMode="email"' "src/app/(app)"` · `grep -n 'inputMode="numeric"' src/components/InterviewFlow.tsx` | 3 hits (`forgot-password/page.tsx:82`, `login/page.tsx:159`, `signup/page.tsx:153`) · `727: inputMode="numeric"` (1 hit) | 5ed270a |
| F4   | viewport `interactiveWidget` (Next 14.2.35 `Viewport` type accepts it — no cast) | `grep -n "interactiveWidget" src/app/layout.tsx` · `npx tsc --noEmit` | `45: interactiveWidget: "resizes-content",` (1 hit) · exit 0 | 5ed270a |
| F4   | `lg` primary in the interview step bar | `grep -c 'size="lg"' src/components/InterviewFlow.tsx` | `1` | 5ed270a |
| F4   | gates | `npx tsc --noEmit` · `npm run lint` · `npm run lint:copy` · `npx vitest run` · `npm run format:check` | exit 0 · `✔ No ESLint warnings or errors` · `lint-copy: PASS` · `Tests 309 passed (309)` · **exit 1** (`billing/page.tsx` CRLF — Task F7) | 5ed270a |
| F4   | staged files LF, UTF-8 without BOM; secrets grep | `git ls-files --eol <8 files>` · `head -c 3 … \| od` · `git diff --cached \| grep -inE "password\|…"` | `i/lf w/lf` × 8 · no BOM · matches are the path `forgot-password/page.tsx` only | 5ed270a |
| F5   | inline Alert mounted twice; root layout no longer mounts it | `grep -rn "<OfflineNotice" src --include=*.tsx` · `grep -c "OfflineNotice" src/app/layout.tsx` | `DecodeClient.tsx:119: <OfflineNotice />` · `AppShell.tsx:19: <OfflineNotice className="mb-6" />` (2 hits) · `0` | d2e6bf6 |
| F5   | no toast; reads `navigator.onLine` in an effect, state starts online | `grep -c "toast" src/components/OfflineNotice.tsx` · `grep -n "navigator.onLine" src/components/OfflineNotice.tsx` | `0` · `16: setOnline(navigator.onLine);` (+ the doc comment at 10) | d2e6bf6 |
| F5   | approved copy only | `grep -c "You're offline\|will sync" src/content/shared.ts` · `grep -n '"You are offline"' src/content/shared.ts` | `0` · `38: title: "You are offline",` (1 hit); `desc` verbatim from the prompt | d2e6bf6 |
| F5   | gates | `npx tsc --noEmit` · `npm run lint` · `npm run lint:copy` · `npx vitest run` · `npm run format:check` | exit 0 · `✔ No ESLint warnings or errors` · `lint-copy: PASS` · `Tests 309 passed (309)` · **exit 1** (`billing/page.tsx` CRLF — Task F7) | d2e6bf6 |
| F5   | staged files LF, no BOM, no secrets | `git ls-files --eol <5 files>` · `head -c 3 … \| od` · secrets grep | `i/lf w/lf` × 5 · no BOM · no matches | d2e6bf6 |
| F6   | exclamation-mark rule + fifth pass | `grep -n "BANNED_PUNCTUATION" scripts/lint-copy.mjs` · `npm run lint:copy 2>&1 \| tail -6` | `55: const BANNED_PUNCTUATION = […]` · `157: scanFile(f, BANNED_PUNCTUATION, …)` (2 hits) · `banned strings` · `banned soft list` · `banned numbers` · `banned punctuation (content only)` · `colour gate` · `lint-copy: PASS` (five pass lines — see Deviations) | aa9cd81 |
| F6   | rule proven on the old `sampleNotice.ts` line and on copy | `node -e` regex probe | `old sampleNotice line caught: true` · `copy "Done!" caught: true` · `!== not caught: false` · `new guard not caught: false` | aa9cd81 |
| F6   | "(optional)" marker rendered when `step.required === false` | `grep -n "optionalSuffix" src/components/InterviewFlow.tsx src/content/app.ts` | `InterviewFlow.tsx:635` · `app.ts:394` (2 hits); `required` reaches the client (`InterviewStep.required?: boolean`, `nextStep()` runs client-side; file steps carry `required: req?.required ?? true`) | aa9cd81 |
| F6   | first-use definitions (page → term → key) | `grep -n "Plan of Action (POA)" src/content/app.ts` | `/dashboard` → POA → `dashboard.actions.reviewPoa` (61) and `dashboard.submitCard.description` (103) · `/case` → POA → `case.howItWorks.bullet3` (408, pass holders) and `case.noPass.desc` (417) · `/compose` → POA → `compose.title` (123, h1 in both pass states) · `/decode` → no term occurs in `DECODE` (`marketing.ts`); ASIN/ODR/AHR/LOA do not occur in `app.ts` at all · Seller Central: no gloss supplied — unchanged (Discovered) | aa9cd81 |
| F6   | README matches the directory | `grep -c "errors.ts" src/content/README.md` · `ls src/content` | `0` · `README.md app.ts auth.ts claims-allowlist.txt legal.ts marketing.ts sampleNotice.ts shared.ts` — every listed module exists | aa9cd81 |
| F6   | gates | `npx tsc --noEmit` · `npm run lint` · `npm run lint:copy` · `npx vitest run` · `npx prettier --check scripts/lint-copy.mjs src/content/README.md` · `npm run format:check` | exit 0 · `✔ No ESLint warnings or errors` · `lint-copy: PASS` (exit 0) · `Tests 309 passed (309)` · `All matched files use Prettier code style!` · **exit 1** (`billing/page.tsx` CRLF — Task F7) | aa9cd81 |
| F7   | format gate green | `npx prettier --write "src/app/(app)/billing/page.tsx"` · `npm run format:check 2>&1 \| tail -3; echo exit=$?` | `src/app/(app)/billing/page.tsx 233ms` · `All matched files use Prettier code style!` · `exit=0` | 12b6a89 |
| F7   | billing page: working-copy CRLF only; committed blob already LF | `git ls-files --eol "src/app/(app)/billing/page.tsx"` before/after · `git diff -- <file>` · `grep -c $'\r' <file>` | before `i/lf w/crlf`, after `i/lf w/lf` · empty diff · `0` CRs — the ` M` in `git status` is the autocrlf stat only; `git add` staged nothing for it (absent from `--cached --stat`) | 12b6a89 |
| F7   | device and vault timestamps keep the time of day | `grep -c "formatDateTime" src/components/DeviceManager.tsx src/components/VaultView.tsx` · `grep -c "formatDate(" <same>` | `DeviceManager.tsx:3` · `VaultView.tsx:2` (import + uses) · `0` · `0` — both are client components whose timestamps arrive after mount (devices fetch, vault list), so the device time zone in `formatTime` cannot cause a hydration mismatch | 12b6a89 |
| F7   | Terms §2 first sentence restored (box A unticked → default); the two approved paragraphs and the 9 Sep date kept | `grep -n "suspension/notice text and helps you draft" legal/terms.md` · `git show bfba421:legal/terms.md \| sed -n '12p'` (source of the 31 Aug wording) · `sed -n '12,18p' legal/terms.md` | `14: AppealDeck is **software** that decodes Amazon suspension/notice text and helps you draft a Plan of Action. It is **not legal advice** …` (1 hit) · identical sentence · independence paragraph + "We never log in…" paragraph present | 12b6a89 |
| F7   | gates | `npx tsc --noEmit` · `npm run lint` · `npm run lint:copy` · `npm run format:check` · `npx vitest run` | exit 0 · `✔ No ESLint warnings or errors` · `lint-copy: PASS` · **exit 0** (first green format gate of the pass) · `Tests 309 passed (309)` | 12b6a89 |
| F7   | staged files LF, no BOM, no secrets | `git ls-files --eol <4 files>` · `head -c 3 … \| od` · secrets grep | `i/lf w/lf` × 4 · no BOM · no matches | 12b6a89 |
| F8   | build ≥ baseline | `npm run build 2>&1 \| tail -60` + `.next/app-path-routes-manifest.json` / `prerender-manifest.json` | `✓ Generating static pages (25/25)` · `ƒ Middleware 27.1 kB` · `app routes total: 30` · `static prerendered routes: 10` · exit 0 — identical to the `a10335d` baseline | 12b6a89 (code) |
| F8   | §5 grep: "guarantee" | `grep -rni "guarantee" src` | non-test hits: `src/core/composer.ts:182` (critic pattern) · `:183` (`code: "BANNED_GUARANTEE"`) · `src/content/app.ts:186` (the `BANNED_GUARANTEE` finding-fix **key**, whose copy is "Remove any promise about Amazon's decision.") · tests: `core.test.ts`, `index.test.ts`, `findingSections.test.ts`. No user-facing "guarantee". | 12b6a89 (code) |
| F8   | §5 grep: storage | `grep -rn "sessionStorage\|localStorage" src` | 0 hits | 12b6a89 (code) |
| F8   | §5 grep: decorative animation | `grep -rn "animate-spin\|animate-pulse" src` | 7 hits, every one a busy-state icon inside a `Button`: `DecodeClient.tsx:176` (submit), `AuthCard.tsx:120` (submit), `DashboardClient.tsx:423` (analyze), `DeviceManager.tsx:208` (revoke), `FieldSuggester.tsx:105` (suggest), `VaultGate.tsx:281/352` (create/unlock). `skeleton.tsx` uses no `animate-*` class. | 12b6a89 (code) |
| F8   | §5 grep: formatter / vault / forbidden sources | `grep -rn "Intl.DateTimeFormat" src --include=*.tsx \| wc -l` · `grep -rn "vault.add(" src/components \| wc -l` · `grep -rli superpower src e2e scripts \| wc -l` | `0` · `0` · `0` | 12b6a89 (code) |
| F8   | §1.1 simplicity statement | `git diff a10335d..HEAD -- src \| grep "^+.*<Card\b"` · `… -- src/components/AppHeader.tsx \| grep "^+.*href"` · `… -- src \| grep "^+.*<Button"` | the only added `<Card` line is the existing dashboard reply card gaining `data-no-print` · no nav additions · no `<Button` added (the one Button change is `size="lg"` on the existing step-bar primary). `OfflineNotice` renders `null` while online; the `.print-only` block is `hidden` on screen. **No new nav item, no new card above the fold, no second primary action** on `/`, `/decode`, `/pricing`, `/faq`, `/terms`, `/login`, `/signup` (rendered in the screenshot run) or `/dashboard`, `/case`, `/compose`, `/vault`, `/billing` (by diff). | 12b6a89 (code) |
| F8   | screenshots for founder sign-off (public routes) | `MSYS_NO_PATHCONV=1 SCREENSHOT_OUT=docs/handoffs/screenshots/2026-09-10 SCREENSHOT_ROUTES="/,/terms#independence" npx playwright test --project=screenshots --reporter=dot` (production server on :3000) | `12 passed` · `30 skipped` (auth routes: no `DEV_LOGIN_*`) · files in `docs/handoffs/screenshots/2026-09-10/`: `home-{375,768,1280}-{light,dark}.png`, `terms#independence-{375,768,1280}-{light,dark}.png` (12 files, 175–225 kB each). Home shows the footer independence line; Terms shows the "Independence from Amazon" section. `/case`, `/compose`, vault create form, `/vault` idle-lock warning: **NOT RUN** (see Deviations). | 60923f8 (spec) |
| F8   | Playwright, first run (production server) | `npx playwright test --project=chromium --reporter=dot` | `44 passed` · **`1 failed`** — `e2e/a11y.spec.ts:56 /forgot-password … color-contrast` (exit 1; the `\| tail` pipe hid the code — read from `PIPESTATUS`). Root cause and harness fix under Discovered. | 60923f8 |
| F8   | Playwright, targeted re-run before the fix (proves it is deterministic, not a flake) | `npx playwright test e2e/a11y.spec.ts -g "forgot-password" --project=chromium` | `1 failed` (same `#aaabaf` blended colour) · exit 1 | 60923f8 |
| F8   | Playwright, a11y spec after the settle wait | `npx playwright test e2e/a11y.spec.ts --project=chromium --reporter=dot` | `18 passed (17.3s)` · exit 0 | c477255 |
| F8   | quick gates at HEAD (`src/` unchanged since `12b6a89`; the two chore commits touch `e2e/` only) | `npx tsc --noEmit` · `npm run lint` · `npm run lint:copy` · `npm run format:check` · `npx vitest run` | exit 0 · `✔ No ESLint warnings or errors` (exit 0) · five `lint-copy:` pass lines + `lint-copy: PASS` (exit 0) · `All matched files use Prettier code style!` (exit 0) · `Test Files 31 passed (31)` · `Tests 309 passed (309)` (exit 0) — vitest count = baseline 308 + 1 (F1's `findByPlaintext` ordering test); the polish prompt's "+ 15" target counted tests the polish pass itself was to add and were added there (287 → 308) | 60923f8 |
| F8   | Playwright, final full run | `npx playwright test --project=chromium --reporter=dot` | `Running 45 tests using 4 workers` · `45 passed (24.4s)` · **exit 0** — 0 failed, ≥ baseline + 1 (the polish pass added the footer-independence test) | c477255 |
| F8   | Lighthouse (`lighthouserc.cjs`: a11y ≥ 1.0 and best-practices/SEO ≥ 0.95 are errors, perf ≥ 0.9 a warning; 8 URLs, desktop preset) | `npx lhci collect --config=lighthouserc.cjs` → crashes (Discovered) · `npx lighthouse http://127.0.0.1:3000/<page> --output=json --output-path=<scratch>/<page>.json --chrome-flags="--headless --no-sandbox --disable-gpu" --preset=desktop --quiet` × 8, scores read from the JSON with `node -e` | perf · a11y · best-practices · SEO = **1 · 1 · 1 · 1** on `/`, `/pricing`, `/login`, `/privacy`, `/terms`, `/refund`, `/faq` · `/decode` = 1 · **0.96** · 1 · 1 → **RED against the 1.0 a11y assertion**: `heading-order` (the `EmptyState` `<h3>` "No notice pasted yet" follows the page `<h1>` with no `<h2>`) and `skip-link` (the root layout's `<a href="#main">` has no target because `src/app/decode/page.tsx` renders `DecodeClient` without the shell's `<main id="main">`). Both pre-existing — `git show a10335d:src/app/decode/page.tsx` has no `main`; `EmptyState.tsx:27` is untouched by this pass — recorded, not fixed (§0.3 / §0.11). | 12b6a89 (code) |

## Discovered during this pass

- **F3 · session hand-over defect.** The previous session's uncommitted Task F3 work had replaced `import { loadCaseFile, loadCaseLog } from "@/lib/caseStore"` in `ComposeView.tsx` with the `formatDate` import instead of adding it (tsc: TS2552 `loadCaseFile`, TS2304 `loadCaseLog`, TS6133 `formatDate` unused). Both imports restored before the `.print-only` block was added; nothing else in that file changed.
- **F3 · no case-kind label in content.** `src/content/app.ts` has no kind label; the only kind→label map is the hardcoded `KIND_LABELS` record in `src/components/InterviewFlow.tsx` (~line 122). The print header therefore carries the date only (`APP.compose.print.header`), as the fix prompt instructed. Moving `KIND_LABELS` into `src/content/app.ts` is an AA-29 leftover, not this pass.
- **F8 · axe scanned mid-animation.** Against the production build `e2e/a11y.spec.ts` failed deterministically on `/forgot-password` with `color-contrast` 2.29:1 for the label, input and submit button, foreground `#aaabaf` on `#ffffff`. `#aaabaf` is not in the palette: it is `text-foreground` (`rgb(18, 22, 33)` when settled — measured in the browser) blended at about one-third opacity by the page's 350 ms framer-motion fade (`initial={{ opacity: 0 }}`), because the helper ran axe straight after `page.goto`. The failure screenshot, taken after the scan, shows full contrast. `/login` and `/signup` carry the identical fade and passed on timing alone (in `next dev` the slower hydration masked it). Not caused by this pass (the F4 diff to that page adds four input attributes). Harness fixed in `e2e/a11y.spec.ts`: `waitForEntryAnimations()` waits until every in-viewport element with an inline `opacity` reaches 1 before axe runs; below-the-fold elements are ignored (no `whileInView` exists in `src/` today, checked). Re-run: 18/18 a11y tests pass.
- **F8 · Lighthouse CI cannot finish on this Windows machine.** `npx lhci collect --config=lighthouserc.cjs` audits the first URL completely, then `chrome-launcher` throws `EPERM` deleting Chrome's temp profile (`%TEMP%\lighthouse.*`) on kill; lhci treats the run as failed, saves nothing, and `lhci assert` then reports "Checking assertions against 0 URL(s)" — a vacuous pass (exit 0) that must not be read as green. Direct `npx lighthouse <url> --output=json --output-path=…` writes the report **before** the crash, so the scores below come from one direct run per URL (`--preset=desktop`, same chrome flags), read from the JSON. The `npm run lighthouse` script (`lhci autorun`) also uploads to `temporary-public-storage`; not used.
- **F8 · Lighthouse a11y 0.96 on `/decode` — two pre-existing audits.** (a) `skip-link`: `/decode` renders `DecodeClient` bare, so the root layout's skip link has no `#main` target; the visual refresh's shell work (spec S§4.1 `<main id="main">`, prompt §1 P0 "`/decode` has no header/footer") fixes it. (b) `heading-order`: `src/components/EmptyState.tsx:27` always renders an `<h3>`; on `/decode` it follows the `<h1>` directly. The visual pass restyles `EmptyState` in Task V4 (S§6.5) but says nothing about the heading level — suggest a `headingLevel` prop (default `h2`) in V4, or a `<p>` where the title is not a section heading. Every other audited page scores 1.0 in all four categories.
- **F8 · rendered Terms date.** `/terms` renders "Last updated: 2026-08-25" from `src/content/legal.ts` while the independence section was added on 9 Sep and `legal/terms.md` says 9 Sep 2026 (draft). Founder decides whether the rendered date should move; not touched here (legal copy).
- **F8 · sticky header in the mobile `/terms` capture.** `terms#independence-375-*.png` shows the header band in the middle of the page: `page.goto("/terms#independence")` scrolls to the anchor and the full-page capture paints the sticky header at that scroll position. Capture artifact, not a layout defect.
- **F0 · stale hash in the F0 rows.** The previous session cited `18da41e` for Task F0. `git cat-file -t 18da41e` → `commit`, but it is on no branch: same subject and date as `cc1fe95`, different tree — the F0 commit before it was amended. Rows corrected to `cc1fe95` (the commit `git log` shows). §0.B.1 exists for exactly this.
- **F8 · Git Bash mangles a lone `/` argument.** `SCREENSHOT_ROUTES="/,/terms#independence"` reached Node as `C:/Program Files/Git,/terms#independence` (MSYS path conversion), so the six home captures were written as NTFS alternate data streams of a zero-byte file `C`. Deleted; re-run with `MSYS_NO_PATHCONV=1`. Use that prefix (or PowerShell) for any env value that starts with `/`.
- **F5 · Alert children wrapper.** `Alert` lays its children out as a flex row (`src/components/ui/alert.tsx`), so `AlertTitle` + `AlertDescription` are wrapped in one `<div>` exactly as `ComposeView.tsx` and the other Alerts do; otherwise title and description would sit side by side. `OfflineNotice` also accepts an optional `className` so `AppShell` can add `mb-6` above the breadcrumb (the `/decode` root already spaces children with `gap-8`).
- **F6 · lint-copy was inert on Windows.** `path.relative()` returns backslash paths on Windows, so the filters `/src\/content\//`, `/src\/components\//` and `SOFT_LIST_FILES` matched **no** files here: probe before the fix — soft-list 0, banned-numbers 0, colour 0 of 167 files; only "banned strings" (unfiltered) actually scanned. CI (`ubuntu-latest`) was unaffected, but `origin/master` is behind, so CI has not run on the last ~20 commits. Fixed inside Task F6 with `relPosix()` (separators normalised to `/`); after the fix the probe shows 6 content files (`app, auth, legal, marketing, sampleNotice, shared`), 105 soft-list files and 37 colour files scanned, and every pass still passes — local and CI now agree. This is also why the fix prompt counted "3 passes": `tail -4` cut the first line; there were four pass lines before this task and five after.
- **F6 · `sampleNotice.ts` non-null assertion.** `FIXTURES.find(…)!.raw` matches the prescribed rule (`)!.`), so the pass would have failed on day one. Rewritten as a guarded lookup that throws at module load if the fixture id is missing (build/prerender fails loudly instead of rendering an empty sample). No copy change.
- **F6 · Seller Central has no gloss.** The prompt lists six terms but supplies five expansions; "Seller Central" is a product name, not an acronym, and no approved wording exists (§0.12: no founder text invented). Left unchanged on `/dashboard` (`submitCard.title`) and `/compose` (`subtitle`); the founder may supply a gloss and it is a one-line `app.ts` change.
- **F6 · ASIN / ODR / AHR / LOA.** None occurs in `src/content/app.ts`; the only ASIN in content is inside `SAMPLE_POA` (`marketing.ts:201`, the pricing-page dialog, not one of the four surfaces). On `/decode` the do-now/do-not lists come from `src/core/guidance.ts` (core, out of scope) and mention "POA", "ASINs" and "Seller Central credentials"; `DecodeClient.tsx:341` already says "Plan of Action" in full. On `/dashboard` the numbered next-actions list (`nextBestActions(next)`, `DashboardClient.tsx:308`) renders core strings above the button.
- **F3 · what still prints on `/compose`.** With the spec's rule set only `header/footer/nav/button/textarea/[data-no-print]/[data-sonner-toaster]` are hidden, so the `PoaSection` card headings and critic notes, the mode banner, the checklist and the expectations card still print above/below the plain text block. The spec listed exactly three `data-no-print` targets; none was added elsewhere. Founder may decide whether the section cards should also be `data-no-print` (would leave header + plain text only).

## Deviations from the prompt

- **F4 · Accept count vs. Do.** The Accept line `grep -rn "spellCheck" src --include=*.tsx | grep -v VaultGate → 5 hits (3 true, 2 false)` counts only the textareas, but the same task's step 2 puts `spellCheck={false}` on the three email inputs (the original Task 8 carries the identical mismatch). The Do was implemented in full; the grep returns **8** (3 on, 5 off). Nothing was dropped to make the count fit.
- **F6 · pass count.** The Accept line expects "four `lint-copy:` pass lines before PASS". The script already printed four before this task (banned strings · soft list · numbers · colour — the prompt's "3 passes" came from `tail -4`), so with the punctuation pass it prints **five**. The added pass is the one the task asked for; nothing else was added or removed.
- **F8 · one unprescribed commit.** `60923f8 chore(e2e): screenshot spec reads SCREENSHOT_OUT and SCREENSHOT_ROUTES overrides (defaults unchanged)` — two env overrides in `e2e/screenshots.spec.ts` so the Task F8 captures could go to a dated folder and include `/terms#independence` without editing the hard-coded 7 Sep list. Defaults and the auth flow are unchanged; the visual-refresh pass can reuse it. Kept separate from the docs commit so no task commit mixes code and docs.
- **F8 · a second unprescribed commit.** `chore(e2e): axe audit waits for in-viewport entry fades to settle before measuring contrast` — the harness fix described under Discovered. Test code only, no `src/` change; separate from the docs commit for the same reason as the screenshot-spec commit.
- **F8 · auth-gated proof NOT RUN.** `DEV_LOGIN_EMAIL` / `DEV_LOGIN_PASSWORD` are set neither in the shell nor in `.env.local` (key names checked, values never read), so: the 30 auth screenshot tests skip; the `/case` saved-line + failure-Alert, `/compose` preview-open, vault create-form (loss warning) and `/vault` idle-lock (`idleMs={20_000}`) captures were **NOT RUN**; the F2 manual idle-lock check and the §5 "fresh browser sees the loss warning before typing" check were **NOT RUN**. The reviewing AI also may not type a password into the login form itself. These need one founder session with dev credentials (or the founder's own browser) — listed under "Founder sign-off".
- **F8 · AA-30 ticked before sign-off.** Ticked in `02-BUILD-PLAN-AMENDMENTS.md` with the F7 hash as the prompt prescribes; the tick text says the founder screenshot sign-off is still pending rather than implying it happened.
