# DECISIONS.md — reasoning ledger

Every non-trivial decision is appended here (one entry per decision): **date · decision · alternatives · rationale · files**. A future session reads this instead of re-deriving intent.

Format:

```
## YYYY-MM-DD — <title>
- Decision: <what was decided>
- Alternatives considered: <options>
- Rationale: <why>
- Files affected: <list>
- Decider: <Founder | AI assistant | both>
```

---

## 2026-08-31 — Repo scaffold + toolchain pin (M-1 / B-02 start)

- Decision: Initialize `V:\AppealDeck` as a TypeScript project with a platform-agnostic `src/core/` layer, Vitest for unit tests, strict `tsconfig`, and a GitHub Actions CI that typechecks + tests on Node 20. Core compiles for web and extension from one source tree (D3 rule 1: no `chrome.*` import in `src/core/`).
- Alternatives considered:
  1. Full Next.js App Router scaffold now (AGENTS.md tech stack lists Next.js) — deferred: web revenue surface is M-W (wk 4–5); the core M-1 work is framework-agnostic and should not be coupled to a web framework yet. Next.js shell is added at M-W (`B-19`).
  2. Vite-only monorepo now — rejected in favor of keeping the root minimal and letting the extension (crxjs/Vite, AM-15) and web (Next.js) entries be added as their own build targets later.
- Rationale: keep the compliance-critical, testable core independent of any host so the same code powers both the web decoder and the MV3 extension; CI green from commit #1 per M-1 gate.
- Files affected: `.gitignore`, `CLAUDE.md`, `docs/DECISIONS.md`, `.env.example`, `package.json`, `tsconfig.json`, `.github/workflows/ci.yml`, `src/core/index.ts`, `src/core/index.test.ts`.
- Decider: AI assistant (founder confirms before build proceeds past Phase-0).

## 2026-08-31 — Toolchain pin (AM-15)

- Decision: Pin `@crxjs/vite-plugin` `^2.7.x` for the future MV3 extension build; WXT is the named fallback if crxjs goes >90 days without a release or breaks against a Chrome/Vite upgrade with no fix in 2 weeks. The platform-agnostic core is unaffected by either choice by construction.
- Alternatives considered: WXT as primary — deferred; crxjs v2.7.1 is viable and peer-deps Vite 3–8.
- Rationale: AM-15 verification (25 Aug 2026).
- Files affected: recorded here; applied in `package.json` when the extension target is added.
- Decider: AI assistant.

## 2026-08-31 — Secrets discipline (Phase-0 blocker, never re-litigated)

- Decision: `.gitignore` blocks `.env*`, `*.pem`, `*.zip`, and all credential/json patterns repo-wide (including inside `dist/`). Real secret values live ONLY in deployment-env stores (Supabase secrets, GitHub Actions secrets, Cloudflare). Repo carries `.env.example` with empty values. A fresh Supabase project is used for AppealDeck — never the compromised `fogvzjtxbqgfppdrxqra`.
- Rationale: the prior leak was caused by `dist/` shipping `.env.local` (plaintext Postgres password + Supabase JWTs). This is the exact failure mode to prevent.
- Files affected: `.gitignore`, `.env.example`, `Planning/01-PHASE-0-BLOCKERS/01-CREDENTIAL-ROTATION.md`.
- Decider: both (founder rotates; AI enforces in repo).

## 2026-08-31 — Legal page drafts (D8 / AM-05 / Paddle prerequisite)

- Decision: AI drafted four legal pages as Markdown for founder review + publish: `content/legal/privacy.md`, `content/legal/terms.md`, `content/legal/refund.md`, `content/legal/withdrawal-consent.md` (the checkout consent implementation spec). All reflect D6 (no "guarantee"), D8 (7-day voluntary refund + EU-withdrawal consent), local-first (D6), and Paddle-as-MoR.
- Alternatives considered: buying templates — deferred; these drafts follow the playbook's compliance spine and the founder verifies before publishing. A scoped legal review (~$300–800) is folded into the pre-US-marketing check (decision-log §3 row 10).
- Rationale: Paddle rejects applications without a live site + legal pages; drafts unblock B-05/B-06. Words are founder-reviewed before any publish (copy is user-facing).
- Files affected: `content/legal/privacy.md`, `content/legal/terms.md`, `content/legal/refund.md`, `content/legal/withdrawal-consent.md`.
- Decider: AI assistant (drafts); founder (publish/approve).

## 2026-08-31 — Team/ops drafting batch (founder-review, then use)

- Decision: AI drafted the collaborator paperwork + ops scaffolding the playbook requires: advisor-agreement template, mutual NDA template, GDPR operational kit, recruitment audition fixture (Section 3 notice with the two traps) + scoring rubric + log template, and top-20 support canned responses. All are founder-reviewed before use; never shared with a candidate before a signed agreement (collaborator policy §1).
- Rationale: these are the "mine" deliverables that let the founder hire the consultant/VA/design-partners safely and run GDPR + support from Week 1.
- Files affected: `Planning/08-TEAM/advisor-agreement-template.md`, `Planning/08-TEAM/nda-template.md`, `Planning/06-OPERATIONS/GDPR-KIT.md`, `Planning/08-TEAM/audition-fixture.md`, `Planning/08-TEAM/audition-rubric.md`, `Planning/08-TEAM/recruitment-log-template.md`, `Planning/06-OPERATIONS/canned-responses.md`.
- Decider: AI assistant (drafts); founder (adopt/use).

## 2026-08-31 — Fixture corpus (B-03 seed) + M-1 core start (parser/classifier/deadlines)

- Decision: created `src/core/fixtures.ts` (typed fixture corpus: INAUTHENTIC, RELATED_ACCOUNT, POLICY, IP, LISTING, FUNDS + 2 adversarial, with the legacy "17-day" parse pattern and missing-invoice trap encoded) and the first platform-agnostic core modules: `noticeParser.ts` (regex fact extraction, no `chrome.*`), `classifier.ts` (deterministic stage-1 → ViolationKind + severity gate), `deadlinesModel.ts` (AM-03 six kinds: appeal_window / funds_appeal_eligible=+60d / funds_review=+90d checkpoint / seller_challenge / aha_72h / indefinite_hold). Tests: 7 passing.
- Alternatives considered: storing fixtures as loose `{raw,expected.json}` files — chose a TS module so the parser is built against typed fixtures and CI validates them in one run (B-03's shape is derivable). Corpus is a SEED: expand to ≥4 per kind and validate the violation taxonomy against build plan §13.1 + the retained consultant before the M-3 gate.
- Rationale: this is the compliance-critical, testable core that powers both web and extension (D3). Deterministic regex always beats LLM output (B-11).
- Files affected: `src/core/fixtures.ts`, `src/core/fixtures.test.ts`, `src/core/noticeParser.ts`, `src/core/classifier.ts`, `src/core/deadlinesModel.ts`, `src/core/core.test.ts`.
- Decider: AI assistant.

## 2026-09-02 — External-AI review digested into AM-16 (evidence-first hardening); its conflicting suggestions rejected

- Decision: The founder's 1 Sep 2026 Meta AI brainstorm was fact-checked (against the verified evidence base + fresh 2 Sep live-web research) and converted into amendment **AM-16**: evidence requirements matrix (`src/core/evidenceModel.ts`), action checklist with attestation, deterministic case-readiness (labeled "case-file completeness — not a prediction"), two-mode composer (gap draft vs full draft), document-type router (poa/ip_dispute/funds_appeal first), outreach letter templates, opt-in outcome schema, quarterly policy re-check. Spec + full fact-check: `Planning/03-PHASE-2-BUILD/04-EVIDENCE-FIRST-HARDENING.md`.
- Alternatives considered:
  1. Adopt the conversation's product ideas as-is (SP-API ingestion, $299/mo subscription, "82% approval predictor", model ensemble fine-tuned on POAs) — rejected: each conflicts with a locked decision (D7/D2/D6/D9) or the local-first wedge; recorded in the rejected register (§4 of the new file) so it is never re-litigated without a founder-approved decision-log append.
  2. Do nothing (spec already has critic + anti-fabrication) — rejected: 2 Sep forum evidence shows instant bot rejections when required documents are missing and template language is detected; our composer could still emit a truthful but evidentially hollow POA. The gap is real.
- Rationale: the #1 real-world rejection cause is missing/mismatched evidence and promises instead of performed actions; modeling evidence per violation kind and gating composition on attested completeness is the highest-leverage hardening available, and it strengthens (not reopens) D6. No D1–D10 decision is changed.
- Files affected: `Planning/03-PHASE-2-BUILD/04-EVIDENCE-FIRST-HARDENING.md` (new), `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md` (AM-16, AA-19…AA-21), `CLAUDE.md` §4.
- Decider: AI assistant (spec + fact-check); Founder ratifies AM-16 before M-4 implementation and owns the two banned-list additions + competitor-dossier merge (07-REFERENCE is edit-gated).

## 2026-09-02 — AM-17 Case OS: lifecycle state machine, objection-aware actions, response analyzer, chat adopted as guided Copilot (not a chatbot)

- Decision: Per founder direction ("OS for sellers, not a writing tool; chat window that suggests actions/documents and offers alternatives when the seller declines"), adopted amendment **AM-17**: `src/core/caseState.ts` (deterministic case state machine incl. FUNDS_TRACK and GATED_PRO_HELP short-circuit), objection-extended `ActionItem` (declined-with-reason + predefined consultant-reviewed alternatives, always incl. "decline and proceed" with honest consequence), `src/core/responseAnalyzer.ts` (Amazon-reply categorization promoted from P1 to v1, deterministic-first, reply fixtures added to the corpus), and **Case Copilot** — the requested chat window built as a guided interview over the M4 questionnaire engine (LLM limited to extract/phrase/select per turn; per-turn guardrails; transcript never a composer source; classic wizard = degradation floor). Spec: `Planning/03-PHASE-2-BUILD/05-CASE-OS-SPEC.md`.
- Alternatives considered:
  1. Free-form chatbot as the primary surface — rejected: it is failure modes F1/F5 (improvised advice, invented facts) with a UI, an AM-12 cost/abuse hazard, and it breaks fact-locking (transcript would become the source of truth). Recorded in the spec §4.1 so it is never drifted into; open-question answering is staged as v1.1 behind an explicit gate.
  2. Skip the lifecycle layer and ship AM-16 alone — rejected: the median real case is multi-round; without state machine + response analyzer the product goes silent after draft #1, which is the "writing tool" ceiling the founder named.
  3. New milestone for the Copilot — rejected: scope guard folds §1–§3 into M-4 and Copilot v1 into M-W, with a defined cut order (v1.1 chat → Copilot skin → analyzer LLM assist) and a feature freeze after AM-17 until first paid Passes + opt-in outcomes.
- Rationale: converts the product from single-document generation to case management (the verifiable gap between junk tools and agencies) while every suggestion remains deterministic-engine output; the seller keeps full agency (D6 read-only), refusals become informed choices instead of silent skips, and no locked decision is touched.
- Files affected: `Planning/03-PHASE-2-BUILD/05-CASE-OS-SPEC.md` (new), `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md` (AM-17, AA-22…AA-25; DoD count 25), `CLAUDE.md` §4.
- Decider: AI assistant (spec, per founder direction); Founder ratifies AM-17 alongside AM-16 before M-4 implementation (AA-25).

## 2026-09-02 — Interview-not-chat: AM-17's conversational surface hardened to a finite step flow (founder second pass)

- Decision: Per the founder's same-day follow-up ("a 24/7 chat window will be used like a general AI tool and burn tokens; make it a subsequent conversation system — OS asks, seller submits or denies with reason, OS analyzes, next window — capped to our services strictly"), AM-17's surface is revised: **no chat window exists**. The Guided Interview (retired name: Case Copilot) is a finite step flow made abuse-proof by construction — no persistent input box (typed per-step inputs; bounded short-text only where a narrative is required); engine owns the agenda; deterministic-first routing (LLM only for bounded free-text extraction); **no `/chat` backend route** (only `decode`/`extract-field`/`compose`/`critique`/`analyze-reply`, input-capped, schema-constrained); per-case LLM budget metered server-side on the case key (free tier reaches only `decode`, behind a deterministic notice-likeness prefilter); finite sessions with a fixed off-topic redirect. The former "v1.1 open chat" is replaced by a zero-LLM case-questions drawer (guidance-corpus cards); generative Q&A parked indefinitely behind opt-in outcome data + a decision-log append. Spec: `Planning/03-PHASE-2-BUILD/05-CASE-OS-SPEC.md` §4 (rewritten).
- Alternatives considered:
  1. Open chat + rate limits/turn caps — rejected: policing instead of design; still reads as "free AI chat", still invites general use, still a per-turn claims/injection surface.
  2. Pure static forms with no free text at all — rejected: loses the decline-with-reason nuance and the honest root-cause narrative, which are the interview's whole value; bounded free-text fields with single extraction calls keep that at cents-per-case.
- Rationale: token spend and abuse become structurally bounded (per-case budget × five schema-constrained routes × prefilter) rather than policy-bounded; the "strictly professional, capped to our services" posture is also the positioning — general-purpose AI chat is the competitors' failure mode.
- Files affected: `Planning/03-PHASE-2-BUILD/05-CASE-OS-SPEC.md` (§4 rewritten, terms/cut-order/DoD updated), `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md` (AM-17 provenance + Interview bullet + M-W gate + AA-23 reworded), `CLAUDE.md` §4.
- Decider: AI assistant (design), per explicit founder direction; ratification rides AA-25 with AM-16/AM-17.

## 2026-09-04 — AM-18 Premium UI/UX: design system v2 and pre-deploy UI waves (presentation only)

- Decision: Per founder direction ("most things are complete; make the UI/UX feel truly premium per modern practice before deployment, and show how to structure it"), adopted amendment **AM-18** with spec `Planning/03-PHASE-2-BUILD/06-PREMIUM-UI-UX-SPEC.md`: tokens v2 (`next/font`, fluid type, tabular numerals, surface tiers, motion/radius/width scales), shadcn/ui primitive gaps, product patterns (HonestExpectationsCard, DeadlineChip, Stepper, EmptyState, LocalFirstBadge, VerifiedStamp, CopyButton, PoaSection, BeforeYouSubmitChecklist), a motion system with reduced-motion honoured in framer-motion, a mandatory loading/empty/error/success quartet per surface, page-by-page requirements, WCAG 2.2 AA + Lighthouse gates, `defaultTheme` → `system`, one AppHeader. Work ships in Waves A (foundation) and B (marketing conversion) before first deploy, Wave C (app confidence) with M-W.
- Alternatives considered:
  1. Implement the external coding agent's 22-item list as given — rejected in part: confetti on a POA draft is the banned readiness-as-approval framing (D6); onboarding tours get skipped; an analytics consent banner is unnecessary if the D10 cookieless choice holds (verify at deploy); "save & exit" to plaintext `localStorage` breaks the local-first-encrypted spine (persist via the vault store instead); the agent's invented founder story ("denied twice") must never be published. Adopted items are mapped to waves in spec §14.
  2. Ad-hoc page polish without tokens/primitives — rejected: it is how inconsistent, "cheap-feeling" UIs happen; tokens → primitives → patterns → pages is the only order that scales for a solo founder with an AI builder.
  3. Storybook / full design-tooling stack — rejected: a dev-only `/dev/ui` gallery route and a one-page `docs/DESIGN-SYSTEM.md` are enough at this size.
- Rationale: for a $199 high-consideration product with zero social proof, perceived quality and honesty _are_ the conversion mechanism; premium here means calm authority (one accent, restraint, no decorative motion), the honest-expectations card as a designed component, product-as-artwork visuals, and complete states — never scarcity, timers, testimonials, or celebration of drafts. Scope guard keeps this presentation-only so the post-AM-17 feature freeze and D6/D7 stay intact.
- Files affected: `Planning/03-PHASE-2-BUILD/06-PREMIUM-UI-UX-SPEC.md` (new), `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md` (AM-18, AA-26…AA-28; DoD count 28; M-W gate amended), `CLAUDE.md` §4/§5.
- Decider: AI assistant (spec, per founder direction); Founder ratifies AM-18, supplies the true founder-note text, and signs off Wave A+B screenshots before first deploy (AA-28).

## 2026-09-04 — Single-host topology for the first deploy; content & voice system and simplicity budget added to AM-18

- Decision: Marketing, auth, and the app ship on ONE origin (Vercel-provided URL now, apex domain when connected) with path-based routing. `src/middleware.ts` gains a single-host mode (default when `NEXT_PUBLIC_APP_HOST` is unset or equals the marketing host); `src/lib/urls.ts` resolves `APP_URL` to `SITE_URL`; the `app.` subdomain split is retained behind env vars with a switch checklist in `AGENTS.md` ("Domain topology"). AM-18 additionally adopts a content & voice system (06-spec §10: trust shown by mechanism and never requested, banned-pattern lint, copy centralised in `src/content/`, full copy audit as AA-29) and a binding simplicity budget (§1.1).
- Alternatives considered:
  1. Keep the planned `app.appealdeck.com` split from day one — rejected for now: an extra domain, extra Supabase/Paddle URL configuration, a cross-origin auth hop, and the middleware's `APP_PREFIXES` no longer matched the real app routes after 882ed39 — on a `*.vercel.app` deploy `/auth/callback` would have redirected to a non-existent host. No product benefit at this stage.
  2. Delete the split code — rejected: cheap to keep behind env vars; a later split (cookie isolation, separate caching) becomes env + DNS only.
  3. Keep copy inline in components and rely on the `guarantee` grep alone — rejected: "every single word speaks properly" needs one reviewable place and a wider banned-pattern gate (assertion-trust words), not one token.
- Rationale: fewer moving parts before first users; the seller never changes origin mid-flow; copy quality becomes auditable and lintable.
- Files affected: `src/middleware.ts`, `src/lib/urls.ts` (new), `src/app/(app)/{login,signup,forgot-password,billing}/page.tsx`, `src/app/(app)/page.tsx`, `.env.example`, `docs/DEPLOYMENT.md` §2–§4, `AGENTS.md` (hosting bullet, deploy step 6, new "Domain topology" section, design standards), `Planning/03-PHASE-2-BUILD/06-PREMIUM-UI-UX-SPEC.md` (§1.1, §8, §10, §13, §15), `02-BUILD-PLAN-AMENDMENTS.md` (AM-18 bullets, AA-29; DoD 29), `CLAUDE.md` §4.
- Decider: AI assistant per founder direction (4 Sep 2026); founder runs the domain checklist when the apex domain is connected.

## 2026-09-07 — Wave C: /dashboard, server license gates, callback same-origin, vault-backed case store

- Decision: Promote `/dashboard` to the app home after the Wave C audit found three critical defects (hardcoded vault passphrase, missing license gate on `/api/analyze-reply`, fabricated deadline on the dashboard). Keep single-host topology.
- Alternatives: (1) Reopen AM-16/AM-17 engine changes — rejected, out of scope for a fix pass. (2) Move case-file persistence back to sessionStorage — rejected, violates settled D2.
- Rationale: Ship the audited state as true, with evidence. Do not invent data the engine does not produce.
- Files affected: `docs/handoffs/2026-09-07-wave-c-fix.md` (evidence log), `src/components/DashboardClient.tsx`, `src/app/api/analyze-reply/route.ts`, `src/components/InterviewFlow.tsx`, `src/components/VaultView.tsx`, `src/components/ComposeView.tsx`, `src/components/BeforeYouSubmitChecklist.tsx`, `src/components/PoaSection.tsx`, `src/app/(app)/case/page.tsx`, `src/app/(app)/compose/page.tsx`, `src/app/(app)/dashboard/page.tsx`, `src/components/Stepper.tsx`, `src/components/VaultGate.tsx` (new), `src/components/FileDropZone.tsx` (new), `src/content/app.ts`.
- Decider: AI assistant executing the Wave C-fix pass per `docs/handoffs/2026-09-07-wave-c-fix-prompt.md` §3 (7 Sep 2026).

## 2026-09-07 — Wave C-fix pass opened (audit findings)

- Decision: Open a fix pass to address the three criticals and ten user-visible logic bugs found by the independent line-by-line audit of Wave C (commits `59b7492..9e4bfb5`).
- Audit findings (criticals): (1) `InterviewFlow.tsx` initialised the vault with `"default-default"` — seller never chose a passphrase. (2) `DashboardClient.tsx` presented the notice date as an appeal deadline (`dueAt: new Date(noticeDate)`) — an invented deadline. (3) `/api/analyze-reply` had no `isLicenseActive` gate — the only paid route without one.
- Rationale: The Wave C commit chain passed all CLI gates but shipped defects that only end-to-end review caught. Each is fixed with evidence in one commit per task, per §0.2.
- Decider: AI assistant per founder direction (7 Sep 2026).

## 2026-09-09 — AM-19 ratified: Meta AI UI/UX brainstorm → 13-item polish pass now (AA-30), critic rules with M-4 (AA-31)

- Decision: Adopt register rows #23–#32 (`docs/handoffs/2026-09-08-meta-ai-uiux-register.md` §6). Ship the ten presentation/hygiene items before first deploy (AA-30, prompt `docs/handoffs/2026-09-09-uiux-polish-prompt.md`); defer the two core-touching rows to M-4 (AA-31). Everything else in the 189-row register stays a recorded backlog (GOOD/NICE, post-freeze) or AVOID. Founder confirmed in chat: all ten rows, the disclaimer wording as proposed, vault idle auto-lock is security hygiene exempt from the AM-17 freeze, "(optional)" markers and first-use term definitions ride AA-29.
- Alternatives: (1) Implement Meta AI's full list — rejected: 56 rows break D6/D7/D9/§1.1 (approval scores, countdowns, social proof, chat bubble, template tabs, Seller Central access). (2) Do nothing until after first sales — rejected: three rows are trust/data-integrity gaps (no independence statement anywhere; silent autosave failure in `InterviewFlow.tsx`; unrecoverable passphrase never disclosed). (3) A sticky disclaimer bar — rejected: §1.1; a footer line + Terms section + FAQ answer carry the same fact.
- Rationale: about one working day of copy, attributes and state feedback that closes real gaps without adding a nav item, a card above the fold or a second primary action; the freeze stands; the single core addition (`Vault.findByPlaintext`) exposes stored data and changes no decision.
- Files affected: `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md` (AM-19, AA-30/AA-31, DoD 31), `Planning/03-PHASE-2-BUILD/06-PREMIUM-UI-UX-SPEC.md` §14 rows 23–32, `docs/handoffs/2026-09-09-meta-ai-uiux-plain-guide.md`, `docs/handoffs/2026-09-09-uiux-polish-prompt.md`, `docs/handoffs/2026-09-09-uiux-polish.md` (evidence log with baselines), `CLAUDE.md` §4.
- Decider: Founder (ratification in chat, 9 Sep 2026); AI assistant (register, guide, prompt, Task 0 paperwork).

## 2026-09-10 — AM-21 ratified: access ladder (no account → free account → Appeal Pass), browser-key draft with passphrase re-lock, purchase continuity; the access pass runs before the visual refresh

- Decision: The guided interview starts with no account and saves every answer into the vault from the first step under a non-extractable browser-generated key; the first document step asks for sign-in (email + password, Google or magic link, all with a return path); on return the seller sets the passphrase once and the same records are re-locked under it; the composer asks for the Appeal Pass, with the checkout in the overlay, a completion event, a read-only license-status endpoint and an activation state that resumes into the draft. Header shows five slots in both states with a lock on Vault when signed out. AI field suggestions move from Pass-only to free-account under a per-user daily cap. Pricing becomes three columns. Rule for every gate: save first, gate second, resume third. Sequence: this pass (AA-33) first, then the visual refresh v3 (AA-32), whose prompt, spec, copy deck and evidence log were updated the same day.
- Alternatives: (1) Keep everything behind the Pass after sign-in — rejected by the founder: a visitor sees a decoder and a price and nothing else. (2) Show locked app pages publicly but gate the interview behind sign-in — rejected by the founder: the interview must be startable without an account. (3) Hold the anonymous draft in memory or in plain browser storage — rejected: lost on refresh or on the Google redirect, or a plaintext store that spec 06 §14 already rejected. (4) Inline sign-in dialog to avoid the redirect — unnecessary once the draft is already encrypted on disk. (5) Google-only sign-in — rejected: Amazon sellers often run on business email; continuity no longer depends on the method. (6) A server-side draft — rejected: local-first, no case data on our servers.
- Rationale: matches D10's funnel (intake before purchase) and the build plan's original free-tier intent (an intake preview with the POA locked), at zero server cost for the free tiers; the vault already re-wraps its DEK for `changePassphrase`, so a second key source is the smallest correct change; the webhook stays the only writer of license state; nothing in D1–D10 is reopened.
- Files affected: `Planning/03-PHASE-2-BUILD/07-ACCESS-AND-CONTINUITY-SPEC.md` (new), `02-BUILD-PLAN-AMENDMENTS.md` (AM-21, AA-33/AA-34, DoD 34), `docs/handoffs/2026-09-10-access-continuity-prompt.md`, `docs/handoffs/2026-09-10-access-continuity.md`, `docs/handoffs/2026-09-10-access-continuity-plain-guide.md`, `docs/handoffs/SESSION-START-PROMPT.md`, `CLAUDE.md` §4; the visual pass material (`docs/handoffs/2026-09-09-visual-refresh-prompt.md`, `…-spec.md`, `…-copy-deck.md`, `…-visual-refresh.md`) updated to start after this pass.
- Decider: Founder (direction and ratification in chat, 10 Sep 2026); AI assistant (spec, prompt, paperwork).

## 2026-09-10 — AM-20 ratified: visual system v3 (brand mark, tokens, shell, hero-as-product)

- Decision: Adopt a single brand green (`#1C7D5E` light / `hsl(160 52% 62%)` dark), the "deck, checked" mark (option E2, founder-locked from eleven candidates) as a generated asset kit (`public/brand/`), colour tokens v3 with tinted semantic badges/alerts and tinted shadows, Inter variable with optical sizing, a fluid type scale, width tokens, a rebuilt header/footer, restyled primitives, and a hero-as-product home page — appearance only, no engine or decision change. Sample-notice text replaced with a decodable fictional notice (fixes the P0 "Try a sample notice → Could not decode" bug). Value-first marketing copy per the copy deck applied in the same pass (Task V10).
- Alternatives: (1) Do nothing until after first sales — rejected: the free funnel's own sample notice fails to decode, and `/decode` renders with no header or footer; these are live-funnel defects, not polish. (2) Hire a designer — deferred to post-revenue; no budget for it pre-launch. (3) Migrate to Tailwind v4 / OKLCH while touching every colour token — rejected as a separate, larger decision out of scope for this pass.
- Rationale: the founder's own words were "premium, advanced appearance — logo, icons, colour theme, fonts, spacing, layout... the appearance needs a lot of effort"; the audit found 2 P0 and 42 P1/P2 defects; fixing them requires exactly this scope, with zero new dependencies and no route beyond metadata routes.
- Files affected: `docs/handoffs/2026-09-09-visual-audit.md`, `…-visual-refresh-spec.md`, `…-visual-refresh-prompt.md`, `…-visual-refresh-plain-guide.md`, `…-copy-deck.md`, `…-visual-refresh.md` (evidence log), `public/brand/*`, `docs/handoffs/assets/*`, `02-BUILD-PLAN-AMENDMENTS.md` (AM-20, AA-32), `CLAUDE.md` §4.
- Decider: Founder (mark selection, sample-notice approval, copy-deck approval, all 9 Sep 2026); AI assistant (audit, spec, prompt, asset generation, build).

## 2026-09-11 — Full-repo audit; AM-16/AM-17 ratified; nine planning docs corrected against the actual codebase

- Decision: Ran a six-part audit of every planning doc, handoff, and branding asset against the real codebase (`docs/handoffs/2026-09-11-full-repo-audit-guidebook.md`), then executed the founder-approved fixes rather than leaving them as an unactioned report. AM-16 and AM-17 are ratified — both were built and tested in code but never formally signed off together, per the standing blocker in `CLAUDE.md`. AA-10's `docs/BUILD_PLAN.md` deliverable is descoped: the engineering it would describe (`src/lib/llm/gemini.ts`'s task-routing matrix) is already built and this file plus `docs/DECISIONS.md` already carry the same information — a second working-spec copy would only be one more place to drift.
- Corrections applied (documentation only, no engineering behavior changed): eight `02-BUILD-PLAN-AMENDMENTS.md` checkboxes (AA-01, AA-05, AA-08, AA-14, AA-15, AA-16, AA-28, AA-29) ticked with evidence citations where the code was already done but the checklist never caught up; `08-WAVE-C-HANDOFF.md` rewritten to correct the specific false statements its own follow-up fix prompt (`docs/handoffs/2026-09-07-wave-c-fix-prompt.md` Appendix B) had flagged back on 7 Sep and that were never corrected; seven docs (`07-REFERENCE/04-UNKNOWNS-REGISTER.md`, `07-REFERENCE/05-RESOURCE-STACK-AND-BUDGET.md`, `04-PHASE-3-LAUNCH/01-WEB-DECODER-LAUNCH.md`, `05-PHASE-4-GROWTH/03-SEO-CONTENT-PLAN.md`, `06-OPERATIONS/GDPR-KIT.md`, `Planning/README.md`, `Planning/MASTER-CHECKLIST.md`) corrected from the superseded Cloudflare Pages + Supabase Edge Functions hosting plan to the actual single-host Vercel architecture decided 4 Sep 2026; `08-TEAM/03-AI-SESSION-CONTINUITY.md` §4 rewritten to describe the dated-topic-file + evidence-log handoff convention actually in use (the original one-file-per-milestone convention was never used even once); nine files' stale `V:\AppealDeck\` repo path corrected to `V:\AppealDeck1\`; `Planning/MCPs Essential List.md` corrected against the live `kilo.json` (eight MCPs marked "Must Add" were already active); `03-PAYMENTS-SETUP.md` and `03-TECHNICAL-RISK-CONTROLS.md` (TRC-11) annotated to record that the `PAYMENTS_PROVIDER` env-switch and the offline-extension licensing cache were both deliberately not built for reasons already true in the shipped architecture, not oversights; two stray untracked files (`appealdeck.md`, `disconnected-chat!.txt`) deleted; `.gitattributes` added (`eol=lf`) to stop a recurring Windows CRLF format-check flake; `src/core/fixtures.ts` gained a `source` provenance note per fixture (all confirmed synthetic, none derived from a real seller's confidential document, per the original fixture-corpus spec's sourcing rule that had gone undocumented).
- Alternatives considered per item, not just applied wholesale: for each doc/code mismatch the audit found, checked whether the _code_ should change to match old guidance still holding real value, or the _doc_ should change to match code that had already (correctly) superseded the old plan — see the guidebook's Section F for the item-by-item reasoning. Two items were built into code rather than just documented: the fixture provenance notes (kept the working single-file structure, added the sourcing safeguard the spec actually cared about) and the legal-content gaps (see the next entry).
- Rationale: several planning docs were actively describing infrastructure and status that no longer matched reality (in two cases — hosting, and the Wave C handoff's false claims — actively misleading, not just stale), which is a real risk for any future session or collaborator reading them cold.
- Files affected: listed above; also `Planning/07-REFERENCE/01-MARKET-EVIDENCE.md` §4 (two new banned-claim rows: "agency results at 1/10th the cost", readiness-as-approval framing) and `Planning/07-REFERENCE/02-COMPETITOR-DOSSIER.md` (§1.5 new entrants, §3a failure-evidence section, a Seller Basics anchor) merged in from the archived `2026-09-02-COMPETITOR-RECHECK.md` on the founder's approval.
- Decider: Founder (approved the guidebook's plan section by section, 11 Sep 2026); AI assistant (audit, guidebook, and the corrections above).

## 2026-09-11 — Legal content brought to parity with the original drafts; two live compliance gaps fixed

- Decision: A clause-by-clause comparison of `legal/*.md` (the original AI-authored drafts) against `src/content/legal.ts` (the actual rendered source) found `legal.ts` was missing real substance, not just differently worded — merged in rather than left as a stale-doc note. Privacy gained: an international-transfers disclosure (Gemini/Paddle processing outside Pakistan), the fuller data-subject-rights list (access/correct/delete/receive a copy/object) with a 30-day response commitment, a breach-notification commitment, an explicit retention statement, a not-directed-at-under-18 line, and a disclosure that notice text goes to Gemini's paid tier for drafting (plus, once wired, the planned analytics disclosure). Terms gained: the severity-gating disclosure (some case types are never sold a Pass), a liability cap protecting the business, and a plain right to update the terms. The refund page's "5 business days" claim was softened to separate "we process within 5 business days" from "your bank decides how long it then takes" — the flat claim overpromised relative to real bank processing times. `legal.ts`'s Terms `lastUpdated` corrected to 2026-09-10 (the date its content actually last changed — the independence-disclaimer sentence restoration, per the 10 Sep uiux-polish-fix pass); Privacy and Refund to 2026-09-11 (today's substantive edit). "Seller Central" gained a first-use gloss ("Amazon's seller dashboard") on the home page, the compose page, and the shared honest-expectations copy, per the house voice rule that Amazon terms are defined on first use per page.
- Two live, currently-broken things were also found and fixed, unrelated to doc staleness: the checkout promised a confirmation email ("You will receive a receipt and a copy of this consent by email") that no code ever sent — D8 explicitly requires a permanent-form confirmation email, so this was an unfulfilled requirement, not a nice-to-have; and the honest-expectations card rendered _after_, not before, the purchase button on `/pricing`, contradicting both the checkout-consent spec (`legal/withdrawal-consent.md`) and D8's intent. Both fixed in the same pass — see `src/lib/email.ts` and the `src/app/pricing/page.tsx` reorder.
- Alternatives considered: retire `legal/*.md` entirely versus sync them — decided to add a short pointer note in each draft file marking `legal.ts` as the live source, rather than delete the drafts outright, since they remain a useful founder-review copy of the original intent.
- Rationale: D8 (EU-withdrawal-compliant checkout) and the general honesty spine (D6) both depend on the actual checkout page matching what it claims — a stated promise with no code behind it is a worse trust gap than the original staleness this session set out to fix.
- Files affected: `src/content/legal.ts`, `src/content/marketing.ts`, `src/content/app.ts`, `src/core/guidance.ts`, `src/app/pricing/page.tsx`, `src/lib/email.ts` (new), `src/app/api/webhooks/paddle/route.ts`, `legal/*.md` (pointer notes).
- Decider: Founder (approved the guidebook's Section F3 recommendation, 11 Sep 2026); AI assistant (the clause comparison, the content merge, and the two code fixes).

## 2026-09-11 — AM-22 Visual overhaul v4: tokens → primitives → pages

- Decision: Lock the founder-approved visual direction (AM-22) as the next working pass after the founder-issues fix pass (AM-2x/V0–V3 complete at `1e4dd05`). The overhaul is presentation/asset/copy/layout only — no engine, API, or capability changes (AM-18 scope guard preserved). Two founder-gated decisions are called out before implementation: (1) color reconciliation — adopt the mockup's warm-neutral HSL palette (`#FBFBFA`/`#14201C`/`#EDEDEA`) or keep the current cooler-neutral tokens (`224 32% 11%` foreground); the brand green `#1C7D5E` ≈ HSL(161 63% 30%) already matches `--primary`. (2) Vault theme override — `/vault` deliberately renders a permanent dark "obsidian" surface regardless of light/dark toggle, or respects `next-themes` like the rest of the app.
- Alternatives considered:
  1. Rebuild pages freehand, taking only the mockup's general "spirit" rather than matching `Main.dc.html`/`Interview.dc.html`/`Decode.dc.html`/`Vault.dc.html` closely — rejected: the founder approved specific markup only after several iteration rounds fixing copy accuracy, swapping stock photos for original illustrations, correcting font choice, and fixing cross-artboard consistency; freehanding the rebuild would re-lose fixes the founder already paid iteration time for. The mockup files are the source of truth for layout (handoff §1–§2).
  2. Reconcile color/radius tokens implicitly, per component, as each page is rebuilt — rejected: the handoff's §0 thesis is token-first (change `globals.css`/`tailwind.config.ts` once, every component inherits it); doing the reconciliation per-component would repeat the exact drift the mockup itself needed a manual consistency pass to fix.
  3. Have this paperwork pass (V0) also decide the two founder-gated calls itself (color reconciliation, vault theme override) rather than pausing on them — rejected: the handoff explicitly flags both as real design decisions needing a founder confirmation (§2.2, §2.7), not something to guess and ship.
- Rationale: the founder's own words were "everything else looks great... I would like to overhaul our entire application in all means to look even strong, perfect, well designed masterpiece," approved after several iterations of mockup fixes this session (handoff §1); token-first ordering means every later task inherits the change automatically (§0 thesis). The handoff (`docs/handoffs/2026-09-11-visual-overhaul-v4-handoff.md`) locks the approved mockup; the formal task prompt is `docs/handoffs/2026-09-11-visual-overhaul-v4-prompt.md`; the evidence log is `docs/handoffs/2026-09-11-visual-overhaul-v4.md`. Session-start prompt repointed to these files.
- Files affected: `docs/handoffs/2026-09-11-visual-overhaul-v4-handoff.md` (existing, read-only reference), `docs/handoffs/2026-09-11-visual-overhaul-v4-prompt.md` (new), `docs/handoffs/2026-09-11-visual-overhaul-v4.md` (new), `docs/DECISIONS.md` (this entry), `docs/handoffs/SESSION-START-PROMPT.md` (PATH lines + history), `02-BUILD-PLAN-AMENDMENTS.md` (AM-22, AA-35, DoD 35).
- Decider: AI assistant (paperwork, per the handoff's §3 Task V0 prescription); Founder (color reconciliation decision for V1, vault theme override decision for V6).

## 2026-09-12 — AM-22 scope addition: real product screenshots at concrete moments + a typography sweep; new Login mockup artboard

- Decision: Founder asked in chat whether AppealDeck should use real screenshots of its own product the way Apollo/Linear/Stripe do (e.g. on the login page), and whether font sizing needed explicit attention across the app. Both are folded into AM-22 rather than treated as separate asks. Real screenshots are approved at concrete product moments (login/signup split-screen now; the home hero panel once V3 rebuilds it) — this does not reopen §2.6's rejection of generic stock photography, a different category of image entirely. Because the real UI doesn't carry the new visual language yet, `Login.dc.html` (new mockup artboard) uses a framed, explicitly-labelled sample-data preview in place of a real screenshot, with a task to swap in a genuine one once the real pages are rebuilt (V3b, new task). A type-scale audit of the four existing mockups found real drift (eyebrow micro-labels alone rendered at three different sizes/trackings across the four screens for the same semantic role) and reconciled it to one named scale, documented in the handoff §2.9 and as a comment in each `.dc.html`.
- Alternatives considered:
  1. Use real screenshots everywhere immediately, including the current (pre-overhaul) UI — rejected: would capture the look this pass exists to replace; screenshots only make sense once V1–V8 build the new design.
  2. Leave typography as ad-hoc per-element sizing and catch it only in the V7 sweep pass, after all five pages are built — rejected: cheaper to fix once, now, in the mockup than to re-discover the same three-way drift across five real pages later; matches the handoff's own token-first thesis (§0).
  3. Treat the login/signup rebuild as part of V7 (sweep pass) rather than its own task — rejected: it is new layout, not a consistency fix, so it gets its own task (V3b) with its own Do/Accept/commit.
- Rationale: real product imagery is a stronger trust signal than illustration for a screen whose whole job is "come back and see your case" — showing the actual interface, honestly labelled where it's sample data, fits D6's honesty spine better than either a generic photo or an unlabelled fake. The typography fix is cheap now and expensive to discover five times over during V3–V6.
- Files affected: `docs/handoffs/2026-09-11-visual-direction-v2-mockup/Login.dc.html` (new), `.../Main.dc.html`, `.../Interview.dc.html`, `.../Decode.dc.html`, `.../Vault.dc.html` (type-scale reconciliation), `.../canvas.json` (5th artboard + note), `docs/handoffs/2026-09-11-visual-overhaul-v4-handoff.md` (§2.8, §2.9 new), `docs/handoffs/2026-09-11-visual-overhaul-v4-prompt.md` (V3b task added), `docs/DECISIONS.md` (this entry). Mockup republished at https://claude.ai/code/artifact/a1305de4-04bf-4dc4-a2f6-658cd62c1337 (the original `020b42eb…` link returned "not found" this session — likely tied to the authoring session's own account context).
- Decider: Founder (the screenshot and typography direction, in chat); AI assistant (the mockup build, the type-scale reconciliation, and folding both into AM-22's task list).

## 2026-09-12 — Follow-up: font-family usage audit (not just size) finds and fixes three more real defects

- Decision: Founder pushed back — asked directly whether font-_family_ usage across Inter/Newsreader/JetBrains Mono had actually been checked, not just sizes. Re-audited by grepping each mockup's Google Fonts `<link>` against actual class usage rather than assuming the earlier size-only sweep had covered it. Found and fixed: `Main.dc.html` loaded JetBrains Mono unused (removed); `Decode.dc.html` loaded Newsreader and defined an unused `.accent` rule with no headline on that screen to carry it (removed rather than inventing one); the documented type-scale's "H1 40" line matched nothing actually built anywhere — `Interview.dc.html`'s real headline is 32px/800, and `Login.dc.html`'s first draft had independently invented 30px — corrected the scale to the real 32px and made Login match Interview exactly; a stray `11px` eyebrow inside `Login.dc.html`'s own preview panel (should be the standard 11.5px) from when the file was first written. All nine eyebrow instances across all five screens are now identical.
- Alternatives considered: give Decode.dc.html an accent word somewhere to justify keeping the Newsreader import, rather than removing it — rejected: the design skill's own content rule is "every element should earn its place"; forcing an accent word onto a screen with no natural headline moment is decoration for its own sake, exactly the kind of filler this project's D6/simplicity-budget conventions already reject elsewhere.
- Rationale: the first pass checked sizes but not whether a loaded font family was ever actually rendered, or whether elements playing the same visual role (Interview's and Login's page headline) were ever compared to each other directly — both are real font-usage bugs, not size drift, and the founder was right to ask separately.
- Files affected: `docs/handoffs/2026-09-11-visual-direction-v2-mockup/Main.dc.html`, `.../Decode.dc.html`, `.../Login.dc.html` (fixes); `docs/handoffs/2026-09-11-visual-overhaul-v4-handoff.md` (§2.9 addendum); `docs/DECISIONS.md` (this entry). Canvas republished to the same link: https://claude.ai/code/artifact/a1305de4-04bf-4dc4-a2f6-658cd62c1337
- Decider: Founder (asked the question that surfaced this); AI assistant (the audit and fixes).

## 2026-09-12 — AM-22 implementation begins: V1–V4 + V3b shipped to real code, both founder-gates resolved (one reversed)

- Decision: Founder approved implementing the mockup direction into the real app ("I approve the super best appearance as we designed into the mockup files") and asked for batched commits (3-4 tasks per commit, gates run once per batch, not per task) rather than the prompt's original one-commit-per-task convention. First batch: V1 (tokens), V2 (primitives), V3 (home page), V3b (auth split-screen, new task), V4 (guided interview layout). The V1 color-reconciliation gate is cleared as warm-neutral in full. The V6 vault-theme gate was answered differently from the handoff's default reading — the founder's follow-up message explicitly said the vault should respect the site's light/dark toggle like every other page, not permanently override to a dark "obsidian" surface; V6 is re-scoped accordingly before it starts.
- Alternatives considered:
  1. Build a new `StepRail` component from scratch for V4, as the original task text specified — rejected on inspection: `src/components/Stepper.tsx` already implements a numbered-circle vertical rail with connecting lines and state (done/current/todo/skipped), unused for this purpose until now. Reused it instead of shipping a duplicate system.
  2. Keep V4's literal 3-column split (rail | question | why-panel) as planned — rejected after finding a real bug: `/case`'s own page already reserves a 20rem sidebar for `CasePreview`, and `main` is capped at `max-w-app` (72rem) regardless of viewport, so the 3rd column starved the question card to ~168px wide at any screen size (verified via `getBoundingClientRect()` in a live browser, not assumed from reading the JSX). Adapted to 2 columns with the why-we-ask content as a full-width persistent panel instead.
  3. Leave the two e2e test gaps found while verifying V4 (a stale click sequence in `access.spec.ts` missing a real interview step added by an earlier pass; a locator ambiguity in `marketing.spec.ts` from an earlier pass's password-toggle button) unfixed as "pre-existing, not mine" — rejected: both were cheap, safe, high-confidence fixes, and leaving a known-red assertion in the suite helps no one.
- Rationale: matching the mockup pixel-for-pixel is subordinate to the real page actually working — a starved 168px question card would have shipped a genuinely broken interview page had it not been checked live in a browser rather than trusted from the code alone. The founder's batching direction is respected going forward: one gate run, one commit, covering this whole batch.
- Files affected: `src/app/globals.css`, `tailwind.config.ts`, `src/lib/fonts.ts`, `src/app/layout.tsx`, `src/components/ui/accent-word.tsx` (new), `src/components/ui/badge.tsx`, `src/components/illustrations/*.tsx` (new), `src/app/dev/ui/DevUiGallery.tsx`, `src/lib/splitAccent.ts` (new), `src/app/page.tsx`, `src/components/AuthCard.tsx`, `src/content/auth.ts`, `src/components/InterviewFlow.tsx`, `src/content/app.ts`, `e2e/access.spec.ts`, `e2e/marketing.spec.ts`, `docs/handoffs/2026-09-11-visual-overhaul-v4-prompt.md`, `docs/handoffs/2026-09-11-visual-overhaul-v4.md`, `docs/DECISIONS.md` (this entry).
- Decider: Founder (implementation go-ahead, both gate resolutions, batching direction); AI assistant (execution, the V4 layout bug diagnosis and fix, the two e2e fixes).

## 2026-09-12 — AM-22 continues: V5 (decode annotations), V6 (vault surface), V8 (final gates + screenshots) shipped; V7 deferred

- Decision: Founder directed "proceed to V5, V6 and V8" — a second batch under the same batching convention, explicitly not including V7 (sweep pass) this round. V5 (decode result rebuild) and V6 (vault surface rebuild) shipped in one commit; V8 (final gates + screenshot set) run and recorded afterward in the same session.
- Alternatives considered:
  1. Invent a fixed set of "what this means" annotation copy keyed only by case kind, independent of the actual pasted text — rejected: would have described phrases that might not appear in a given seller's notice at all, a direct D6 violation (fabricated claims about the seller's own document). Instead built `buildNoticeAnnotations()` to locate real substrings via the same intent as the core engine's existing `parseNotice()` output, so every card is anchored to text genuinely present in what the seller pasted, and degrades to fewer cards rather than filler when nothing matches.
  2. Replace the existing "Start your case — free" CTA at the bottom of `/decode`'s result with the new "Start your Plan of Action" button, to avoid two similar-looking CTAs on one page — rejected: `e2e/access.spec.ts:62` asserts that exact link by name, so replacing it would have required touching an unrelated, currently-green test for a cosmetic reason. Added the new CTA alongside it instead, inside the annotation column, which only renders when annotations exist.
  3. Replace the vault's existing hover-tooltip crypto disclosure (`APP.vault.cryptoDetails`) with the new short always-visible envelope caption line, to avoid duplicating the same information two ways — rejected for this pass: the tooltip carries materially more detail (full key-derivation explanation) than the short caption is right for, and removing a disclosure control wasn't asked for. Added the caption alongside the tooltip.
  4. Trust the first Lighthouse run's ~0.68–0.70 performance scores (against `next dev`) at face value and report them as the finding — rejected: recognized as a known Lighthouse-vs-dev-server artifact before reporting, re-ran against a real `next build`/`next start` production server, and got 1.0 across all four categories on every page tested, matching the visual-refresh-v3 baseline. Reporting the dev-server number without the production re-check would have been a misleading regression claim.
- Rationale: same as the prior AM-22 batch — matching the mockup is subordinate to the app actually working honestly. Every new UI surface here (annotations, envelope caption) is additive to what already existed and green-tested, not a replacement made for its own tidiness.
- Files affected: `src/lib/decodeAnnotations.ts` (new), `src/components/AnnotationCard.tsx` (new), `src/app/decode/DecodeClient.tsx`, `src/app/globals.css`, `src/content/marketing.ts`, `src/components/VaultView.tsx`, `src/content/app.ts`, `src/app/dev/ui/DevUiGallery.tsx`, `docs/handoffs/2026-09-11-visual-overhaul-v4-prompt.md`, `docs/handoffs/2026-09-11-visual-overhaul-v4.md`, `docs/handoffs/screenshots/2026-09-11/` (new, 30 files), `docs/DECISIONS.md` (this entry), `CLAUDE.md` §4.
- Decider: Founder (scope for this round — "V5, V6 and V8", explicitly not V7); AI assistant (execution, the annotation-anchoring design, the Lighthouse dev-vs-prod investigation).

## 2026-09-21 — Commercial-first-principles reset: paid acquisition ruled out, price raised to $249 flat, channel staged, D6/router scope corrected

- Decision: Founder asked for a review grounded in real market/business fundamentals rather than the accumulated D1–D10/AM-xx stack, and delegated the pricing number to the AI assistant. Findings and calls: (1) at $199 net $188.55, paid acquisition is arithmetically closed — legal-vertical CPC ($9.87 avg, $131.63/lead) requires 5.2% click-to-purchase break-even against a realistic 0.5–2%, and no realistic price fixes this (4.2% break-even even at $249, 2.1% at $499), so price is a credibility/margin decision, not a channel one; (2) price raised to **$249 flat, worldwide, no country tiering, no `.99`** — implemented in `src/content/app.ts`, `src/content/marketing.ts`, `src/content/shared.ts`, and `CLAUDE.md` §1; (3) acquisition channel staged: keep the free-decoder/organic path, and run a one-week test emailing ~20 appeal consultants/agencies to price the case-record capability as professional tooling (agencies invert the CAC problem and pay ~$415/mo in this category); (4) next 30 days = three-day fixes then ten real unpaid seller conversations, not a metrics-gated pilot; (5) confirmed the IP/trademark/copyright and related-account router exclusions in `src/core/workspace.ts` were never actually locked by D6 (which names only forged documents, fraud, and child safety) — they were implementation drift, not policy, and reopening them (evidence + outreach, no dispute drafting — decided earlier the same day) does not reopen D6; (6) corrected two stale reference facts that would have produced false marketing copy: AppealsPro's 23% win rate is withdrawn (`01-MARKET-EVIDENCE.md`, `02-COMPETITOR-DOSSIER.md`), and "Amazon's assistant does not write appeals" is no longer safe now that staff confirm it guides appeals; (7) shipped `navigator.storage.persist()` on vault unlock plus an honest on-page storage state, closing a live silent-data-loss risk (the vault is IndexedDB and a case waiting on Amazon routinely exceeds the ~7-day eviction window with no server copy by design); (8) reframed positioning: decode-and-draft is now the commoditized part (Amazon's own assistant does it free and is improving), so the durable claim is the independent case record — sharpened the existing "replies" FAQ answer in `src/content/marketing.ts` to state, honestly, only what is actually built today (visibility into the prior submission before sending again — not an automated diff, which remains unbuilt); (9) added `docs/DECISION-PRINCIPLES.md` as a standing frame for future calls, since good process had substituted for commercial judgement for eight weeks.
- Alternatives considered:
  1. Country-tiered pricing (higher in US/UK/DE, lower where purchasing power is low) — the founder's own instinct, and Paddle supports per-country overrides natively with no re-architecture needed. Rejected for now: it does not reopen the paid-acquisition auction (still mathematically closed at any of these prices), and this audience shares Facebook/Telegram groups where a public price split would surface and read as bait-and-switch in a market whose whole differentiation is transparency. Deferred to revisit after ~30 paid cases, once real conversion-by-market data exists.
  2. $249.99 instead of $249 — rejected: the retail `.99` signals e-commerce discounting; round numbers read as professional-service pricing, which matters more here than the psychological last-cent trick.
  3. Sellers-only or agencies-first as an exclusive channel choice — rejected in favor of staged/both: sellers-only repeats the unvalidated 8-week pattern (five visual passes, zero users); agencies-first abandons the consumer product and the local-first promise on no evidence yet that agencies will actually pay for this specific capability. A one-week, zero-cost email test resolves the uncertainty before either commitment is made.
  4. Bulk-editing all ~30 historical Planning/ docs that still cite $199 — rejected: most are append-only historical records (build sequences, launch checklists, outreach drafts) whose $199 mentions are either dated context or would need a full founder-approved amendment pass of their own; rewriting them silently would misrepresent history. Only the two live reference docs already owned by this session's edits (`01-MARKET-EVIDENCE.md`, `02-COMPETITOR-DOSSIER.md`) and the always-current `CLAUDE.md` one-pager were updated; the rest are flagged, not silently left inconsistent.
  5. A new marketing tagline ("Never send Amazon the same appeal twice") inserted into the hero — rejected as a bolt-on: the existing copy voice (AM-18 "honesty is the aesthetic") is deliberately plain and factual, not slogan-driven, and the same idea already had a natural, honest home in the existing "replies" FAQ answer once tightened to describe only the built capability.
- Rationale: eight weeks of planning had never calculated customer acquisition cost or lifetime value; that single number turned out to constrain strategy more than any feature decision, and finding it changed the acquisition plan, the price, and the positioning claim in one pass. Following Decision Principle #4 ("follow the money before the feature") and #3 ("separate a decision from its implementation") resolved a false constraint (the D6-router conflation) and a real one (the CAC math) in the same session.
- Files affected: `src/content/app.ts`, `src/content/marketing.ts`, `src/content/shared.ts`, `src/lib/vault/persistence.ts` (new), `src/lib/vault/persistence.test.ts` (new), `src/components/VaultGate.tsx`, `src/components/VaultView.tsx`, `Planning/07-REFERENCE/01-MARKET-EVIDENCE.md`, `Planning/07-REFERENCE/02-COMPETITOR-DOSSIER.md`, `CLAUDE.md` §1/§4, `docs/DECISION-PRINCIPLES.md` (new), `docs/handoffs/2026-09-21-commercial-review.md` (new), `docs/handoffs/2026-09-21-phase-{1,2,3,4}-*.md` (new, the preceding four-phase research), `docs/DECISIONS.md` (this entry).
- Decider: Founder (channel staging, next-30-days plan, delegated the price number); AI assistant (the CAC/CPC arithmetic, the $249 price recommendation and its implementation, the router/D6 distinction, the two stale-fact corrections, the storage-persistence fix, the FAQ copy tightening).

## 2026-09-21 — Full-chain re-inspection: closed the Pass-scope disclosure gap and backfilled AM-23/24/25

- Decision: Founder asked for a complete re-inspection of the entire chain — the original ChatGPT/Meta AI transcript (`prompt-for-chatgpt.txt`), the 18 Sep session's response (`70c39f9`, seven open founder questions), the four-phase research (21 Sep, same day), and this session's commercial reset — to determine whether anything real remained undone. Verified against the live code rather than prior summaries and found three of the 18 Sep session's seven questions were never actually answered: (1) "what does the Pass cover — revisions, replies, duration?" — checked the entitlement code (`src/lib/license.ts`, migration `0009`) and found a license is already scoped to one case, not one draft, so revisions are already technically covered, but this was stated nowhere in pricing, FAQ, or Terms copy; closed by adding one sentence each to `src/content/marketing.ts`'s "pass" FAQ item and `src/content/legal.ts`'s licence clause, stating plainly that one Appeal Pass covers every revision of its case with no additional charge and no expiry (confirmed `expires_at` is never set at purchase, so this is honestly the current behavior, not an aspiration); (2) "split policy + add a verification type?" — confirmed `ViolationKind` in `src/core/index.ts` still has no `VERIFICATION` category; correctly left open, deferred behind the ten seller conversations rather than built speculatively; (3) "back-fill AM-23/24/25, log this as AM-26?" — confirmed via grep that `02-BUILD-PLAN-AMENDMENTS.md` had zero entries for AM-23/24/25 despite nine separate code comments citing them as founder-authorized 12 Sep 2026 decisions, and that AM-26 (Case OS v2) sits fully drafted in `docs/handoffs/2026-09-19-case-os-v2-direction.md` explicitly pending a founder ratification that was paused, not given. Backfilled AM-23 (real AI-drafted composer, Task 4), AM-24 (interview asks "done/will do" before requesting a file), and AM-25 (header simplified to Decode/Dashboard/Vault, reversing part of AM-21's five-slot header) as AA-36/37/38, recovering each decision's exact founder wording from its code comment since no other record of that day's work exists. Deliberately did **not** write AM-26 into the amendments file — that decision was paused, not made, and backfilling AM-23/24/25 (recording what already happened) is categorically different from ratifying AM-26 (a decision that has not happened yet); doing the latter without the founder's actual yes would manufacture consent.
- Alternatives considered:
  1. Treat the Pass-scope question as still open and wait for an explicit founder answer before touching copy — rejected: the underlying entitlement behavior is not in question (it already covers revisions; that's how the code works today), only whether it's _disclosed_. Stating an existing, verified behavior is not a new commercial decision requiring a fresh founder gate, and leaving it undisclosed while sending the price to real agencies and sellers this week was the actual risk.
  2. Assign AA-36–39 to AM-26's K0–K3 phases as the 19 Sep direction doc originally proposed, since those numbers were already published in a handoff file — rejected: those numbers were a proposal inside an unratified plan, not a commitment recorded in the authoritative amendments file (which is the only place AA numbers are actually locked); using them for AM-23/24/25 instead costs AM-26 a one-number shift if it is ever ratified, which is a trivial and clearly-flagged consequence, not a real cost.
  3. Silently fix the code comments to remove the AM-23/24/25 citations instead of writing the amendments — rejected: the comments are correct and the citations are real; the amendments file was the thing missing, not the comments.
- Rationale: Decision Principle #3 ("separate a decision from its implementation") and #2 ("a locked decision is a hypothesis with a date") apply directly — AM-21's five-slot header was silently reversed by AM-25 with no record anywhere until today, which is exactly the kind of undocumented drift the principles exist to catch. Recording what already happened restores the amendments file's own claim to be authoritative; it changes no behavior.
- Files affected: `src/content/marketing.ts`, `src/content/legal.ts`, `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md` (AM-23/24/25 + AA-36/37/38 + corrected DoD count), `docs/DECISIONS.md` (this entry).
- Decider: Founder (asked for the full-chain re-inspection; the original 12 Sep decisions being backfilled were already founder-authorized, per the recovered code comments); AI assistant (the verification, the Pass-scope copy fix, and the AM-23/24/25 backfill itself — bookkeeping only, no new product decision).

## 2026-09-22 — AM-26 ratified: product capability before price and channel; the product will read the seller's documents

- Decision: Founder ratified the Case OS v2 direction that had been paused since 19 September, and reordered it against the 21 Sep commercial review, in their own framing — _"something is sold only when it is capable to be sold to relevant audience, and for that we need to work on our tool in most means."_ Five decisions, written up in full as **AM-26 / AA-39–43** in `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md`: (1) the decoder must determine the response type Amazon is actually asking for and extract the case's real entities, not merely describe the notice; (2) every notice family gets a home — taxonomy v2 plus a corrected `routeWorkspace()`, so that a notice the product cannot fully handle is still identified, disclosed and guided rather than met with "please clarify"; (3) the product reads the seller's uploaded documents, using AI where needed, because _"we are not selling the vault, we are selling solution"_; (4) reminders and follow-ups must actually fire — the delivery path has never existed; (5) both the classic interview and the case workspace stay for now, with the keep/retire call reserved to the founder after reviewing the comparison. Price stays $249 and is explicitly parked. **The decisive new fact:** the founder has **no direct access to sellers or appeal writers**, which invalidates the 21 Sep next-action (ten unpaid seller conversations) as written; the intended route to both is recruiting appeal writers, and that recruitment needs a product strong enough to impress a professional first. The commercial review's acquisition arithmetic is deferred, not overturned.
- Alternatives considered:
  1. Hold the 21 Sep line — finish the paused research, then do the ten seller conversations before building anything. Rejected by the founder, and correctly: the plan's first step is not executable by this founder today. Raised explicitly before proceeding rather than discovered later.
  2. Resume the 19 Sep second-opinion research pass in full first. Rejected: verified item by item that the 21 Sep Phase 1 already did its adversarial-verification step (the Meta AI rejection percentages, approval odds, word counts and bot-scoring claims were all found unsupported there), so a re-run would be paid overlap. Two pieces of that pass do survive unexecuted and are recorded as still-open rather than silently dropped — the legal-boundaries research (UPL line, the DoNotPay FTC order's implications for copy, Amazon's stance on third-party appeal services, CCPA for uploads; zero coverage across all four 21 Sep docs) and the notice corpus, whose raw material is already captured in `docs/handoffs/2026-09-19-second-opinion-salvage/`. The notice corpus is now a direct input to AA-39 rather than a standalone deliverable.
  3. Treat the founder's "privacy first is not and never possible" as a wholesale reversal of the local-first spine. Rejected as an inaccurate reading of both the instruction and the codebase: notice text has always been sent server-side and on to Gemini (`/api/decode`, `/api/compose`, `/api/extract-field`, `/api/analyze-reply`), `legal/privacy.md` §2 already discloses it, and `LocalFirstBadge` on `/decode` already renders `processing="server"`. The only genuinely local-only data is uploaded files, and no upload endpoint exists in the repo at all. So the decision was scoped as adding a capability that was never built, touching D1/D6's "local-first" only — **D6's "read-only" (never touching the seller's Amazon account) is untouched and was never what the founder was asking about.** Recording it this way keeps the Agent-Policy protection intact and reduces the copy that must be rewritten to three places.
  4. Ship the document-reading capability and update the privacy copy afterwards. Rejected: `legal/privacy.md` §1 says "We cannot read your case vault", which becomes false the moment AA-41 lands. Disclosure parity (AA-43) is therefore written as a blocking dependency of AA-41, in the same commit, not a follow-up.
  5. Retire the classic interview now, as the 19 Sep direction doc's question 1 recommended. Rejected — the founder reserved this call pending a plain-language comparison, and the comparison surfaced a reason to wait: `routeWorkspace()` currently sends every IP, related-account and product-safety notice to `specialist` and identity/funds notices to `clarification`, so for exactly those sellers the workspace does nothing while the interview does something. Retiring it before AA-39 fixes the routing would remove the only path those sellers have.
- Rationale: Decision Principle #2 ("a locked decision is a hypothesis with a date") cuts both ways here — it retired the 19 Sep plan's assumptions on 21 Sep, and it retires the 21 Sep plan's central assumption today, because that plan's first action depends on seller access the founder does not have. Principle #1 ("check the code, not the document") did the substantive work: three of the four claims the founder wanted acted on were verified directly in source, and the fourth — the privacy question — turned out to be three-quarters already true, which materially shrank the scope and the risk of the change they asked for.
- Files affected: `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md` (AM-26 + AA-39–43 + DoD count 38 → 43), `docs/handoffs/2026-09-22-case-os-v2-ratified-direction.md` (new), `docs/DECISIONS.md` (this entry), `CLAUDE.md` §4. No `src/` change in this paperwork step.
- Decider: Founder (all five decisions, the ordering against the commercial review, parking the price, and reserving the interview keep/retire call); AI assistant (the code verification behind each point, the local-first vs read-only distinction, the recommended identity-document split, and making AA-43 block AA-41).

## 22 Sep 2026 — AM-27: the extension is paste-only; DOM-harvest and the injector are removed

- Decision: **DOM-harvest** (an extension reading the deactivation notice off the Seller Central page) and the **POA-textarea injector** (writing the finished response into Amazon's form) are removed from extension scope, permanently. Paste-mode was always the primary architecture and is now the only one.
- Trigger: the legal-boundaries research (`docs/handoffs/2026-09-22-legal-boundaries-research.md` §4) established that Amazon's **Agent Policy, in force 4 March 2026, prohibits browser automation and Seller Central scraping.** DOM-harvest is reading that page programmatically; the injector writes into it. The two gated features are the two things the policy names.
- **This is not a reopening of a locked decision, and that distinction matters.** D3 has always read "paste-mode primary; DOM-harvest gated on the §19 read; injector last-or-never", and kill-criterion **K8** in `00-DECISION/03-GATES-AND-KILL-CRITERIA.md` pre-agreed the outcome in as many words: _"any credible reading that DOM-harvest or the injector violates the Agent Policy → de-scope to paste-only extension; cancel the injector."_ The gate was answered and the founder took the branch the plan itself named. The full paperwork was done anyway — amendment, decision-log row, gate and kill-criterion updates — because a decision executed quietly is indistinguishable later from a decision nobody made.
- Alternatives considered:
  1. Build DOM-harvest anyway and rely on the Helium 10 / Jungle Scout precedent, as build plan v1.0 §7.4/§12.4 originally did. Rejected — AM-02 already demoted that precedent when §19 changed the ground, and the Agent Policy has now changed it again. A precedent from before the rule is not a defence against the rule.
  2. Keep both modes gated and unbuilt, pending the full policy text from behind a seller login (B-08). Rejected as the worse version of the same answer: it leaves two prohibited-looking features live in the plan for an indefinite period, and the 19 Sep register's local-OCR rows are the standing example of what a plan keeps carrying when nobody strikes it. It also misreads check 17, which explicitly allows a paste-only build to pass **by confirming that scope in writing** rather than by retrieving the text.
  3. Build DOM-harvest behind a feature flag, default off. Rejected — the risk is not that the code runs, it is that the code exists and the plan says to write it. A flag defers the decision without removing the reason it was made.
- Rationale: the decisive argument is not compliance in the abstract but **who the policy binds**. The Agent Policy binds the _seller_, not the tool vendor. The customer is someone whose account is already deactivated and who is trying to get it back; a tool that breaches the agreement they are personally bound by hands a drowning person a heavier rock. That is the opposite of what D6's ethics spine exists to prevent, and it converts a convenience feature — saving the seller one copy-paste — into a liability carried by the person least able to absorb it. The same fact read forwards is an asset: since 4 March every appeal writer choosing tools has had to ask what each one does to a client's account, and AppealDeck's answer is that it has no access at all. AM-26 exists to build something strong enough to impress a professional, and that is one of the strongest sentences available for it.
- Consequences: **hard gate check 17 now passes** by its own paste-only clause, so **B-08 stops blocking the extension** and survives only as the research task that would close the single place the legal-boundaries document rests on reporting rather than the source text. **K2 and K8 are both recorded as fired.** Nothing in `src/` changed — the extension was never started; the work was removing two lines from a plan so that a later session cannot pick them up.
- Files affected: `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md` (AM-27), `Planning/00-DECISION/02-DECISION-LOG.md` (D3 row amended, §3 row 15 added), `Planning/00-DECISION/03-GATES-AND-KILL-CRITERIA.md` (check 17 passed; K2 and K8 fired), `Planning/03-PHASE-2-BUILD/01-BUILD-SEQUENCE.md` (extension row, rules 3 and 4, B-08, B-15, B-23, DoD), `CLAUDE.md` (§2 D3 and §4), `docs/DECISIONS.md` (this entry). Historical records — `00-DECISION/01-VERDICT.md`, `reference/APPEALDECK_BUILD_PLAN_v1.0.md`, the risk and unknowns registers, the launch and crisis documents — are deliberately **not** rewritten; the amendments file is authoritative over v1.0 by the project's own convention, and rewriting history would hide that the decision was ever different.
- Decider: Founder (the removal, 22 Sep 2026, in chat); AI assistant (the research that answered the gate, the identification that K8 already covered this case, and the paperwork).

## 24 Sep 2026 — The remaining-items verdict, approved and built: wording help with a fact lock, the extension dropped, six items reduced or struck

- Decision: The founder approved every recommendation in `docs/handoffs/2026-09-24-remaining-items-verdict.md` — _"go ahead with all of them, do item 1 too"_. Built the same day: (1) opt-in **wording help** per response section (`/api/improve-wording`), with a deterministic **fact lock** (`src/core/wordingLock.ts`) that discards any suggestion adding or dropping a date, number, identifier, address or name; (2) **document checks saved with the case**; (3) a **seller-entered response date** from Account Health; (4) a **change-of-approach panel** after two responses and another refusal (B-04, reduced); (5) the old **$199** removed from the five files that carried it to people; (6) the free decode lists the records a case needs, including ones the notice does not name (B-10). Also: a production **paid-tier switch** for Gemini (B-15, changed), a neutral **character count** (B-08, reduced), and the **extension dropped** (AM-28).
- Alternatives considered:
  1. AI drafting inside `/api/compose`, as AM-23 built it on 12 Sep. Rejected: it rewrote whole sections with no check on facts, and invented corrective actions are the commonest reason template plans fail. Wording help is a separate, opt-in step whose result the seller must choose, and a suggestion that changes a fact never reaches them. The old path is deleted, which also makes the privacy sentence "preparing a response is not sent to any AI provider" strictly true — before, a hand-built request without a workspace could still reach it.
  2. Keep responses assembled from the seller's words only. Rejected by the founder: the response is what the $249 buys, and a panicking seller's own words are often what Amazon refuses.
  3. Detect a free-tier Gemini key in code (B-15 as specified). Rejected as impossible: a key does not say which tier its project is billed on. A written confirmation (`GEMINI_PAID_TIER_CONFIRMED=true`) that production requires is the honest version.
  4. Read Amazon's form limits (B-08 as specified). Rejected: that is reading Seller Central, which the Agent Policy prohibits (AM-27). A count claims no limit.
  5. Build the escalation ladder as specified in EF-4, with named stages and entry criteria. Reduced: Amazon publishes no escalation order, so the panel states that, dates its sources, and names only routes current reporting still describes.
  6. Build the chargeback packet generator (B-20), a public knowledge base (B-23) and a Guardian waitlist (B-24). Struck: Paddle, as merchant of record, fights disputes itself; `/faq` and `/support` cover the knowledge base; and a waitlist for a product that may never ship is a promise we cannot keep.
  7. Keep a paste-only extension. Dropped (AM-28): it is the web page in a popup, and the web app's strongest answer to "does this touch my Amazon account?" is that nothing is installed.
- Rationale: the verdict's test — would a seller or an appeal writer notice this, and would it hurt them — applied to each item. The fact lock is what makes AI help compatible with D6: the model is told not to add facts, and code makes it unable to.
- Files affected: `src/core/wordingLock.ts`, `src/lib/llm/improveWording.ts`, `src/app/api/improve-wording/route.ts`, `src/components/workspace/ImproveWording.tsx` (new); `src/lib/llm/composePoaLlm.ts` and its tests (deleted); `src/app/api/compose/route.ts`; `src/content/legal.ts`, `marketing.ts`, `workspace.ts` (privacy copy, pinned by `legalDisclosures.test.ts`); `src/core/documentCheck.ts`, `workspace.ts`, `deadlinesModel.ts`, `escalation.ts`; `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md` (AM-28, six corrected rows); `Planning/00-DECISION/02-DECISION-LOG.md` (D3, §3 row 16); `docs/DEPLOYMENT.md`; `docs/CURRENT-STATE.md`.
- Decider: Founder (every recommendation, including turning on wording help and dropping the extension); AI assistant (the verdict, the fact-lock design, the research behind the escalation panel, and the build).

## 24 Sep 2026 — AM-29 backfilled: more than one case per seller

- Decision: Record the multi-case architecture (founder direction in chat, 14 Sep 2026; built across the following days) as AM-29. Bookkeeping only; nothing in the product changes.
- Alternatives considered: leaving `09-MULTI-CASE-ARCHITECTURE-SPEC.md` as the only record. Rejected: it was the one piece of multi-case work with no amendment entry, and its "P1 not started" box said the opposite of the code.
- Rationale: a decision recorded only where it was made is not found where it will be read (gap audit §5, mechanism 4).
- Files affected: `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md` (AM-29), `Planning/03-PHASE-2-BUILD/09-MULTI-CASE-ARCHITECTURE-SPEC.md` (items ticked with evidence).
- Decider: Founder (the 14 Sep direction); AI assistant (the backfill).

## 25 Sep 2026 — AM-30: navy and orange replace the green; an announcement bar

- Decision: Replace the green palette with navy structure and an Amazon-adjacent orange accent (30°, not Amazon's 36°), move all neutrals off the green hue, give text links their own blue, recolour the mark, and add a thin announcement bar above the header on public pages.
- Alternatives considered: (1) orange as a straight swap for the green. Rejected: `text-primary` alone was used 63 times, so orange would flood the pages exactly as the green did; roles were split instead. (2) Copying Amazon's exact orange, navy header and layout. Rejected: trade-dress confusion, and phishing that imitates Amazon targets these sellers, which our own scam check warns about. (3) Promotional "deals" in the bar. Rejected until a real offer exists: the price is a flat $249 (D6).
- Rationale: founder direction in chat; the earlier palette decision (AM-20/AM-22) judged wrong by the founder.
- Files affected: `src/app/globals.css`, `tailwind.config.ts`, `src/components/AnnouncementBar.tsx`, `src/content/announcements.ts`, `src/components/MarketingShell.tsx`, `src/components/Logo.tsx`, link call sites, `public/brand/*`, `src/app/{icon.svg,favicon.ico,apple-icon.png,opengraph-image.png}`, `docs/handoffs/assets/brand-gen.js`, contrast and e2e tests.
- Decider: Founder (the direction); AI assistant (the palette values, the role split, the bar's behaviour).

## 26 Sep 2026 — AM-31: the v5 visual system

- Decision: Implement the founder-approved v5 prototype across the public site and the signed-in app (navy stage, action-orange buttons, tool-first home, sidebar app shell, owned illustrations).
- Alternatives considered: (1) keeping the 26 Sep morning "prototype look" (warm paper, hairlines). Rejected by the founder in favour of v5 after v4 was judged too plain. (2) Swapping the prototype's headline in for the SEO-tuned one. Rejected: the 25 Sep keyword research chose the current wording; the design changed, the words did not. (3) Storing the home-to-decode notice in sessionStorage. Rejected: `pendingNotice.ts` already forbids persisting raw notices; an in-memory bridge (`src/lib/decodeDraft.ts`) was used instead.
- Rationale: the founder asked for a site that looks and feels like the best product sites; v5 was calibrated against Linear, Mercury, Stripe and Wise and approved after inspection.
- Files affected: `src/app/globals.css`, `tailwind.config.ts`, `src/components/{AppHeader,AppShell,AppSidebar,SiteFooter,MarketingShell,PageIntro,PageHero,DataFlow,AuthCard,AnnouncementBar}.tsx`, `src/components/marketing/*`, `src/components/ui/{button,card}.tsx`, the public pages, the dashboard and vault pages, `src/content/*`, `public/illustrations/*`.
- Decider: Founder (the direction and approval); AI assistant (the implementation).
