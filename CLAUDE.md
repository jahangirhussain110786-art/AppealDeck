> **Start here (24 Sep 2026):** [docs/CURRENT-STATE.md](docs/CURRENT-STATE.md) is the register of what a seller can actually do today, where, what proves it, and what still needs founder configuration. The status bullets in §4 below are history; when they disagree with the register, the register is right. Change its row in the same commit as the capability.

> **18 Sep 2026 integrity update:** Current implementation and live migration details are in [docs/handoffs/2026-09-18-integrity-fixes.md](docs/handoffs/2026-09-18-integrity-fixes.md). This supersedes older status claims below: account-ID and per-case entitlements, transactional Paddle processing, isolated guest/account vaults, portable backup restore, strict request validation and corrected CI are now implemented. Supabase migrations 0009 and 0010 were applied; Paddle setup is sandbox-only. No app deployment occurred. Production webhook delivery and confirmation email configuration remain launch checks.

# CLAUDE.md — AppealDeck project memory (read at every session start)

> Auto-loaded context for any coding agent. Keep under ~300 lines. Sections 1–3 and 5 are near-static; only Section 4 churns (update every session per `08-TEAM/03-AI-SESSION-CONTINUITY.md` §3).

## 1. Product one-pager

AppealDeck helps suspended/flagged Amazon sellers by (1) decoding their deactivation/notice for free, (2) drafting an AI-generated Plan of Action (POA — the structured appeal Amazon requires), (3) tracking appeal/funds deadlines, and (4) keeping an encrypted, local-first case vault. Sold as a **$249 one-time "Appeal Pass" per case** (flat, worldwide, no country tiering — 21 Sep 2026 commercial reset, see §4) (Guardian $29/mo + Expert Review deferred — D7). Audience: suspended Amazon sellers in panic. Founder: Jhangir Hussain, solo, Pakistan, non-developer, individual seller (Hawlton = brand/trading name only). Ethics spine IS the strategy: local-first, read-only, no automation, no "guarantee", honest-expectations framing, severity gating (D6).

## 2. Locked decisions D1–D10 (settled — do not reopen)

- **D1** GO, conditional (web-first compliance spine, Phase-0 blockers first, honest no-guarantee positioning, gates enforced).
- **D2** Paddle primary MoR (5% + $0.50, tax-inclusive). Fallback ladder: Polar (warm, verify Pakistan payout via Stripe Connect at signup) → Dodo Payments (plan-C). License keys self-issued via Supabase `licenses` table driven by MoR webhooks.
- **D3** Build order: platform-agnostic TS core (parse→classify→intake→compose→critic) wks 1–4 → web decoder + paid composer live wks 4–5 → MV3 extension wks 4–8, **paste-only**. ~~DOM-harvest gated on BSA §19 read; injector last-or-never~~ — **both removed from scope 22 Sep 2026 (AM-27, founder decision)**: Amazon's Agent Policy, in force 4 Mar 2026, prohibits Seller Central scraping and browser automation, and the policy binds the *seller*, so a tool that breaches it endangers the very account the seller is trying to recover. This is the de-scope branch D3 and kill-criterion K8 always named, not a reopening. Paste-mode was always primary and is now the only mode.
- **D4** Entity: individual (sole proprietor), Jhangir Hussain, Pakistan. Personal payout (Payoneer/Wise personal). Hawlton brand only. Incorporate at ~100 paying users / PKR 5–10M retained profit.
- **D5** Solo founder: no partnership agreement. Written collaborator agreement (IP assignment, non-solicit, credential protocol) BEFORE any contractor/VA/expert gets access. Paid fixture audition for every appeals expert. Consultant retainer ~$1–2k wk 3.
- **D6** Ethics spine (non-negotiable): "guarantee" greps to 0; honest-expectations card before purchase; severity gating (forged-docs/fraud/child-safety routed to pro-help, never sold); read-only always; local-first always; win rates only from opt-in outcome data.
- **D7** Deferred: Guardian $29/mo until monitoring ships; Expert Review until MoR rails resolved; multi-tenant/mobile/non-English → P1/P2; SP-API only after legal clarity.
- **D8** 7-day no-questions voluntary refund; EU-withdrawal-compliant checkout consent (explicit prior consent + permanent-form confirmation email).
- **D9** Cloud LLM: Gemini Flash **paid tier** via own backend only (key never in extension); free tier = dev fixtures only (free-tier data trains Google). Cost ceiling + circuit breaker + per-device rate limits before free tier is public; rules-only degradation floor.
- **D10** North-star: paid Appeal Passes/week. Funnel: decoder session → decode completed → intake started → purchase → (opt-in) outcome. Analytics: Plausible or Umami + backend counts.

These are settled. Do not reopen; deviations require a founder-approved append to `00-DECISION/02-DECISION-LOG.md`.

## 3. FORBIDDEN SOURCES (verbatim from build plan §2.6 — absolute, no exceptions)

Never copy, adapt, paraphrase, or "take inspiration at code level" from:

- `V:\Extension 2.3\spchatgpt\` and `V:\Extension 2.3\spchatgpt-rebranded\` (third-party proprietary — Superpower ChatGPT)
- `V:\Extension 2.3\content\core.js`, `content\mc-managers.js`, `content\spchatgpt-*.js`, `content\chatgpt-managers.bundle.js`, `content\chatgpt-polyfills.js`
- `V:\Extension 2.3\chunked\`, `V:\Extension 2.3\dist\`, `V:\Extension 2.3\Branding\rebrand-*.{js,cjs}`
- Any file whose content mentions "superpower" (grep before committing any copied file).

Everything in the build plan §6 donor table is verified originally-authored / MIT and safe.

## 4. Current state (update every session)

- **Milestone:** M-1 core complete; M-W web surface live; Wave C complete (presentation layer + Wave B closeout). M-4 (full composer) in progress. See `AGENTS.md` "Current Project State" for full detail.
- **DONE this session (2 Sep 2026):** Founder's 1 Sep Meta AI brainstorm fact-checked (two live-web research passes, dated 2 Sep) and digested into the planning layer. NEW `Planning/03-PHASE-2-BUILD/04-EVIDENCE-FIRST-HARDENING.md`: claim-by-claim verdicts, the five POA-tool failure modes (F1–F5), spec EF-1…EF-5 (evidence requirements matrix `src/core/evidenceModel.ts`; action checklist + attestation; deterministic case-readiness rendered ONLY as "case-file completeness — not a prediction"; gap-draft vs full-draft composer; document-type router poa/ip_dispute/funds_appeal; outreach letters; opt-in outcome schema; quarterly policy re-check), plus a binding rejected-suggestions register (SP-API now, auto-submit, $299/mo, approval predictors, model ensembles — locked by D2/D6/D7/D9). `02-BUILD-PLAN-AMENDMENTS.md` gained **AM-16 (AA-19…AA-21)**; `docs/DECISIONS.md` appended. Verified 2 Sep: the 4 Mar 2026 **Agent Policy** is real and in force (automated tools must self-identify; the "§19" placement is single-source — B-08 confirms numbering from the text); funds request window is **+60d** (Meta AI's 90d was stale; our deadlinesModel already correct); inauthentic invoice rules confirmed (≤365d, completed transaction, pro-forma/quotes fail, Amazon phones suppliers); the 2026 AI-appeal-tool wave is all-subscription with zero review footprint; two tool deaths confirmed (appealshub 404, appealpath broken TLS); ecommerceChris de-published pricing → §1.2 anchor re-verify due before any marketing push.
- **ALSO DONE 2 Sep (later session):** Founder direction "OS not writing tool; chat that suggests actions/docs and offers alternatives on refusal" → NEW `Planning/03-PHASE-2-BUILD/05-CASE-OS-SPEC.md` + **AM-17 (AA-22…AA-25)**: case state machine (`caseState.ts`, FUNDS_TRACK + GATED_PRO_HELP), objection-aware actions (predefined consultant-reviewed alternatives; declined ≠ claimed), `responseAnalyzer.ts` (Amazon-reply categories, promoted P1→v1, reply fixtures), and the **Guided Interview** (founder second pass same day: NOT a chat window — a finite step flow, abuse-proof by construction: no persistent input box, typed per-step inputs, deterministic-first routing, NO `/chat` backend route [only decode/extract-field/compose/critique/analyze-reply, schema-constrained], per-case LLM budget on the case key, free tier reaches only `decode` behind a notice-likeness prefilter, fixed off-topic redirect; zero-LLM case-questions drawer replaces "open chat"; generative Q&A parked indefinitely). Panic-hour DO-NOW/DO-NOT triage cards join free decode. No new milestones: core pieces ride M-4, Interview rides M-W; feature freeze after AM-17 until first paid Passes + opt-in outcomes.
- **ALSO DONE 4 Sep 2026:** Founder direction "premium-feel UI/UX before deploy" → NEW `Planning/03-PHASE-2-BUILD/06-PREMIUM-UI-UX-SPEC.md` + **AM-18 (AA-26…AA-28)**: audit of the live UI (§0), premium principles (calm authority; honesty is the aesthetic; product-as-artwork; panic-hour ergonomics), tokens v2 (`next/font`, fluid type, tabular nums, surface tiers, motion/radius/width scales), shadcn primitive gaps (Badge/Skeleton/Alert/Progress/Tabs/Accordion/Dialog-Sheet/Tooltip/Breadcrumb), product patterns (HonestExpectationsCard, DeadlineChip, Stepper, EmptyState, LocalFirstBadge, VerifiedStamp, CopyButton, PoaSection, BeforeYouSubmitChecklist), motion system (MotionConfig reduced-motion, ≤320 ms, no decorative animation), mandatory state quartet, page-by-page reqs (dashboard = caseState home; interview resume + encrypted save-and-exit; `/pricing` Free-vs-Pass table + FAQ + D8 consent; `/decode` sample notice; `/faq`), WCAG 2.2 AA + Lighthouse gates, `defaultTheme` → `system`, one AppHeader. Waves A+B = pre-deploy minimum (~5 days), Wave C with M-W. External agent's 22 items adjudicated in spec §14 (confetti, tour, analytics banner rejected; plaintext-localStorage save replaced by vault persistence; invented founder story never published). Scope guard: presentation only — feature freeze stands. **Later same day:** founder decided **single-host topology for the first deploy** (marketing + auth + app on one origin; `app.` split kept behind env vars — switch checklist in `AGENTS.md` → "Domain topology"); shipped `src/lib/urls.ts`, `src/middleware.ts` single-host mode, shared URLs in auth/billing pages, `.env.example` + `docs/DEPLOYMENT.md` updated. Spec gained §1.1 simplicity budget, §8 topology, §10 content & voice system (trust shown by mechanism, never requested; copy in `src/content/`; **AA-29** copy audit + lint gate; DoD 29).
- **DONE 5 Sep 2026 (this session):** Wave A foundation `67979dc`; Wave B Tasks 0–2 `c8d06de` / `8b22a4c` / `6728aaa`. **UNCOMMITTED (pending):** Wave B Task 3 (`e74c5b4` — home page server component, hero-as-product, how-it-works, expectations strip, founder note section, footer FAQ link); **Wave B marketing surface completion:** `/pricing` rewrite (Free-vs-Pass table + trust card + 7-item FAQ accordion + D8 EU withdrawal consent checkbox + CheckoutButton + sample-POA Dialog + HonestExpectationsCard); legal pages rewrite (`LegalPage.tsx` component with structured `LEGAL` content + sticky TOC + "last updated" stamp); `/faq` new static route (7 Q&As grouped by Pricing/Decoding/Deadlines/Vault/Submitting); `opengraph-image.tsx` (dynamic, brand + honest tagline via `@vercel/og`); `apple-touch-icon.svg` + `public/manifest.webmanifest`; `src/content/marketing.ts` (FAQ content, SAMPLE_POA); `src/content/legal.ts` (LegalDoc/LegalSection types, meta descriptions); `src/content/shared.ts` (tocHeading, lastUpdated, consentPrompt). **Validation:** tsc ✅, lint ✅ (no warnings or errors), lint-copy PASS, format:check ✅ (2 pre-existing warnings only), vitest 244/244 ✅, build ✅ (26 routes + middleware, all marketing pages static).
- **DONE 7 Sep 2026 (Wave C-fix, Tasks 0–6):** Fix prompt `docs/handoffs/2026-09-07-wave-c-fix-prompt.md` Tasks 0–6 complete and committed (`dd76b68`, `7a5c605`, `0c39ffb`/`4719ac6`/`8bbbefd`, `17f319a`, `087d3b1`, `ad95fcb`, `5331820`). Evening audit found the commit labelled task-3 held Task 2 work and `ComposeView.tsx` re-encoded to UTF-16; Task 3 was redone from spec (ComposeView via VaultGate + Alert banner + GLOBAL_EXPECTATIONS; PoaSection code-mapped via `src/lib/findingSections.ts`; checklist on `computeReadiness`), Task 2 leftovers closed (shared ReadinessCard with Progress, labelled kinds, decoder hint, toasts to content), interview fallback prompt reworded (B-16, `490474b`). All gates green: vitest 281/281, build 25 static pages. **Remaining: Tasks 7 (auth surface), 8 (tests/e2e/screenshots), 9 (docs/AA-28/AA-29), 10 optional.** Evidence: `docs/handoffs/2026-09-07-wave-c-fix.md`.
- **DONE 8 Sep 2026:** Founder's 5 Sep Meta AI UI/UX brainstorm (~295 suggestions) grep-verified against `src/` and tiered in `docs/handoffs/2026-09-08-meta-ai-uiux-register.md` (189 rows: MUST/REALLY GOOD/GOOD/NICE/AVOID, status, timing, effort). Net new MUSTs: non-affiliation/trademark disclaimer (absent from `src/` + `legal/`), autosave status + surfaced vault-write failure (`InterviewFlow.tsx` swallows errors), passphrase-unrecoverability disclosure. 13 small "now" items (§5) ≈ 1 day; **draft AM-19 (§6, 10 rows) awaits founder ratification** — nothing appended to spec 06 §14 or `docs/DECISIONS.md` yet.
- **DONE 9 Sep 2026:** Founder asked for a plain-words reading of the 8 Sep register (found it coded and repetitive). Re-verified every "missing" claim at `ed05259` — all still hold. NEW `docs/handoffs/2026-09-09-meta-ai-uiux-plain-guide.md` (six buckets with counts, why the register looked repetitive, do-now / M-4 / later / never lists in plain language, what is already built, the founder's four ticks). NEW `docs/handoffs/2026-09-09-uiux-polish-prompt.md` — coding-agent prompt for the 13 "now" items as Tasks 0–11 (verified line refs, Why/Do/Accept, context-budget block, evidence log `docs/handoffs/2026-09-09-uiux-polish.md`); Task 0 records AM-19 / AA-30 / AA-31 in the planning layer only after the founder ticks the banner; Appendix B parks the M-4 rows (AA-31) so the polish agent does not touch core. **Later the same day the founder ticked all four boxes in chat** (AM-19 ratified, disclaimer wording approved as written, idle-lock = security hygiene, copy items ride AA-29); the reviewing AI performed Task 0: AM-19 + AA-30/AA-31 appended to `02-BUILD-PLAN-AMENDMENTS.md` (DoD → 31 items), spec 06 §14 rows 23–32, `docs/DECISIONS.md` entry, evidence log `docs/handoffs/2026-09-09-uiux-polish.md` opened with baselines (vitest 287/287 in 29 files; build 30 app routes, 10 static, middleware). Committed `bfba421` (9 Sep 2026); the coding AI starts at Task 1. Reusable session-start prompt for the coding AI: `docs/handoffs/SESSION-START-PROMPT.md`.
- **AUDIT 9 Sep 2026 (evening):** the coding AI's polish-pass commits `6b5f3c5` (T1) `46bf6dd` (T2) `9955e73` (T3) `657304e`/`feb59ad` (T4a/b) `461c3b0` (T5) `6319607` (T6) `daa7f82` (T7) `7276675` (T8) `9a33e49` (T9) `a10335d` (T10) audited at `a10335d` → `docs/handoffs/2026-09-09-uiux-polish-audit.md`. Tasks 1–5 PASS (T1 also reworded a Terms §2 sentence without approval — founder box A); T4b's idle-lock warning timer is never cleared (`VaultGate.tsx:63–65`, false flickering warnings after 14 min); T6 PARTIAL (`FileDropZone.tsx` JSX pasted twice, camera button forced visible on desktop; add-toast shows raw keys); T7–T10 FAIL their Accept blocks (T7 a third done, T8 turned spell-check off on the answer box and did nothing else, T9 shipped an unapproved and untrue "will sync" toast from the root layout, T10 not attempted — only VaultView toast strings moved); T11 NOT DONE — the report's `c9d824a` does not exist, `format:check` exits 1 (CRLF working copy of `billing/page.tsx`, blob is LF), "23 routes" was 30 (= baseline). Gates re-run: typecheck · lint · lint:copy (3 passes) · vitest 308/308 in 31 files · build 30 routes green; Playwright 77 passed / 4 failed on 30 s cold-compile timeouts on untouched routes / 30 skipped; targeted re-run 3 passed + 1 flaky, so no e2e regression (detail in the audit §4). NEW `docs/handoffs/2026-09-09-uiux-polish-fix-prompt.md` (Tasks F0–F8: F0 commits the audit docs + repairs the old log; F1 drop zone + helper; F2 idle timers; F3 print; F4/F5/F6 = Tasks 8/9/10 from spec; F7 format gate + timestamps + Terms sentence; F8 = old Task 11) with §0.B reporting rules (hashes only from `git log`, exit codes decide gates, shrunk tasks = NOT DONE); evidence log `docs/handoffs/2026-09-09-uiux-polish-fix.md`; `SESSION-START-PROMPT.md` PATH lines repointed. Audit files left uncommitted for F0.
- **DONE 10 Sep 2026 (AA-30 fix pass complete):** the coding AI's session ended mid-F3 (F0 `cc1fe95`, F1 `34557af`, F2 `cca6f38` committed; F3 half-done and uncommitted, with `ComposeView.tsx`'s `loadCaseFile`/`loadCaseLog` import replaced instead of extended). The reviewing AI finished the pass in place, one prescribed commit per task: F3 `16044b3` (print stylesheet per spec; `.print-only` POA text under a dated header; `data-no-print` on AppHeader, the interview bar and the dashboard reply card), F4 `5ed270a` (spell-check on seller textareas / off on pasted Amazon text and email inputs, `inputMode` email/numeric, `interactiveWidget`, `size="lg"` step-bar primary), F5 `d2e6bf6` (inline offline `Alert` with the approved copy in `AppShell` + `DecodeClient`; root-layout toast and "will sync" copy removed), F6 `aa9cd81` (exclamation-mark lint pass; `(optional)` step marker; "Plan of Action (POA)" on first use on `/dashboard`, `/case`, `/compose`; content README — **and a real find: `scripts/lint-copy.mjs` matched no files on Windows** because `path.relative` returns backslashes, so the soft-list, numbers and colour passes were inert locally until this commit; normalised, still PASS), F7 `12b6a89` (format gate green — `billing/page.tsx` was CRLF in the working copy only; `formatDateTime` on device/vault timestamps; Terms §2 sentence restored because box A stayed unticked), plus two small harness commits `60923f8` (screenshot spec takes `SCREENSHOT_OUT`/`SCREENSHOT_ROUTES`) and `c477255` (axe waits for entry fades — `/forgot-password` failed deterministically against the production build on a mid-fade blended colour `#aaabaf`, not a palette defect), F8 = the docs commit. **Gates at code-final `12b6a89`:** typecheck · lint 0 · lint:copy PASS (5 passes) · format:check exit 0 · vitest 309/309 · build 30 routes / 10 static / middleware 27.1 kB · Playwright 45 passed 0 failed · Lighthouse via direct `lighthouse` runs (`lhci collect` dies on a Windows temp-dir EPERM and then asserts 0 URLs) 1.0 in all four categories on 7 pages; **`/decode` a11y 0.96** from two pre-existing audits (`skip-link`: the bare decode page has no `<main id="main">`; `heading-order`: `EmptyState` renders `<h3>` under the `<h1>`) — both for the visual pass. AA-30 ticked in `02-BUILD-PLAN-AMENDMENTS.md`; evidence + Discovered/Deviations in `docs/handoffs/2026-09-09-uiux-polish-fix.md` (F0's `18da41e` was a dangling pre-amend object → `cc1fe95`; MSYS converts a lone `/` env value into the Git install path; the rendered Terms page says "Last updated 2026-08-25"; no approved gloss for "Seller Central"; two Accept-count mismatches recorded rather than gamed). 12 public screenshots in `docs/handoffs/screenshots/2026-09-10/`; auth-gated captures, the F2 idle-lock check and the fresh-browser loss-warning check **NOT RUN** (no dev credentials; the AI does not type passwords). `SESSION-START-PROMPT.md` repointed at the visual refresh v3 prompt, as that prompt's banner prescribes for F8.
- **DECIDED 10 Sep 2026 (later, in chat) — AM-21 access ladder + continuity; runs BEFORE the visual pass:** founder direction: a visitor must see the whole product in the header (Decode · Case · Dashboard · Vault 🔒 · Pricing|Billing · Sign in/out); the guided interview starts with **no account** and is saved into the vault from the first answer under a **non-extractable browser-generated key** (`KeyMode "device"`); the first document/action step asks for **sign-in** (email + password, Google and magic link all stay, every path carries `?next=`); on return the seller sets the passphrase once and `relockWithPassphrase` re-wraps the same DEK; the composer asks for the **Appeal Pass** with the Paddle overlay, a `checkout.completed` event, a read-only `GET /api/license/status` and an activation state that resumes into the draft; AI field suggestions become free-account under a per-user daily cap (`fixedWindow(20, "1 d")` + breaker); pricing becomes three columns (Free · Free account · Appeal Pass). Rule for every gate: **save first, gate second, resume third.** Disclosed boundary: clearing browser data before sign-in deletes the draft (no server copy by design). NEW `Planning/03-PHASE-2-BUILD/07-ACCESS-AND-CONTINUITY-SPEC.md` (design authority), prompt `docs/handoffs/2026-09-10-access-continuity-prompt.md` (Tasks A0 ✅ paperwork … A8), evidence log `docs/handoffs/2026-09-10-access-continuity.md` (baselines at `7bd3e4e`), plain guide `docs/handoffs/2026-09-10-access-continuity-plain-guide.md`; **AM-21 (AA-33, AA-34)** appended (DoD → 34; AA-32 stays the visual pass's, appended by its V0); `docs/DECISIONS.md` entry; `SESSION-START-PROMPT.md` PATH lines → the access prompt/log. The visual refresh v3 material (prompt, spec, copy deck, evidence log) was updated the same day: start condition = the access pass's `docs(access/task-8)` commit; header/nav keys, signed-out states, three-column pricing and compose-gate copy come from AM-21; its V0 no longer commits the visual deliverables (committed 10 Sep by the reviewing AI).
- **DONE 10 Sep 2026 (AM-21 access + continuity pass, AA-33, code-complete):** Tasks A1–A7 committed (`33e5a16` header, `ed0b4cb` A2 signed-out pages, `6fc49bf` A3 vault device-key mode, `5b10997` A4 client-side interview + gates, `c8c6381` A5 signed-out dashboard + AI-suggestion cap, `fcee50f` A6 purchase continuity, `33bf5b5` A7 pricing/FAQ/privacy). The reviewing AI then audited A1–A4 at the founder's request and found the pass did not actually work end to end: the sign-in gate never blocked progress at the file step, a passphrase relock left a blank screen instead of resuming, `CasePreview` showed the wrong kind for the exact signed-out audience it targets, `?kind=` never auto-started a fresh visitor's interview, a signed-out reload never resumed, and a pre-existing `interviewEngine.ts` bug looped every first-time appellant forever on "No, this is my first appeal." All fixed and tested in `fix(access/task-4-fix)` (`50dbd4c`), including the two required tests A4 had skipped (`e2e/access.spec.ts`, a `CasePreview` render test). Also found and fixed mid-audit: a stray uncommitted, build-breaking partial fix already sitting in the working tree (reconciled, not discarded) and a lingering `.next` dev-cache corruption from running `npm run build` alongside `next dev` (unrelated to the code, cost an hour of false test failures — always stop the dev server before `npm run build`, or vice versa). Full detail, including two harmless Accept-grep/Prettier mismatches, in the evidence log's Discovered section. **Gates at `33bf5b5`:** tsc 0 · lint 0 · lint:copy PASS (5 passes) · format:check 0 · vitest 326/326 in 36 files · build 31 routes (11 static / 20 dynamic) · Playwright `--project=chromium` 52/52. AA-33 ticked in `02-BUILD-PLAN-AMENDMENTS.md`. **Not done this session:** Task A8's screenshot capture (S§11 item 9) and AA-34 (founder's signed-in walk-through + sandbox checkout) — both need a human with real credentials; see the evidence log's "Founder sign-off" section. `master` still unpushed (origin at `048d311`).
- **DONE 10 Sep 2026 (second verification pass on AM-21; AM-20 ratified; visual refresh v3 started):** Founder asked whether the whole access + continuity pass was "fully well implemented"; re-ran every gate fresh and found one test-coverage gap (S§11 item 3 — five header slots, lock only on Vault — had been confirmed correct by code review but had no regression test) and one pre-existing bug worth documenting (the sample-notice text fails the decode API's notice-likeness filter, tracked below, not a defect in this pass). Added the missing test (`7f5dbc6`) and documented the sample-notice bug in the evidence log (`a4d37dd`); the access + continuity pass is now genuinely code-complete and gate-verified with no known gaps besides the two that always needed a human (AA-34, A8 screenshots). Founder then asked to implement the visual refresh v3 pass completely. **AM-20 ratified** (visual system v3 — brand mark, tokens v3, tinted semantics, hero-as-product, shell rebuild); prompt `docs/handoffs/2026-09-09-visual-refresh-prompt.md`, spec `…-visual-refresh-spec.md`, audit `…-visual-audit.md`. Task V0 paperwork done (this bullet, AM-20 appended to `02-BUILD-PLAN-AMENDMENTS.md` between AM-19 and AM-21, `docs/DECISIONS.md` entry, baselines recorded in `docs/handoffs/2026-09-09-visual-refresh.md`: tsc 0, lint 0, lint:copy PASS (5), format:check 0, vitest 326/326, build 31 routes, playwright 53/53 at `a4d37dd`). Starting Task V1.
- **DONE 11 Sep 2026 (visual refresh v3, AA-32, Tasks V0–V11 complete):** V1 `b593398` (tokens v3, Tailwind scales, Inter axes) · V2+V3 `593908e` (brand kit wired; primitives restyled) · badge-contrast fix `13c3b9f` · V4–V6 `6fa862d` (header/footer/MarketingShell/SectionHeading; home hero-as-product; pricing) · V7–V9 `6e041a1` (decodable S§14 sample + shelled decode page; FAQ/legal/auth/system pages; app surfaces) · V10+V11 code `d720bd2` (copy deck applied — 12 FAQ items in 5 groups, expectations card off `/decode` and onto the dashboard; `DevUiGallery.tsx` with the S§7.9 sections + fixture compositions; 42 screenshots in `docs/handoffs/screenshots/2026-09-10-visual/`) · docs commit = `docs(visual-v3/task-11)` in `git log`. **Four defects found by the pass's own gates and fixed in `d720bd2`:** (1) every successful decode on `/decode` crashed into the error boundary — `DeadlineChip` called `getTime()` on the API's ISO-string `dueAt` (pre-existing since before `a4d37dd`, invisible because the old sample never passed the filter and no test asserted a rendered result; now `DeadlineLike` + unit tests + e2e "the sample notice loads and decodes to a result card"); (2) signed-out `/vault` lost its `<h1>` when the locked state became an `EmptyState` (`titleAs` prop); (3) footer `h3` group titles failed Lighthouse `heading-order` on `/decode` (now `h2`, class unchanged); (4) `VerifiedStamp` invisible in dark mode (solid-bg foreground token on a tinted bg → `text-success`). **Gates at `d720bd2`:** tsc 0 · lint 0 · lint:copy PASS (5) · format 0 · vitest 332/332 in 36 files · build 32 routes / 13 static / middleware 26.8 kB · Playwright 96 passed, 0 failed, 30 skipped (auth captures, no credentials) · Lighthouse 1.0 in all four categories on `/`, `/pricing`, `/decode`, `/faq`, `/login` · all §5 greps clean except the width-token grep = 2 accepted exceptions (footer legal strip is spec-literal; a dialog is not a page container). **Open for the founder:** S§11 item 2 — the hero headline renders on 4 lines at 1280 with the spec-literal `max-w-[16ch] text-display` and the copy deck's unchanged 80-character headline (shorten copy / accept 4 lines / approve a smaller display size — evidence log Deviations); sign off the 42 screenshots; auth-gated captures NOT RUN. Windows gotchas recorded in the log: `/dev/ui` returns 404 on `next start` by design (capture the gallery from `next dev`); a bare `npx playwright test` writes PNGs to the screenshots spec's default folder (always pass `SCREENSHOT_OUT` + `MSYS_NO_PATHCONV=1`).
- **DONE 11 Sep 2026 (full-repo audit + fix pass, all founder-approved before building):** A six-part audit read every planning doc, handoff, and branding asset against the real codebase (`docs/handoffs/2026-09-11-full-repo-audit-guidebook.md` — living checklist, all sections now closed except the founder-only items below). Then executed the approved plan across five commits: `1eca47b` (composer critic rules AA-31 — future-tense/blame-shifting/vague-time/document-freshness/unreferenced-evidence checks, all warning/info per "never hard blocks"; the EF-5 opt-in outcome schema AA-21 — `src/core/outcomeModel.ts`, `POST /api/outcome`, `supabase/migrations/0008_outcome_events.sql`, `OutcomeShareCard.tsx` on the dashboard; D10 analytics wiring — `src/lib/analytics.ts`, Plausible script loader, four funnel events; the D8 purchase-confirmation email that was promised on the checkout but never sent — `src/lib/email.ts`, Resend REST API, wired into the Paddle webhook; fixture provenance notes on all 26 fixtures; the ID paste normaliser) · `131dd7b` (privacy/terms/refund brought to real parity with the original drafts — international transfers, data-subject rights + 30-day response, breach notification, retention, a liability cap, severity-gating disclosure; refund timeline honesty fix; Seller Central glossed on first use; consent wording ratified) · `9322ec3` (eight AA checkboxes ticked with evidence, AM-16/AM-17 ratified, `08-WAVE-C-HANDOFF.md` rewritten to remove statements known false since 7 Sep, seven docs corrected off the superseded Cloudflare hosting plan onto the real Vercel single-host architecture, nine stale `V:\AppealDeck\` paths fixed, `AGENTS.md` refreshed from a frozen 8 Sep snapshot, the AI-session-continuity doc rewritten to match actual practice, the two new banned-claim rows added, the 2 Sep competitor recheck merged into the dossier) · `c117826` (`.gitattributes` eol=lf, `.auth.json` gitignored, two stray files deleted) · `be2cfd8` (the first-ever screenshot capture of `/dashboard`, `/case`, `/compose`, `/vault`, `/billing` — using this session's `dev@appealdeck.com` test account, all 66 Playwright checks green). **Gates at `be2cfd8`:** tsc 0 · lint 0 · lint:copy PASS · format:check PASS · vitest 362/362 in 39 files · build 32 routes + middleware. **Genuinely deferred, not forgotten:** Sentry (no decision requires it, founder priority call), the crxjs pin (extension work hasn't started), AA-31's remaining scope (ASIN/case-ID extraction, seller override UI, root-cause step, dated corrective-action UI, character counter — all separate UI/parser features, not critic rules), migration `0008` needs a manual Supabase-dashboard apply before any outcome row can be written (`docs/DEPLOYMENT.md` §4), and the `V:\Extension 2.3\` leaked-file cleanup (outside this repo — deferred to when/if the extension project starts).
- **DECIDED 11 Sep 2026 (later, same day) — visual overhaul v4, plan locked, NOT implemented:** the founder approved a new visual direction (design-canvas mockups iterated across several rounds — font choice fixed, two flagged elements fixed, cross-artboard consistency fixed) and asked for a full real-application visual overhaul "professionally and perfectly," then explicitly said implementation happens in a future session, not this one. Locked into `docs/handoffs/2026-09-11-visual-overhaul-v4-handoff.md`: answers the founder's token-architecture question (yes — `src/app/globals.css` + `tailwind.config.ts` + `src/lib/fonts.ts` is the real single source of truth; the mockup's hardcoded-hex drift was a property of the disconnected `.dc.html` mockup format, not the real codebase), documents every mockup decision (Inter + Newsreader-italic-accent + JetBrains Mono, color palette needing HSL reconciliation against current tokens, radius scale mostly already matching `--radius-xl`, a `.pill`/`.pill-sm`-style Badge size variant, aurora/dot-grid hero background, a layered-custom-SVG illustration approach instead of stock photography, new split-screen interview and annotated-decode layouts, an "obsidian" always-dark vault surface pending a founder confirm on whether it overrides the site theme toggle), and lays out a token-first task order for a fresh session. Approved mockup source (`Main.dc.html`, `Interview.dc.html`, `Decode.dc.html`, `Vault.dc.html`, `canvas.json`) copied into `docs/handoffs/2026-09-11-visual-direction-v2-mockup/` so it survives past this session's temp scratchpad; published reference: https://claude.ai/code/artifact/020b42eb-325e-412d-a378-8838d1917a1e. `SESSION-START-PROMPT.md` repointed at the new handoff. **No code changed for the overhaul itself — this is planning + doc-lock only, per explicit founder instruction.**
- **DONE 11 Sep 2026 (later, same day) — founder-reported access/session bugs fixed; composer diagnosed as the real priority:** Founder reported three bugs directly in chat (not a written handoff): the vault passphrase gate was appearing on Dashboard/Case/Compose when signed in (should be Vault-only, and off by default); a signed-out visitor's draft was surviving closing the tab entirely (should be session-only); signing out then clicking Case/Dashboard still showed the dev account signed in. All three root-caused and fixed, verified live in a browser: Dashboard/Case/Compose/Vault all now pass `deviceMode autoUnlock` to `VaultGate` unconditionally — Compose had never been wired to auto-unlock at all — so **no page ever asks for a passphrase by default**; a new opt-in "Protect with a passphrase" / "Switch to automatic unlock" control lives only on the Vault page (`VaultView.tsx`), backed by a new `Vault.relockWithDeviceKey()` (`src/core/vault/vault.ts` — there was previously no way back from passphrase mode once set); a new `ensureFreshGuestSession` helper (`src/lib/vault/guestSession.ts`) uses a `sessionStorage` flag to wipe a signed-out visitor's leftover vault data on a genuinely new browser session while preserving it across same-tab reloads/navigation; the stale-sign-out bug was Next's client Router Cache replaying an already-rendered signed-in page — fixed with a hard `window.location.assign` in `SignOutButton.tsx` plus `experimental.staleTimes.dynamic = 0` in `next.config.mjs` app-wide. Gates: tsc 0 · lint 0 · lint:copy PASS · format 0 · vitest 366/366 (4 new tests) · build 32 routes. **Known limitation surfaced honestly, not hidden:** case data and vault evidence still share one encrypted store, so if a seller opts into a vault passphrase it does apply to Dashboard/Case too — splitting into two independently-keyed stores is the real fix if that's ever unacceptable, described as an option, not built. **Also this session — the more important finding:** read `src/core/composer.ts` end to end and confirmed the founder's "the generated POA was only 3 lines" complaint precisely: `composePoa()` never reads `CaseFile.rootCause` or `timelineEvents` at all — Root Cause and Preventive Measures are literal bracketed fill-in-the-blank placeholders (`"[Describe what caused the {kind} issue...]"`) regardless of what the seller typed, and there is no LLM call anywhere in `/api/compose`, despite a working Gemini integration (`src/lib/llm/gemini.ts`) wired for other tasks. There is also no interview question at all for preventive measures, and no minimum-substance check on the free-text root-cause answer (any non-empty string, including "idk", is accepted). Full plain-language diagnosis with options: `docs/handoffs/2026-09-11-founder-issues-diagnosis-and-plan.md`. Actionable coding-agent prompt for the fix, written for a fresh AI session with no prior context: `docs/handoffs/2026-09-11-founder-issues-fix-prompt.md` (Tasks 0–3; Task 4 — real AI-drafted composer — is explicitly founder-gated, do not start without the founder saying so in chat). **Not committed by this session** — Task 0 of the fix prompt commits everything from this bullet together. `SESSION-START-PROMPT.md` repointed at the fix prompt.
- **BLOCKERS (founder):** Phase-0 credential rotation (Supabase `fogvzjtxbqgfppdrxqra` — confirmed this repo runs on a different, clean project, but the old leaked files at `V:\Extension 2.3\extraction\` are still on disk and the old project's key rotation is unverified); Paddle/Polar/Wise/CWS/domain accounts; Agent-Policy/BSA text retrieval (B-08); Plausible/Umami account signup (code is wired, `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` just needs a real value) and a Resend account for the confirmation email (`RESEND_API_KEY`). sign off the 42 AA-32 screenshots (`docs/handoffs/screenshots/2026-09-10-visual/`), the 12 AA-30 screenshots (`docs/handoffs/screenshots/2026-09-10/`), and the 66-file auth-gated set (`docs/handoffs/screenshots/2026-09-11-audit/`); do the AA-34 walk-through (sign in → passphrase → resume; sandbox checkout → activation → draft); two calls flagged in the visual-overhaul-v4 handoff for whoever starts that pass — the color-token reconciliation (§2.2) and the vault theme-override decision (§2.7).
- **DONE 11 Sep 2026 (founder-issues fix pass, Tasks 1–3, verified) / VISUAL OVERHAUL STARTS NEXT:** Founder implemented Tasks 1–3 of `docs/handoffs/2026-09-11-founder-issues-fix-prompt.md` directly (Task 0 was moot — the access/session fixes were already committed as `42e6486` before this pass started). This session independently re-ran every gate from a clean checkout (not just trusted the evidence log) and confirms all three are genuinely correct: **Task 1** (`6436aba`) — `composePoa()` now reads `data.rootCause`/`preventiveMeasures` verbatim (with a 40-char + low-effort-phrase `isNarrativeSufficient()` gate instead of accepting "idk"), a new optional `intake_preventive_measures` interview step feeds it, and `composerModeFor()` reports a `gapReason` (`evidence`/`narrative`/`both`) so the gap-draft explains _which_ kind of gap it is — real logic change with matching new tests, not cosmetic. **Task 2** (`ed44480`) — new `PasswordInput` (`src/components/ui/password-input.tsx`) replaces all 11 bare `type="password"` fields (login, signup, reset-password, VaultGate ×5, VaultView protect-dialog ×2) with a show/hide toggle; zero literal `type="password"` left in `src/`. **Task 3** (`e7a3dfb`) — Vault "View" now opens a real preview dialog (image/PDF/text, 200k-char cap, honest "no preview for this type" fallback) instead of a truncated toast; download uses `showSaveFilePicker` with a blob-URL fallback and honest "Download started" vs "Saved {name}" copy. **Gates independently re-run at HEAD (`e7a3dfb`):** tsc 0 · lint 0 · lint:copy PASS · format:check 0 · vitest 373/373 in 39 files · build 32 routes + middleware — all match the evidence log exactly, no discrepancy found. Evidence log `docs/handoffs/2026-09-11-founder-issues-fix.md` filled in and committed. **Only gap left, honestly documented, not a defect:** Task 3's manual browser upload/preview/download check was never run (needs a live vault + real files) — logic is covered by the vault crypto test suite instead. Task 4 (real AI-drafted composer) remains founder-gated, not started. `docs/handoffs/SESSION-START-PROMPT.md` repointed at `docs/handoffs/2026-09-11-visual-overhaul-v4-handoff.md` — a fresh AI session's first job is writing that pass's own task-by-task prompt per the handoff's §3 order.
- **DONE 12 Sep 2026 (AM-22 V0 fix + a scope addition, planning-only, no `src/` touched):** Audited the prior session's AM-22 "V0 paperwork" commit (`533dc84`) against the plan and found five real defects, all fixed and committed as `4e8c3b1`: 6 references to a wrong `DevUIGallery.tsx` path/casing (real file is `src/app/dev/ui/DevUiGallery.tsx` — would have broken later Accept-block greps and risked a stray file on a case-sensitive deploy); a dead V1 Accept-grep (`"220 32% 11%"` never existed in `globals.css`; corrected to the real `"224 32% 11%"`); `docs/DECISIONS.md`'s AM-22 "alternatives considered" was copy-pasted from the unrelated AM-19/AM-20 entries and replaced with the real alternatives; the missing AM-22/**AA-35** section added to `02-BUILD-PLAN-AMENDMENTS.md` (every prior AM-XX got one, this pass's V0 hadn't); a stale "32 routes" figure corrected to the real 33 (pre-existing miscount, not a regression). **Then, founder asked in chat** whether AppealDeck should use real product screenshots the way Apollo/Linear/Stripe do (e.g. login page) and asked for explicit attention to font-size hierarchy — both folded into AM-22 (not separate work): real screenshots approved at concrete moments (login/signup now, home hero once V3 rebuilds it) without reopening §2.6's stock-photo rejection (a different category of image); added **Task V3b** (login/signup rebuild) and a new `Login.dc.html` mockup artboard — a labelled sample-data preview in a browser-chrome frame, since the real UI doesn't carry the new visual language yet to screenshot honestly; audited and reconciled real type-scale drift across all four existing mockups (eyebrow micro-labels alone had drifted to three different sizes/trackings for the same role) into one named scale, documented in handoff §2.9. Mockup canvas republished (the original link was dead this session — "not found") at https://claude.ai/code/artifact/a1305de4-04bf-4dc4-a2f6-658cd62c1337. `docs/DECISIONS.md` gained two entries; handoff gained §2.8/§2.9; `SESSION-START-PROMPT.md` unchanged (still points at the AM-22 prompt/log). **Still nothing in `src/` touched** — planning/mockup only, per the founder's standing "implement in a future session" instruction.
- **DONE 12 Sep 2026 (AM-22 implementation, V1–V4 + V3b, batched per founder direction):** Founder approved implementing the mockup direction ("I approve the super best appearance as we designed into the mockup files") and both founder-gates resolved — V1 color reconciliation adopted in full (warm-neutral, exact hex→HSL, `--primary` unchanged); V6 vault theme **reversed from the handoff's default reading** — founder: "let the vault support the light and dark both variants with the theme... we just selected for entire webapp," so `/vault` will respect `next-themes` like every other page, no permanent obsidian override. Founder also directed batched commits (3-4 tasks/commit, gates once per batch) going forward. Shipped: **V1** tokens (`globals.css` neutrals + radius + `--font-accent`/Newsreader, `tailwind.config.ts`, `AccentWord`). **V2** primitives (`Badge` size variants pixel-exact, `.marketing-surface-aurora`, 3 layered SVG illustrations, `DevUiGallery` additions). **V3** home page (accent word on "today.", aurora background — `HeroArtifact.tsx`'s deadline panel already existed from visual-refresh-v3, not new work). **V3b** (new task, founder direction after asking "should we use real screenshots like Apollo?") — `AuthShell` rebuilt as a split-screen with a framed, honestly-labelled sample-data dashboard preview, answering that real screenshots belong at concrete moments (not stock photos, which stays rejected) but can't be genuine until the real pages carry the new look. **V4** guided interview (reused the existing `Stepper` as the rail instead of building a duplicate `StepRail`; found and fixed a real bug — the mockup's literal 3-column split starved the question card to 168px wide inside `/case`'s existing sidebar layout, verified live in a browser via `getBoundingClientRect()`, not assumed — adapted to 2 columns + a full-width persistent why-we-ask panel). Also found and fixed, unrelated to this pass: a stale `e2e/access.spec.ts` assertion missing a real interview step from an earlier pass, and a `getByLabel` ambiguity in `e2e/marketing.spec.ts` from the `PasswordInput` toggle button. **Gates:** tsc 0 · lint 0 · lint:copy PASS · format:check 0 · vitest 373/373 · build 33 routes (after clearing a stale `.next` — never run `next build` alongside a live `next dev`, confirmed the hard way again) · Playwright 53/54, the one failure confirmed flaky-under-parallelism (passes in isolation, matches this project's own documented pattern). Evidence: `docs/handoffs/2026-09-11-visual-overhaul-v4.md`. Committed `39a09a8` and pushed to `origin/master`, per the founder's direct "tests, commit, push!" instruction.
- **DONE 12 Sep 2026 (AM-22 V5/V6/V8, same session, committed `4bf76b0`):** Founder directed "proceed to V5, V6 and V8" (V7 sweep pass explicitly not requested this round). **V5** decode result rebuilt as a two-column annotated layout: new `src/lib/decodeAnnotations.ts` builds "what this means" cards from phrases the core engine's own `parseNotice()` already flags (stated/ambiguous/legacy appeal window, an inauthentic/could-not-verify phrase, a root-cause structure mention) — every card anchored to a real substring of the seller's own pasted text, never invented (D6); degrades to fewer cards, not filler, when nothing matches. New `AnnotationCard.tsx` + `hl-risk`/`hl-clear` `<mark>` styles (token-first, `--warning`/`--success`). New "Start your Plan of Action" CTA added alongside (not replacing) the existing "Start your case — free" button, since `e2e/access.spec.ts:62` asserts that link by name. **V6** vault surface rebuilt on the standard theme tokens exactly as the founder's reversal specified (confirmed 0 hardcoded obsidian hex existed even before this task): `VaultDoorIllustration` replaces the flat empty-state icon, file rows use the V1 `rounded-row` token, filenames/captions render `font-mono tabular-nums`, and a new short always-visible envelope-caption line was added alongside (not replacing) the existing tooltip. **V8** full gate re-run (tsc/lint/lint:copy/format/vitest 373/373/build 33 routes) plus the **full** Playwright suite this time (54/54, including files the V5/V6 batch's targeted check hadn't covered) · Lighthouse investigated properly rather than trusted at face value: a first run against `next dev` showed perf ~0.68–0.70 (a known dev-server artifact, not a regression) — re-run against a real production build (`next build` + `next start`) confirmed **1·1·1·1 across performance/a11y/best-practices/SEO** on `/`, `/decode`, `/login`, `/pricing`, matching the visual-refresh-v3 baseline exactly · 30 screenshots captured via the existing `e2e/screenshots.spec.ts` harness (not a new script) at `docs/handoffs/screenshots/2026-09-11/` — `/`, `/decode`, `/login`, plus **auth-gated** `/case` and `/vault` using the real dev-account session, at 375/768/1280px × light/dark. Evidence + founder sign-off list: `docs/handoffs/2026-09-11-visual-overhaul-v4.md`. Committed `4bf76b0`, not yet pushed as of this bullet.
- **(Bullets for 13–20 Sep 2026 are not backfilled here** — see the 18 Sep integrity-update banner at the top of this file, `docs/handoffs/2026-09-18-*.md`, `docs/handoffs/2026-09-19-case-os-v2-*.md`, and the memory index for that stretch: composer/vault integrity fixes landed, migrations 0009/0010 applied, and a Case OS v2 direction pass was planned but paused mid-research on 19 Sep.)
- **DONE 21 Sep 2026 (four-phase research, then a commercial-first-principles reset):** Ran the founder-approved four-phase plan end to end — `docs/handoffs/2026-09-21-phase-{1,2,3,4}-*.md` — verifying Amazon-mechanics claims, seller value and willingness-to-pay, the ten-stage journey (found a live IndexedDB-eviction data-loss risk and a half-built prior-submission comparison), and a build/pilot plan. The founder then asked for a review grounded outside the accumulated decision stack, "like a smart business operator." That review (`docs/handoffs/2026-09-21-commercial-review.md`) found paid acquisition is **arithmetically closed** at $199 (legal-vertical CPC ~$9.87, break-even needs 5.2% click-to-purchase vs a realistic 0.5–2%) and that decode-and-draft is now the commoditized part of the product (Amazon's own Seller Assistant guides appeals). Founder decisions: channel staged (organic decoder + a one-week agency-consultant pricing test); next 30 days = 3-day fixes then ten real unpaid seller conversations, not a metrics pilot; pricing delegated to the AI — **implemented at $249 flat, worldwide, no country tiering** (`src/content/app.ts`, `marketing.ts`, `shared.ts`, this file §1). Shipped the same session: `navigator.storage.persist()` on vault unlock + an honest on-page storage-state message (`src/lib/vault/persistence.ts`, `VaultGate.tsx`, `VaultView.tsx`) — the vault is IndexedDB and a waiting case routinely exceeds the ~7-day eviction window with no server copy by design; corrected two stale reference facts that would have produced false marketing copy (AppealsPro's 23% win rate is withdrawn; "Amazon's assistant does not write appeals" is no longer safe) in `01-MARKET-EVIDENCE.md`/`02-COMPETITOR-DOSSIER.md`; tightened the existing "replies" FAQ answer in `marketing.ts` to state only the built capability (visibility into the prior submission, not an automated diff). Confirmed the IP/related-account router exclusions in `workspace.ts` were implementation drift, not a D6 lock (D6 names only forged-docs/fraud/child-safety) — reopening them as evidence + outreach only, no dispute drafting, was decided earlier the same day and does not reopen D6. NEW `docs/DECISION-PRINCIPLES.md` — a standing frame for future calls (check the code not the document; a locked decision is a hypothesis with a date; separate a decision from its implementation; follow the money before the feature; effort spent is not evidence of value; etc.). `docs/DECISIONS.md` appended. **Gates:** tsc 0 · lint 0 · lint:copy PASS · format:check 0 · vitest 563/563 in 62 files (9 new) · build 33 routes. Committed `4bf2691`, pushed to `origin/master`.
- **DONE 21 Sep 2026 (later, same session) — full-chain re-inspection; closed a real disclosure gap; backfilled AM-23/24/25:** Founder asked for a complete re-inspection of the whole chain (the original ChatGPT/Meta AI transcript → the 18 Sep session's response and its seven open questions → this session's four phases and commercial reset) to check whether anything real was left undone. Verified against live code rather than prior summaries and found three of the 18 Sep session's seven questions were never actually answered. **Closed today:** "what does the Pass cover?" — the entitlement code already scopes a licence to one case (not one draft), so revisions are already covered, but this was stated nowhere; added one sentence each to the pricing FAQ (`marketing.ts`) and the Terms licence clause (`legal.ts`) stating plainly that one Appeal Pass covers every revision of its case, no extra charge, no expiry (confirmed `expires_at` is never set at purchase — this is truthfully the current behavior, not a promise). **Backfilled, pure bookkeeping, zero behavior change:** AM-23 (real AI-drafted composer, Task 4, founder-authorized 12 Sep — "whatever it is fix this any way and make sure there we get a real POA, not just text"), AM-24 (interview asks "done/will do" before requesting a file, 12 Sep), AM-25 (header simplified to Decode/Dashboard/Vault, reversing part of AM-21's five-slot header, 12 Sep) — all three were cited in code comments since 12 Sep but never written into `02-BUILD-PLAN-AMENDMENTS.md`; recovered each decision's exact wording from its code comment, since no other record of that day's work survives. **Deliberately left open:** the "split policy + add a VERIFICATION type" question (confirmed `ViolationKind` in `src/core/index.ts` still has no such category) — correctly deferred behind the ten seller conversations rather than built speculatively; and **AM-26 was deliberately NOT written in** — that Case OS v2 ratification was paused by the founder, not given, and backfilling an already-made decision is not the same as manufacturing consent for one that was never made. **Gates:** tsc 0 · lint 0 · lint:copy PASS · format:check 0 · vitest re-run pending this commit · build pending this commit.
- **DONE 22 Sep 2026 (AM-26 ratified — product capability before price and channel; paperwork only, no `src/` touched yet):** Founder ratified the Case OS v2 direction that had been paused since 19 Sep, reordering it against the 21 Sep commercial review: _"something is sold only when it is capable to be sold to relevant audience."_ **The fact that changed the plan: the founder has no direct access to sellers or appeal writers**, so the 21 Sep next-action (ten unpaid seller conversations) is not executable as written; the intended route to both is recruiting appeal writers, and that needs a product strong enough to impress a professional first. The commercial review's acquisition arithmetic is **deferred, not overturned**; price stays $249 and is explicitly parked. **AM-26 / AA-39–43** appended to `02-BUILD-PLAN-AMENDMENTS.md` (DoD → 43): (1) the decoder must decide the response type and extract real entities, not describe; (2) every notice family gets a home — taxonomy v2 + corrected `routeWorkspace()`, since IP/related-account/product-safety currently route to `specialist` (a silent refusal) and identity/funds to `clarification`; (3) **the product reads the seller's uploaded documents** — founder: _"we are not selling the vault, we are selling solution"_; (4) reminders must actually fire — `reminderAt`/`reminderDue` are stored and read, and **no delivery mechanism exists anywhere in the repo** (only job is purchase emails); (5) both journeys stay, keep/retire reserved to the founder after the written comparison. **The privacy scoping matters and was verified, not assumed:** notice text has always gone server-side and on to Gemini (`/api/decode`, `/api/compose`, `/api/extract-field`, `/api/analyze-reply`), `legal/privacy.md` §2 already discloses it, `/decode` already renders `processing="server"` — the only genuinely local-only data is uploaded files, and **no upload endpoint exists at all**. So AM-26 narrows D1/D6's **"local-first"** only; D6's **"read-only"** (never touching the seller's Amazon account, the Agent-Policy protection) is untouched and was never what the founder was asking about. AA-43 (privacy/badge/marketing copy correction) **blocks** AA-41 and must ship in the same commit, because `privacy.md` §1's "We cannot read your case vault" becomes false the moment document reading lands. Direction + the two-journey comparison + verified-code table: `docs/handoffs/2026-09-22-case-os-v2-ratified-direction.md`. The paused 19 Sep second-opinion pass is closed: its verification step was already done by 21 Sep Phase 1, its notice corpus is absorbed into AA-39, and **only the legal-boundaries research remains genuinely open and owned by nobody** (zero coverage in any 21 Sep doc; must land before any outreach making claims about what AppealDeck does).
- **DONE 22 Sep 2026 (contrast, case-switch race, migrations applied):** Semantic colours were tuned as fills and failed WCAG AA wherever they render as small text on their own 5-10% tint (every `Badge`, the workspace `IconTile`): light warning 3.86:1, light primary 4.01:1, light success 4.27:1, dark destructive 3.44:1. Text now resolves to reading-grade `--*-ink` tokens of the same hue via `tailwind.config.ts`'s `textColor` — fills/borders/rings keep the approved AM-22 values exactly — and `src/app/__tests__/contrast.test.ts` computes every pairing from the real stylesheet so a palette change fails in milliseconds naming the pair (`dc9f55d`). The e2e axe call sites were asserting `toEqual([])` with no detail; they now share `e2e/axe.ts`, whose output revealed the long-standing "order-dependent" contrast flake was never a palette defect at all — the specs flip the theme with `emulateMedia` and axe scanned mid-`transition-colors`, comparing the light theme's `#4a5953` against the dark theme's `#0e1613` for 1.08:1. Then fixed a real case-switch race (`ff9b02f`): `DashboardClient` fired `setActiveCaseId` without awaiting it, so a seller who picked a case and navigated in the same breath loaded the one they had just left; the choice is now recorded synchronously in `sessionStorage` before the first await and adopted on the next read. **The first version of that fix was wrong and measurably so** — it cleared the marker at the start of `writeActivePointer`, which deletes the old pointer before adding the new one, so a navigation inside that window left a vault with no pointer and no marker; tracing caught it, and the clear moved to last, with tests pinning both hazards. Founder then applied migrations `0008` and `0011`; both verified by REST probe (columns, RLS anon lockout, CHECK constraints, the unique index, the cron's own query), all probe rows deleted. **Found while verifying: `/api/jobs/case-reminders` was never registered in `vercel.json`**, so AA-40 reminders would never have fired even with the table and the key in place — added at `0 8 * * *` with `maxDuration` 60.
- **METHOD NOTE (earned the hard way, 22 Sep 2026):** never quote a Playwright number from a bare local `npx playwright test`. The config runs `next dev` locally and `next start` only under `CI`, so first-hit routes compile inside the 30s budget and time out at random: the same commit measured **53 passed / 19 failed** locally and **71 passed / 1 failed** under `CI=1 --retries=0` minutes apart. Build first, then `CI=1 npx playwright test --retries=0`; `--retries=0` matters because CI mode's 2 retries relabel a real first-attempt failure as "flaky" and still exit 0. And when something fails, `git stash` and re-run to prove whether it is pre-existing rather than reasoning that a change "can't have affected it" — that reasoning produced two wrong reports to the founder in one session.
- **DONE 22 Sep 2026 (reachability: finished a retirement that was reported complete but was not):** A reachability audit of every core module, component and API route found five things nothing could reach — and four of them were created that morning by the classic-interview retirement (`d9cb847`), which deleted the callers and left the callees. The worst was **`FieldSuggester` + `/api/extract-field`: the AI field-suggestion feature ratified as AM-21/AA-33 was silently dead**, its route still live, Pass-gated and calling Gemini with no UI able to reach it. That regression shipped unnoticed and unreported; this entry is the correction. **AA-33's suggestion half is now deliberately retired, not restored** — the endpoint returned a `low | medium | high | critical` severity rendered as a red "Critical" badge, which is exactly the graded severity the 19 Sep register's item #7 rejected ("no numeric level, no timeline") and which AA-39 replaced with `determineResponseType` plus `clock.ts`'s date-derived urgency bands. Restoring it would have put a weaker LLM classifier on screen able to contradict the decoder about the same notice. The rest of AM-21 (the access ladder, the device-key vault, save-first/gate-second/resume-third) is untouched. Also deleted: `/api/interview`, `JourneyProgress`, `SignInGate`, `AnnotationCard` (superseded earlier when `DecodeClient` inlined its own rendering). **Caught during the cleanup:** `/api/read-document` (AA-41) reuses the extract-field rate limiter, so it is not dead — kept and renamed `rateLimitDocumentRead` / `ratelimit:read-document` to name what it actually guards. **NEW `scripts/lint-reachability.mjs` + `npm run lint:reachability`, wired into CI**, because seven defects in one week were all of one kind — built but unreachable, or unverified — and vigilance had already failed at each of them: it fails the build when a component has no non-test reference, when an API route has no caller in `src/`, and, specifically, **when a `/api/jobs/*` route has no `vercel.json` cron** — the exact shape of the AA-40 bug found earlier the same day. External entry points (the Paddle webhook) need an explicit allowlist entry with a reason.
- **DONE 22 Sep 2026 (#87 scam-suspect check — the first of the three remaining correctness gaps):** A newly deactivated seller is the most phishable person online: frightened, expecting a message from Amazon, and disposed to do what it says. Until now the product decoded a forgery exactly as it decoded a real notice and then helped the seller answer it — pointing a panicking person at a criminal, which is the worst thing this product could do. NEW `src/core/noticeAuthenticity.ts` (pure, deterministic, no model decides whether someone is being defrauded): flags a reinstatement/appeal fee or gift-card/crypto/wire demand, a request for a password or one-time code, a move to WhatsApp/Telegram/a personal number, links whose host is not Amazon, and reply addresses that are not `@amazon.*` — each quoting the seller's own words. **It reaches no verdict, by design and by test:** authenticity cannot be settled from pasted text (a real notice forwarded through a mail client gets its links rewritten; a good forgery is word-perfect), so it names what is worth checking and sends the seller to the one place that settles it — their own Account Health page. A test asserts the output never contains "is a scam", "fraudulent", "is genuine" or "is legitimate"; another asserts **every fixture in the real notice corpus stays silent**, because crying wolf on a genuine deactivation would delay an appeal inside a short window. Wired into `/api/decode` and rendered above the response-type decision on `/decode` (if the message is forged, nothing below it matters), and recomputed as the seller types in the workspace's own notice field, since a notice can be pasted there without ever visiting `/decode`. Browser-verified both ways: the forged notice raises all four signals, the genuine sample raises none. e2e asserts the wiring end to end — the one feature where being silently unwired would do harm rather than merely lose value.
- **DONE 22 Sep 2026 (#91 and #86 — the last two correctness gaps from the 19 Sep register):** **#91**, attempts made before AppealDeck: a seller usually arrives after appealing once or twice alone and being refused, and the product counted only what it had recorded itself, so a third attempt was treated as a first. Every rule that depends on the number inherited the mistake — `noveltyRequired()` never fired, the AA-42 duplicate-submission guard had nothing to compare against though repeat-submission-without-change is the best-evidenced rejection cause in the research, and the composer wrote as a first appeal. Prior attempts now go into the **same `submissions[]` array** (marked `source: "prior"`, `revision: 0`), so the attempt count, the novelty guard, the pre-submit checklist and the history all became correct at once instead of each being taught about a second list; verified by finding that all seven call sites already read `submissions`. The only place this was ever asked was the classic interview's "Prior appeals" step, so **retiring the interview on 22 Sep silently removed it — a third casualty of `d9cb847`, alongside `FieldSuggester` and the orphans, and one a caller-graph cannot see because `priorAppealCount` was still referenced by its Zod schema while being read by nothing.** **#86**, multi-issue notices: `noticeParser` already pushed every matching kind into `kindHints[]`, and `classifyStage1` then walked a priority list and returned the first, discarding the rest — so a two-violation notice built a plan for one and the seller never learned the other was unanswered. NEW `src/core/noticeIssues.ts` keeps them all with the sentence that raised each, reading the *same* exported `KIND_PATTERNS` and `KIND_PRIORITY` rather than a third copy; `workspaceGaps` refuses to call a response ready while an issue is unconfirmed. **`POLICY` is suppressed when a more specific kind already claimed the same sentence** — "detail page policy violation" is one issue, not two, and a feature that cries wolf about the count is worth nothing; the suppression is deliberately narrow, so two genuinely different issues in one sentence are still two. **Three schema/commit traps caught while building, each the same silent-drop class as the unregistered cron:** `workspaceSchema` rejected `revision: 0` and would have stripped both `source` and `issues` (Zod drops unknown keys), and `RequestReview`'s `onSave` is a field-enumerating route-confirmation commit that does `submissions: old.submissions`, so a recorded prior attempt was discarded on the way to the vault until it was given `onCommitWorkspace`. Browser-verified both features end to end; e2e covers both.
- **DONE 22 Sep 2026 (legal-boundaries research — the last open item from the paused 19 Sep pass):** `docs/handoffs/2026-09-22-legal-boundaries-research.md`, sourced and dated, plain-words summary first, explicitly not legal advice. **UPL:** writing an Amazon appeal is not practising law, because an Amazon appeal is a private contractual dispute rather than a court or administrative proceeding — but the BSA's escalation path ends in AAA arbitration, and a non-attorney cannot file an arbitration demand, appear as counsel, or claim privilege, so the product should say where it stops and that nothing here is privileged. **FTC v. DoNotPay** (order 16 Jan 2025, announced 11 Feb, $193k): the standard is *substantiation*, not sincerity — never claim equivalence to a lawyer/consultant/appeal writer, and never claim efficacy without opt-in outcome data with a denominator. Checked: no lawyer-equivalence claim exists anywhere in `src/content`, and `lint-copy` already bans win rates and bare percentages. **Amazon's Agent Policy (in force 4 Mar 2026) prohibits browser automation and Seller Central scraping** — AppealDeck never touches Amazon Services at all, so it is not an "Agent" by construction, which is both a compliance answer and the strongest single line available for appeal-writer outreach; **but D3's planned extension DOM-harvest and injector modes are what the policy describes, and should be struck.** **GDPR applies with no size threshold** if EU sellers are served, and "flat, worldwide" pricing is targeting them — comply or decline is a founder decision. **CCPA/CPRA does not apply** at this scale ($26,625,000 / 100k consumers / 50% revenue), though government IDs are "sensitive personal information" and the product now reads them. **Three verified defects in the current product:** "not legal advice" exists in `legal/terms.md` and `legal/withdrawal-consent.md` but the rendered `/terms` page comes from `src/content/legal.ts`, where the string "advice" appears **zero times** — the founder's own disclaimer renders nowhere; the privacy policy still describes the AI field suggestions and interview drafting flow deleted earlier the same day; and the vague "depends on the provider's terms" line can now be specific, because Google's paid tier verifiably does not use prompts to improve its products and retains abuse logs for 55 days. **Legal copy deliberately not edited — that is a founder call**; proposed wording and a ranked list are in §7.
- **DONE 22 Sep 2026 (the three legal-copy defects fixed, and gated):** (1) **"Not legal advice" now renders.** It had been written in `legal/terms.md` in August, that file's own banner claimed the rendered copy was at "substantive parity" with it, and the sentence was never carried into `src/content/legal.ts` — so it existed in the repo and on no page a seller could read. The rendered Terms now says AppealDeck is software, not a law firm and not a substitute for one, that nothing there is legal advice, and that no claim is made about performing a lawyer's, consultant's or professional appeal writer's work — the claim the FTC's DoNotPay order turns on. The stale parity claim in `legal/terms.md` is corrected in place rather than quietly fixed. (2) **The privacy policy no longer describes deleted features** — the optional AI field suggestions and the guided-interview drafting flow, both removed the same morning, plus a stray "interview drafts" in the pre-sign-in paragraph. (3) **The vague "handle request data under their own retention terms" is replaced with the verified specifics**: Google states it does not use prompts or responses to improve its products on the paid tier, and logs them for up to 55 days solely to detect abuse — the second stated because a seller cannot weigh a retention window we decline to name. `terms` last-updated moved to 2026-09-22. **NEW `src/content/__tests__/legalDisclosures.test.ts`** asserts each of these against the *rendered* source, because the original defect was invisible for weeks precisely because nobody diffed the draft against the live copy; verified it fails 5 of 9 against the old text. Both pages checked in a browser. **§7 item 2 then done the same day:** a new Terms section, *"Where this service stops"*, states that AppealDeck helps answer Amazon through Amazon's own channels and that is the whole of it; that it does not help with arbitration, demand letters, pre-arbitration notices or court filings, and that a case reaching that point needs a lawyer; and that **nothing a seller tells it is legally privileged** — the point nobody would think to ask about, and the one a seller could otherwise discover far too late. Pinned by two more assertions in `legalDisclosures.test.ts` and verified rendering, in the page's table of contents, in a browser.
- **DONE 22 Sep 2026 (AM-27 — the extension is paste-only):** Founder removed **DOM-harvest** (reading the notice off the Seller Central page) and the **POA-textarea injector** from extension scope, permanently. Trigger: Amazon's Agent Policy, in force 4 Mar 2026, prohibits browser automation and Seller Central scraping — DOM-harvest is reading that page programmatically and the injector writes into it. **Not a reopening of a locked decision:** D3 already gated DOM-harvest on the §19 read and made the injector "last-or-never", and `00-DECISION/03-GATES-AND-KILL-CRITERIA.md` **K8** pre-agreed this exact outcome ("de-scope to paste-only extension; cancel the injector"); the founder took that branch, so the prescribed paperwork was followed rather than an edit made quietly. The decisive argument is that **the Agent Policy binds the seller, not the tool** — a deactivated seller using a tool that breaches their own agreement is handed a heavier rock, which is the opposite of this product's purpose. **Nothing in `src/` changed; the extension was never started.** The point of the removal is that a future session cannot pick the modes up, the way the 19 Sep register kept carrying local OCR after the founder had replaced it. **Two consequences:** hard gate **check 17 now PASSES by its own paste-only clause** ("a paste-only build with no DOM-reading features may pass this check by confirming that scope in writing" — AM-27 is that confirmation), so **B-08 no longer blocks the extension** and survives only as the research item that would close the one gap in the legal-boundaries doc; and the no-access position becomes an outreach asset, since every appeal writer choosing tools since March has had to ask what each does to a client's account, and AppealDeck's answer is that it has none. Recorded in `02-BUILD-PLAN-AMENDMENTS.md` (AM-27), `00-DECISION/02-DECISION-LOG.md` (D3 row amended + §3 row 15), `00-DECISION/03-GATES-AND-KILL-CRITERIA.md` (check 17 passed, K2 and K8 fired), `03-PHASE-2-BUILD/01-BUILD-SEQUENCE.md` (B-08, B-15, B-23, M-6, DoD) and `docs/DECISIONS.md`. Historical documents (`01-VERDICT.md`, the v1.0 build plan, the risk register) are deliberately **not** rewritten — the amendments file is authoritative over them by the project's own convention.
- **DONE 22 Sep 2026 (planned-vs-built gap audit — finding only, no classification, no code changed):** Founder asked for every planning, handoff and decision document to be read line by line against the codebase, and every non-implemented or missed item listed in a new file, so that a second AI round can classify each one against the current mindset. All 69 files under `Planning/`, all 66 under `docs/`, `CLAUDE.md`, `AGENTS.md` and `legal/*.md` — 18,366 lines — read and grep-verified at `c6bb9a0`. Register: `docs/handoffs/2026-09-22-planned-vs-built-gap-audit.md`, with stable IDs (A-01…A-13 · B-01…B-27 · C-01…C-09 · D-01…D-11) and, in §8, the four questions the classification pass must answer per ID. **The headline is that 13 features are built, tested, ticked off as delivered, and unreachable by any seller** — mostly casualties of retiring the classic interview (`d9cb847`), which was the right decision whose consequences were never swept up: the attestation layer (AA-19), the objection/alternatives model (AA-22), the action-check step (AA-37), the three EF-4 outreach letters, `whyAmazonWantsIt`, `idNormalize.ts`, and `draftStrength.ts` — the last built specifically from the founder's own 12 Sep complaint that a thin draft showed as "Full draft", so that complaint is still live in the product. **`scripts/lint-reachability.mjs` could not see any of it**, because a reference from the dev-only `/dev/ui` gallery counts as a real reference and `src/core`/`src/lib` modules are not checked at all; the two scans that found all thirteen are in the register's §7 and belong in the script. Also found: the analytics event names diverge from the spec's deliberately-verbatim list and `checkout_opened` is missing (cheap now, unfixable later without losing comparability); **no database backup exists at all** — the nightly `supabase db dump` GitHub Action was never built; there is **no `superpower` CI grep gate** although forbidden sources is one of the three absolutes in §3 of this file; and `$199` survives in **171 places across 54 files** (register §4a — *never* find-and-replace it: roughly half are competitors' $199/mo tiers or true history, but `docs/DEPLOYMENT.md:82` sets up the Paddle product at the wrong price and `legal/withdrawal-consent.md:13` contradicts the consent card a buyer is actually shown). **757 unticked checkboxes exist across the planning layer and the number carries no information** — work that shipped sits unticked, and features nobody can reach sit ticked; §3 lists nine of each direction. Nothing was classified, struck or built in this pass, by instruction.
- **DONE 23 Sep 2026 (the classification round, then wave 1 batch 1):** The 22 Sep register listed 63 gaps and deliberately reached no verdict, because the founder asked for finding first and classification second. This session did the second pass: `docs/handoffs/2026-09-23-gap-classification.md` gives every ID a disposition under AM-26's lens — not "was it promised?" but **"would an experienced appeal writer, shown this product cold, notice its absence?"** I re-verified before classifying rather than trusting the register, and every spot-check held. **14 items are struck outright, each with its reason written down**, because the register's own warning is that an unwritten strike means the next session finds the code again. The five rebuilds in Category A are **one coherent feature** — *what does this case need, have you done it, can you prove it, and what if you can't* — not five errands, and they belong on the workspace rather than back in the retired interview. Committed `9a422b7` with the two lines the register said to fix first regardless: `docs/DEPLOYMENT.md:82` set up the Paddle product at **$199** (following it would have charged sellers the wrong price on day one) and `legal/withdrawal-consent.md:13` claimed the consent card says $199 when it says $249 — the same defect shape as the missing "not legal advice" sentence found on 22 Sep, so it is corrected in place with a dated note. **Then wave 1 batch 1 (`326ef22`, pushed), chosen on one rule — none of it deletes anything, so none waits on the founder agreeing with a strike:** **A-07**, the draft-strength signal, built and unreachable since 12 Sep while the founder's own "3-line draft shown as Full draft" complaint stayed live in the product, now renders in `ResponseReview` with the tone mapping extracted so "every level has a tone and copy" is a test, plus a test that none of the copy mentions Amazon, approval, likelihood or odds; **A-08 reclassified on contact with the code** — there is no ID input field anywhere in `src/`, so `normalizePastedId` is deferred to B-06, but the hazard underneath it is real and was measured: a zero-width space pasted inside an ASIN makes `entities.ts`'s `\bB0[A-Z0-9]{8}\b` match **nothing**, the ASIN vanishes with no error, and nothing sanitised notice text — new `stripInvisibleChars` runs where the notice is *stored*, never inside the extractor, because `entities.ts` guarantees `raw.slice(start,end) === value` and sanitising after the spans are computed would slide every offset (browser-verified: one character dropped, the decode result now shows the ASIN it used to lose); **B-12**, the funnel events renamed to the spec's verbatim strings with `checkout_opened` added at the point the Paddle overlay actually opens — and **`intake_started` was found defined since 11 Sep and fired from nowhere**, so funnel step 3 would always have been empty; **B-14**, the `superpower` forbidden-sources rule — one of the three absolutes in §3 — is finally a CI gate (`scripts/lint-forbidden-sources.mjs`), and it failed on its own doc comment on the first run, recorded in the script rather than worked around; plus **D-09**'s stale `V:\AppealDeck` path. **Gates:** tsc 0 · lint 0 · lint:copy PASS · lint:sources PASS (new) · lint:reachability PASS · format 0 · vitest **797/797 in 73 files** · build 30 static pages · Playwright `CI=1 --retries=0` **73 passed / 0 failed / 3 skipped** (baseline 71/1/3; the known cross-file flake did not reproduce).
- **DONE 23 Sep 2026 (the three strikes, then B-03 — both founder-directed the same day):** **Strikes (`c592f7a`).** `interviewEngine.ts` could not simply be removed, which is why it survived the 22 Sep retirement: `CaseFile` and `createCaseFile()` were never part of the interview and twenty-odd modules read them, so they move to `src/core/caseFile.ts` — what they always were — and the step engine around them goes. **This closes AA-40's open classic-interview keep/retire question**, which the founder answered in substance on 22 Sep by retiring the surface. `alternativesFor()` went with it rather than being parked uncalled in another module; the A-02 rebuild recovers it from `git show 37d606b:src/core/interviewEngine.ts`, recorded in both `caseFile.ts` and the classification doc, because keeping an unreachable copy "to be safe" would have recreated the exact disease this pass cures. Three of that file's 29 tests never tested the interview — they test the case record — so they moved to `caseFile.test.ts`. `poaClipboard.ts` was verified superseded before deleting (`ResponseReview` has its own `CopyButton` path); `motion.ts` was a dead re-export claiming to be the home of a system that lives in `providers.tsx`; `Stepper` now owns its own progress shape. **B-03, the reply delta (`17d20d8`)** — the largest unbuilt item in the register. Until today `applyWorkspaceReply()` reset **every** requirement to `"needed"`, so a seller who had read their invoice, recorded what it supports and linked it to the case lost all of it the moment Amazon wrote back, and again the next round; the median real case is multi-round, so the product destroyed the most work exactly where it promised to save the most, on the path an appeal writer would try first. Now four outcomes, each meaning one thing: **reopened** (you marked it reviewed and Amazon asks again — the only honest reading of "conflict", so it returns to needed quoting Amazon's sentence), **added**, **outstanding** (Amazon declining to repeat a request does not withdraw it), **carried** (reviewed, unmentioned, **kept** — the work that used to be destroyed). Matched by label because `proposedRequirements()` issues a fresh id per call while the existing requirement carries the seller's real work; vault record, filename, hash, page and note ride through untouched on all four, including a reopen. The delta renders **before** the revision starts and is recomputed from the authoritative workspace on apply, so a stale preview can never be what gets written. One existing test asserted the old reset on a line contradicting its own name and now asserts what the name always claimed. Verified end to end by a new unauthenticated e2e test and walked in a browser. **Found while checking the test count and fixed, because it made every total this project has quoted wrong:** `workspaceExport.test.ts` imported `documentWorkspace` from `workspace.test.ts`, and importing one test file from another makes Vitest collect the imported file's suites **inside the importing file too** — all 36 tests in `workspace.test.ts` were running twice. Nothing failed, which is why it survived; adding 7 tests moved the reported total by 14, which is how it surfaced. Fixture moved to `workspace.fixture.ts`; **the honest total is 747, not 769 with 29 duplicates inside it.** No other test file imports another (checked). **Gates:** tsc 0 · lint 0 · lint:copy PASS · lint:sources PASS · lint:reachability PASS · format 0 · **vitest 747/747 in 72 files** · build clean · Playwright `CI=1 --retries=0` **74 passed / 0 failed / 3 skipped**.
- **DONE 23 Sep 2026 (the workspace evidence surface — A-01, A-02, A-03, A-05, A-06, `0bbff36`):** Built as **one** surface, because *what does this case need, why do they want it, how do I get it, and what if I can't* is one question. New `src/core/requirementGuidance.ts` is the bridge; `RequirementGuidance.tsx` is what a seller sees. **A-02/A-03 is the one that mattered:** a requirement could only be "here it is" or "I'm waiting", so a seller who genuinely could not obtain a compliant invoice — the most common dead end in this product — had no way to say so and `workspaceGaps` told them forever to "review and link evidence". `Requirement` gains `cannot_obtain`, carrying the reason in the seller's own words and which predefined alternative they chose (`alternativesFor` recovered from the deleted `interviewEngine.ts`); the gap becomes a **named** gap, the draft stays a working draft because the evidence really is missing, and the response states it **under its own heading** — a reader must never mistake a declared gap for a supplied record. **A-01 had two separate reasons for never firing, neither visible from reading the code:** it read `actionItems`, which only the retired interview could mark done; rewired to the workspace's corrective actions it *still* did not fire, because `critiquePoa`'s workspace branch returns before reaching it — and since 22 Sep that branch is every case. The attestation clears on edit, because one that survives a rewrite is an attestation to text the seller never read. **The finding this pass turned up: `EVIDENCE_MATRIX.UNKNOWN` is an empty array, and a case started by typing a notice straight into `/case` is `UNKNOWN` — so the entire evidence matrix, EF-1's central asset, was invisible on the most ordinary path into the product.** Found by the e2e test, not by reading. Now falls back to the record's canonical description, scoped deliberately to unclassified cases only: when a case *is* classified and its matrix does not name the record, silence is honest, because another violation's sentence would put a wrong reason on screen (the identity-document sentence talks about funds release, nonsense on a verification case). The label→`EvidenceKind` pairing lives beside the only code that produces those labels, same reasoning as `noticeIssues.ts` reading `KIND_PATTERNS`. **Not B-05** — raising a requirement Amazon did not spell out is still unbuilt. Both new fields went into `workspaceSchema.ts` in the same edit as the model, since that validator strips what it does not know and has silently dropped two fields here already. **Gates:** tsc 0 · lint 0 · lint:copy PASS · lint:sources PASS · lint:reachability PASS · format 0 · **vitest 765/765 in 73 files** (18 new, arithmetic exact) · build clean · Playwright `CI=1 --retries=0` **76 passed / 0 failed / 3 skipped**.
- **DONE 23 Sep 2026 (wave 1 closed — A-13, B-22, B-13, `bc12d7e`):** **B-22** — there was no support page, address or stated response window anywhere in `src/`, and the footer's "Support" link pointed at the privacy policy's contact anchor, which is how it survived a casual look. `/support` names the operator, states **two business days**, and says what support *cannot* do (no Amazon login, no prediction of the outcome, no document verification, and **it cannot extend an Amazon deadline — do not wait on a reply here while yours runs out**). Those limits are the page, not a disclaimer bolted to it. **Two founder items in `docs/DEPLOYMENT.md` §6a:** create the `support@appealdeck.com` mailbox, and change the window if two business days is not what can actually be held to. **B-13** — licences, entitlements, outcome events and case reminders had **no backup of any kind**; losing the licence table means every paying customer loses access to the case they bought with no record on this side of what they are owed. Nightly encrypted dump as a GitHub Action: it **refuses an empty dump**, **proves the file decrypts** before uploading (a backup nobody has opened is not a backup), and **fails loudly when the secrets are absent** rather than reporting success while writing nothing — that quiet success is the defect this repo has hit several times. Flags verified against the installed Supabase CLI; **a GitHub Action cannot be run from here**, so the first real run is a founder step. **A-13, and it earned its keep immediately** — the gate now ignores dev-gallery references, checks `src/core`/`src/lib` for importers, and **does not count a mention inside a comment**; running it found two more orphans: **`EvidenceSlotPanel`** (only live mention was another file's comment explaining why it was superseded; also the sole place the EF-4 letters ever rendered — **whoever builds B-05 should read it first: `git show 8b0e4b3:src/components/EvidenceSlotPanel.tsx`**) and **`SignOutButton`** (orphaned when sign-out moved into the profile menu). Both deleted. Four components stay gallery-only and now need a written reason (`Stepper`, two illustrations, `kbd`, `separator`). **Gates:** tsc 0 · lint 0 · lint:copy PASS · lint:sources PASS · lint:reachability PASS · format 0 · vitest 765/765 in 73 files · build clean, `/support` static · Playwright `CI=1 --retries=0` **78 passed / 0 failed / 3 skipped**.
- **DONE 23 Sep 2026 (wave 2 closed — B-01, B-02, B-05, B-06; `92558b8` + `71b5fac`):** **B-01** — taxonomy v2's four families had **zero** fixtures, so `core.test.ts`'s per-fixture classification assertion held nothing honest for four of ten families. Sixteen fixtures added from the 19 Sep salvage research's recorded notice wording, all synthetic. **They found a real gap on the first run:** a gated-category notice ("restricted to qualified sellers") matched nothing and fell to `UNKNOWN` — the "please clarify" dead end AA-39 exists to remove. **B-02** — reply fixtures, ≥2 per analyser category plus the adversarial case the spec names by hand, and **it caught exactly what it was built to catch**: a refusal quoting the seller's own plan back ("You wrote: '…your account is now active'… We do not have enough information") was reported as **reinstated**, because the analyser returned the first matching rule and stopped. Telling a deactivated seller they are back is the worst thing this code can do — they stop answering and the window closes. A refusal now beats reinstatement language and the result reports `ambiguous` rather than false certainty; the override is deliberately narrow. **B-05, the union** — `proposedRequirements` never consulted `evidenceModel.ts`, so a record Amazon did not spell out was never raised, and Amazon routinely does not spell it out. The list is now the notice's own ∪ the matrix's **required** records, each carrying `source`; an inferred record is shown as **ours** in plain words rather than a blockquote, and `workspaceGaps` no longer demands a notice quote for it (which would have shown a seller a fault in their own text that was actually ours). **B-06, the override** — the decoded kind was set once and read-only forever; K12 pre-agreed it and no mechanism was ever built. It stopped being cosmetic the moment B-05 landed, since the kind now decides which unspoken records are raised on top of the guidance and the severity gate. The correction is **additive by construction**: nothing a seller has already reviewed is removed for telling us we read the notice wrong. **Two process notes:** the reachability gate caught my own new fixture file and a *third* laundering route with it — `fixtures.ts` has no product consumer and passed only because `core/index.ts` re-exports it, now named in the exemption; and **both e2e tests from the workspace surface had a real race** (reload/navigate before the vault write landed), found by reading the failure rather than calling it flaky, fixed with a save-confirmation wait and two consecutive clean full runs. **Gates:** tsc 0 · lint 0 · lint:copy PASS · lint:sources PASS · lint:reachability PASS · format 0 · **vitest 781/781 in 74 files** · build clean · Playwright `CI=1 --retries=0` **79 passed / 0 failed / 3 skipped**.
- **DONE 23–24 Sep 2026 (integration-defect passes, pushed):** Two audits (a parallel ChatGPT audit and mine) found the same class of defect: features that work alone and break where steps meet. Every claim was checked against the code before being fixed. Commits `a63f54a`…`1b3ace9` fixed the agreed order: taxonomy split `INAUTHENTIC` / `INAUTHENTIC_DOCUMENTS`, typed notices classified, deadlines counted only from a date the notice itself states, and stored click-counted dates repaired. Then **`e2ea152`**: a 3 MB upload limit (Vercel refuses request bodies over 4.5 MB); the clock, waiting-on card and reminder switch now render for workspace cases; negation and past tense no longer raise records; submission records keep the text actually sent and what was unresolved; per-question questionnaire answers; a corrected notice rebuilds the evidence list and keeps work already done; lint-reachability checks that every vercel.json function and cron points at a real route; DEPLOYMENT.md says Vercel Pro, not Hobby. **Gates at `e2ea152`:** vitest 978/978 in 80 files · Playwright chromium `CI=1 --retries=0` 86/0/3. **Founder calls left open:** the Next.js 15/16 upgrade (14 is out of support); document checks per case or account-wide; whether to typecheck test files (`tsconfig.json` excludes `*.test.ts`, which hid two signature breaks). Detail: memory `integration-defects-2026-09-23`.
- **DONE 24 Sep 2026 (the rest of the ChatGPT audit, then the framework — founder: "do all of this remaining"):** `5779b7d` document checks (model quotes, code compares against today, the notice's ASINs/IDs and the seller's stated business details; new "Not checked" status; "Found" needs a quote), `Workspace.caseFacts` + the facts ledger listing document-versus-case disagreements, two evidence records given real kinds, document checks per case, privacy copy matched to the code, reminder delivery status, DEPLOYMENT §6a/§6b/§9 launch checks. `84c14bb` test files typechecked (72 errors; one real — the scam-check corpus test had tested `undefined` since it was written). `045ec24` **Next.js 16.3.6 + React 19 + Node 24 + ESLint 9** (`src/proxy.ts`, async `cookies()`, flat config; four React Compiler lint rules off with the reason — a separate pass), and the three defects that had kept **CI red since at least 23 Sep unnoticed**: `requireUser` lost the return path whenever `NEXT_PUBLIC_APP_URL` was unset (as DEPLOYMENT says to leave it), `/reset-password` stranded visitors without an auth backend, `/faq` accessibility 0.98. Then `docs/CURRENT-STATE.md` (the register) and `src/core/evaluationSet.test.ts` (ChatGPT §12: eleven representative cases end to end; the twelfth, guest-to-account, is `e2e/integrity.spec.ts`) — which on its first run found a routine "complaint about the authenticity of ASIN…" classified UNKNOWN; the pattern now covers Amazon's usual wording, whole notice corpus still clean. **Still the founder's:** AI drafting for workspace responses (reverses a privacy sentence, D9 cost), and every "Needs config" row in the register.
- **DONE 24 Sep 2026 (the remaining-items verdict, then all of it — founder: "go ahead with all of them, do item 1 too"):** `docs/handoffs/2026-09-24-remaining-items-verdict.md` judged every open gap-audit item against the code and found three the reports missed. Built: **opt-in wording help** per response section (`/api/improve-wording`) behind a deterministic **fact lock** (`src/core/wordingLock.ts` discards a suggestion that adds or drops a date, number, identifier, address or name), with the old whole-section AI path deleted from `/api/compose` so preparing a response calls no model; **document checks saved with the case** (dated, tied to the file hash, marked stale when the case details change, exported); a **seller-entered response date** from Account Health; a **change-of-approach panel** after two responses and another refusal, with dated sources; the decode listing the records a case needs; **$199 removed** from the live manifest, the sandbox script (which created a $199 price) and the outreach drafts; a production `GEMINI_PAID_TIER_CONFIRMED` switch (B-15); a character count (B-08); analytics tests. **Extension dropped (AM-28)**; multi-case work recorded (AM-29); stale ticks, prices, Cloudflare and superseded spec sections corrected in place. **Gates:** typecheck 0 · lint 0 · lint:copy/reachability/sources PASS · format 0 · vitest 1109/1109 in 87 files · Playwright chromium `CI=1 --retries=0` 109/0/3. Pushed `9534106`; GitHub CI green (build, e2e, Lighthouse). **Still the founder's:** the EU call, the accounts in `docs/CURRENT-STATE.md`, setting `GEMINI_PAID_TIER_CONFIRMED=true` after DEPLOYMENT §6b, the two backup secrets + one restore drill, and rerunning `scripts/setup-paddle-sandbox.mjs` so `NEXT_PUBLIC_PADDLE_PRICE_APPEAL_PASS` points at a $249 sandbox price.
- **DONE 24 Sep 2026 (a ChatGPT functional audit, finished here after its session ran out):** Ten seam defects fixed, found by ChatGPT and each checked here against the code: clearing a saved field brought the old text back after reload (an empty string was treated as "no override"); questionnaire drafts were keyed by question position, so changing the form moved an answer under a different question (now keyed by question text, and old drafts are migrated on load); the draft limit was 20 fields for a 50-question schema; confirming issue coverage threw away unfinished response text; the corrective-action attestation tick came back after the actions changed; saving the response replaced the whole workspace instead of merging its own fields; exports left out questionnaire answers; unfinished drafts were sent to `/api/compose`; the wording lock let a dropped "not" or planning language through, and its disclosure claimed more than regex checks can establish (both fixed); a suggestion could be accepted while its parent was saving. ChatGPT's session stopped while tracing a "document check lost after reload" failure. **That was a test defect.** `journey.spec.ts` waited for `/^Checked /`, which matches an image's immediate "Checked on this device." line and not only the dated line that appears after the save, so the reload raced the write. The product only shows the dated line once the write has landed. Debug logging and the scratch spec were removed; the regressions are in `e2e/response-continuity.spec.ts`. Write-up: `docs/audits/2026-09-24-functional-audit.md`. **Gates:** typecheck/lint/lint:copy/reachability/sources/format all 0 · vitest 1116/1116 in 87 files · build clean · Playwright chromium `CI=1 --retries=0` 116/0/0.
- **NEXT ACTIONS (23 Sep 2026, superseding the stale list below):** (1) ~~**Waves 1 and 2**~~ — **both closed**. Remaining is **wave 3** (B-04 the escalation ladder · B-07 deadline provenance · B-10 the free-decode evidence hook · B-11's surviving parts · B-08's replacement under AM-27 · B-15 · Category C's bookkeeping in one commit · Category D including the $199 triage) and the deferred list with named gates in the classification doc §6. Formerly: **A-13 last** (add the gap audit's §7 scans to `lint-reachability.mjs`; it must land last or it fails the build with the remaining findings, and it must not flag `*.fixture.ts`), plus **B-22** (a support surface — there is no support page, address or response window anywhere in `src/`, and a professional asks who is behind a one-person tool before anything else) and **B-13** (nightly `supabase db dump`; licences, entitlements, outcome events and reminders have **no backup of any kind**). (2) **Rest of wave 2, now that B-03 is in:** B-02 (reply fixtures including the adversarial one), B-05 (+A-05 — `proposedRequirements()` never consults `evidenceModel.ts`, so a requirement Amazon did not spell out is never raised, and inferring it is the expertise being sold), B-01 (the four taxonomy-v2 families have zero fixtures, so classifier accuracy is unmeasured for 40% of families), B-06 (seller override — a tool an expert cannot correct is a tool an expert cannot use). (3) Still founder-owned and unblocked: `RESEND_API_KEY`, `NEXT_PUBLIC_PLAUSIBLE_DOMAIN`, `CRON_SECRET`, the EU comply-or-decline decision, and the two GCP projects behind AA-11/B-15.
- **SUPERSEDED (kept for the reasoning) — NEXT 4 ACTIONS:** (1) Build **AA-39 (K0 kernel)** — response-type determination, entity extraction with spans, taxonomy v2, and `routeWorkspace()` corrected so no notice family is left homeless; notice-family ground truth comes from `docs/handoffs/2026-09-19-second-opinion-salvage/salvage-research_amazon-mechanics.md`. Everything else in AM-26 depends on it. (2) Then AA-40 (reminder delivery + "since you were here"), AA-41+AA-43 together (document reading + the disclosure correction, one commit), AA-42 (evidence pack, Seller Challenge counter, verification checklists). (3) ~~Founder applies the outstanding migrations~~ — **done 22 Sep 2026: `0008` and `0011` are applied and verified against the live project; migrations `0001`-`0011` are now all in.** (4) Once Plausible/Umami and Resend accounts exist, add `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` and `RESEND_API_KEY` to Vercel env — analytics is inert without the first, and **AA-40's email reminders need the second**, which is now the only thing standing between a stored reminder and an email actually arriving. Two founder calls ride the build and neither blocks starting: classic-interview keep/retire (AA-40) and the identity-document processing split (AA-41). The one-week agency/consultant pricing test (`docs/handoffs/2026-09-21-agency-outreach-and-seller-script.md`) stays available but is now downstream of a stronger product, per the founder's 22 Sep ordering.

## 5. Key file links

> Repo layout: the app lives at repo root (`src/`, `package.json`, `legal/`, `docs/`); all playbook/planning docs live in `Planning/`; `.agents/` + `.claude/` skills stay at root.

- Build plan (canonical): `Planning/03-PHASE-2-BUILD/reference/APPEALDECK_BUILD_PLAN_v1.0.md`
- Amendments (authoritative over v1.0): `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md`
- Build sequence (order): `Planning/03-PHASE-2-BUILD/01-BUILD-SEQUENCE.md`
- Risk controls: `Planning/03-PHASE-2-BUILD/03-TECHNICAL-RISK-CONTROLS.md`
- Premium UI/UX spec (AM-18): `Planning/03-PHASE-2-BUILD/06-PREMIUM-UI-UX-SPEC.md`
- Decisions log + gates: `Planning/00-DECISION/`
- Team / continuity: `Planning/08-TEAM/`
- Reasoning ledger: `docs/DECISIONS.md`
- Coding-AI session-start prompt (founder pastes it into every new coding session; two PATH lines point at the current pass): `docs/handoffs/SESSION-START-PROMPT.md`
- Milestone/session handoffs: `docs/handoffs/` (4 Sep 2026 handoff prompt for the coding agent: `docs/handoffs/2026-09-04-premium-ui-ux-and-single-host.md`; 2 Sep 2026 session log: `docs/handoffs/2026-09-02-external-ai-review-and-case-os.md`; archived 2 Sep research: `Planning/03-PHASE-2-BUILD/reference/2026-09-02-*.md`)
