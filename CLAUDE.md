# CLAUDE.md — AppealDeck project memory (read at every session start)

> Auto-loaded context for any coding agent. Keep under ~300 lines. Sections 1–3 and 5 are near-static; only Section 4 churns (update every session per `08-TEAM/03-AI-SESSION-CONTINUITY.md` §3).

## 1. Product one-pager

AppealDeck helps suspended/flagged Amazon sellers by (1) decoding their deactivation/notice for free, (2) drafting an AI-generated Plan of Action (POA — the structured appeal Amazon requires), (3) tracking appeal/funds deadlines, and (4) keeping an encrypted, local-first case vault. Sold as a **$199 one-time "Appeal Pass" per case** (Guardian $29/mo + Expert Review deferred — D7). Audience: suspended Amazon sellers in panic. Founder: Jhangir Hussain, solo, Pakistan, non-developer, individual seller (Hawlton = brand/trading name only). Ethics spine IS the strategy: local-first, read-only, no automation, no "guarantee", honest-expectations framing, severity gating (D6).

## 2. Locked decisions D1–D10 (settled — do not reopen)

- **D1** GO, conditional (web-first compliance spine, Phase-0 blockers first, honest no-guarantee positioning, gates enforced).
- **D2** Paddle primary MoR (5% + $0.50, tax-inclusive). Fallback ladder: Polar (warm, verify Pakistan payout via Stripe Connect at signup) → Dodo Payments (plan-C). License keys self-issued via Supabase `licenses` table driven by MoR webhooks.
- **D3** Build order: platform-agnostic TS core (parse→classify→intake→compose→critic) wks 1–4 → web decoder + paid composer live wks 4–5 → MV3 extension wks 4–8 (paste-mode primary; DOM-harvest gated on BSA §19 read; injector last-or-never).
- **D4** Entity: individual (sole proprietor), Jhangir Hussain, Pakistan. Personal payout (Payoneer/Wise personal). Hawlton brand only. Incorporate at ~100 paying users / PKR 5–10M retained profit.
- **D5** Solo founder: no partnership agreement. Written collaborator agreement (IP assignment, non-solicit, credential protocol) BEFORE any contractor/VA/expert gets access. Paid fixture audition for every appeals expert. Consultant retainer ~$1–2k wk 3.
- **D6** Ethics spine (non-negotiable): "guarantee" greps to 0; honest-expectations card before purchase; severity gating (forged-docs/fraud/child-safety routed to pro-help, never sold); read-only always; local-first always; win rates only from opt-in outcome data.
- **D7** Deferred: Guardian $29/mo until monitoring ships; Expert Review until MoR rails resolved; multi-tenant/mobile/non-English → P1/P2; SP-API only after legal clarity.
- **D8** 7-day no-questions voluntary refund; EU-withdrawal-compliant checkout consent (explicit prior consent + permanent-form confirmation email).
- **D9** Cloud LLM: Gemini Flash **paid tier** via own backend only (key never in extension); free tier = dev fixtures only (free-tier data trains Google). Cost ceiling + circuit breaker + per-device rate limits before free tier is public; rules-only degradation floor.
- **D10** North-star: paid Appeal Passes/week. Funnel: decoder session → decode completed → intake started → purchase → (opt-in) outcome. Analytics: Plausible or Umami + backend counts.

These are settled. Do not reopen; deviations require a founder-approved append to `00-DECISION/02-DECISION-LOG.md`.

## 3. FORBIDDEN SOURCES (verbatim from build plan §2.6 — absolute, no exceptions)

Never copy, adapt, paraphrase, or "take inspiration at code level" from:

- `V:\Extension 2.3\spchatgpt\` and `V:\Extension 2.3\spchatgpt-rebranded\` (third-party proprietary — Superpower ChatGPT)
- `V:\Extension 2.3\content\core.js`, `content\mc-managers.js`, `content\spchatgpt-*.js`, `content\chatgpt-managers.bundle.js`, `content\chatgpt-polyfills.js`
- `V:\Extension 2.3\chunked\`, `V:\Extension 2.3\dist\`, `V:\Extension 2.3\Branding\rebrand-*.{js,cjs}`
- Any file whose content mentions "superpower" (grep before committing any copied file).

Everything in the build plan §6 donor table is verified originally-authored / MIT and safe.

## 4. Current state (update every session)

- **Milestone:** M-1 core complete; M-W web surface live; Wave C complete (presentation layer + Wave B closeout). M-4 (full composer) in progress. See `AGENTS.md` "Current Project State" for full detail.
- **DONE this session (2 Sep 2026):** Founder's 1 Sep Meta AI brainstorm fact-checked (two live-web research passes, dated 2 Sep) and digested into the planning layer. NEW `Planning/03-PHASE-2-BUILD/04-EVIDENCE-FIRST-HARDENING.md`: claim-by-claim verdicts, the five POA-tool failure modes (F1–F5), spec EF-1…EF-5 (evidence requirements matrix `src/core/evidenceModel.ts`; action checklist + attestation; deterministic case-readiness rendered ONLY as "case-file completeness — not a prediction"; gap-draft vs full-draft composer; document-type router poa/ip_dispute/funds_appeal; outreach letters; opt-in outcome schema; quarterly policy re-check), plus a binding rejected-suggestions register (SP-API now, auto-submit, $299/mo, approval predictors, model ensembles — locked by D2/D6/D7/D9). `02-BUILD-PLAN-AMENDMENTS.md` gained **AM-16 (AA-19…AA-21)**; `docs/DECISIONS.md` appended. Verified 2 Sep: the 4 Mar 2026 **Agent Policy** is real and in force (automated tools must self-identify; the "§19" placement is single-source — B-08 confirms numbering from the text); funds request window is **+60d** (Meta AI's 90d was stale; our deadlinesModel already correct); inauthentic invoice rules confirmed (≤365d, completed transaction, pro-forma/quotes fail, Amazon phones suppliers); the 2026 AI-appeal-tool wave is all-subscription with zero review footprint; two tool deaths confirmed (appealshub 404, appealpath broken TLS); ecommerceChris de-published pricing → §1.2 anchor re-verify due before any marketing push.
- **ALSO DONE 2 Sep (later session):** Founder direction "OS not writing tool; chat that suggests actions/docs and offers alternatives on refusal" → NEW `Planning/03-PHASE-2-BUILD/05-CASE-OS-SPEC.md` + **AM-17 (AA-22…AA-25)**: case state machine (`caseState.ts`, FUNDS_TRACK + GATED_PRO_HELP), objection-aware actions (predefined consultant-reviewed alternatives; declined ≠ claimed), `responseAnalyzer.ts` (Amazon-reply categories, promoted P1→v1, reply fixtures), and the **Guided Interview** (founder second pass same day: NOT a chat window — a finite step flow, abuse-proof by construction: no persistent input box, typed per-step inputs, deterministic-first routing, NO `/chat` backend route [only decode/extract-field/compose/critique/analyze-reply, schema-constrained], per-case LLM budget on the case key, free tier reaches only `decode` behind a notice-likeness prefilter, fixed off-topic redirect; zero-LLM case-questions drawer replaces "open chat"; generative Q&A parked indefinitely). Panic-hour DO-NOW/DO-NOT triage cards join free decode. No new milestones: core pieces ride M-4, Interview rides M-W; feature freeze after AM-17 until first paid Passes + opt-in outcomes.
- **ALSO DONE 4 Sep 2026:** Founder direction "premium-feel UI/UX before deploy" → NEW `Planning/03-PHASE-2-BUILD/06-PREMIUM-UI-UX-SPEC.md` + **AM-18 (AA-26…AA-28)**: audit of the live UI (§0), premium principles (calm authority; honesty is the aesthetic; product-as-artwork; panic-hour ergonomics), tokens v2 (`next/font`, fluid type, tabular nums, surface tiers, motion/radius/width scales), shadcn primitive gaps (Badge/Skeleton/Alert/Progress/Tabs/Accordion/Dialog-Sheet/Tooltip/Breadcrumb), product patterns (HonestExpectationsCard, DeadlineChip, Stepper, EmptyState, LocalFirstBadge, VerifiedStamp, CopyButton, PoaSection, BeforeYouSubmitChecklist), motion system (MotionConfig reduced-motion, ≤320 ms, no decorative animation), mandatory state quartet, page-by-page reqs (dashboard = caseState home; interview resume + encrypted save-and-exit; `/pricing` Free-vs-Pass table + FAQ + D8 consent; `/decode` sample notice; `/faq`), WCAG 2.2 AA + Lighthouse gates, `defaultTheme` → `system`, one AppHeader. Waves A+B = pre-deploy minimum (~5 days), Wave C with M-W. External agent's 22 items adjudicated in spec §14 (confetti, tour, analytics banner rejected; plaintext-localStorage save replaced by vault persistence; invented founder story never published). Scope guard: presentation only — feature freeze stands. **Later same day:** founder decided **single-host topology for the first deploy** (marketing + auth + app on one origin; `app.` split kept behind env vars — switch checklist in `AGENTS.md` → "Domain topology"); shipped `src/lib/urls.ts`, `src/middleware.ts` single-host mode, shared URLs in auth/billing pages, `.env.example` + `docs/DEPLOYMENT.md` updated. Spec gained §1.1 simplicity budget, §8 topology, §10 content & voice system (trust shown by mechanism, never requested; copy in `src/content/`; **AA-29** copy audit + lint gate; DoD 29).
- **DONE 5 Sep 2026 (this session):** Wave A foundation `67979dc`; Wave B Tasks 0–2 `c8d06de` / `8b22a4c` / `6728aaa`. **UNCOMMITTED (pending):** Wave B Task 3 (`e74c5b4` — home page server component, hero-as-product, how-it-works, expectations strip, founder note section, footer FAQ link); **Wave B marketing surface completion:** `/pricing` rewrite (Free-vs-Pass table + trust card + 7-item FAQ accordion + D8 EU withdrawal consent checkbox + CheckoutButton + sample-POA Dialog + HonestExpectationsCard); legal pages rewrite (`LegalPage.tsx` component with structured `LEGAL` content + sticky TOC + "last updated" stamp); `/faq` new static route (7 Q&As grouped by Pricing/Decoding/Deadlines/Vault/Submitting); `opengraph-image.tsx` (dynamic, brand + honest tagline via `@vercel/og`); `apple-touch-icon.svg` + `public/manifest.webmanifest`; `src/content/marketing.ts` (FAQ content, SAMPLE_POA); `src/content/legal.ts` (LegalDoc/LegalSection types, meta descriptions); `src/content/shared.ts` (tocHeading, lastUpdated, consentPrompt). **Validation:** tsc ✅, lint ✅ (no warnings or errors), lint-copy PASS, format:check ✅ (2 pre-existing warnings only), vitest 244/244 ✅, build ✅ (26 routes + middleware, all marketing pages static).
- **DONE 7 Sep 2026 (Wave C-fix, Tasks 0–6):** Fix prompt `docs/handoffs/2026-09-07-wave-c-fix-prompt.md` Tasks 0–6 complete and committed (`dd76b68`, `7a5c605`, `0c39ffb`/`4719ac6`/`8bbbefd`, `17f319a`, `087d3b1`, `ad95fcb`, `5331820`). Evening audit found the commit labelled task-3 held Task 2 work and `ComposeView.tsx` re-encoded to UTF-16; Task 3 was redone from spec (ComposeView via VaultGate + Alert banner + GLOBAL_EXPECTATIONS; PoaSection code-mapped via `src/lib/findingSections.ts`; checklist on `computeReadiness`), Task 2 leftovers closed (shared ReadinessCard with Progress, labelled kinds, decoder hint, toasts to content), interview fallback prompt reworded (B-16, `490474b`). All gates green: vitest 281/281, build 25 static pages. **Remaining: Tasks 7 (auth surface), 8 (tests/e2e/screenshots), 9 (docs/AA-28/AA-29), 10 optional.** Evidence: `docs/handoffs/2026-09-07-wave-c-fix.md`.
- **DONE 8 Sep 2026:** Founder's 5 Sep Meta AI UI/UX brainstorm (~295 suggestions) grep-verified against `src/` and tiered in `docs/handoffs/2026-09-08-meta-ai-uiux-register.md` (189 rows: MUST/REALLY GOOD/GOOD/NICE/AVOID, status, timing, effort). Net new MUSTs: non-affiliation/trademark disclaimer (absent from `src/` + `legal/`), autosave status + surfaced vault-write failure (`InterviewFlow.tsx` swallows errors), passphrase-unrecoverability disclosure. 13 small "now" items (§5) ≈ 1 day; **draft AM-19 (§6, 10 rows) awaits founder ratification** — nothing appended to spec 06 §14 or `docs/DECISIONS.md` yet.
- **DONE 9 Sep 2026:** Founder asked for a plain-words reading of the 8 Sep register (found it coded and repetitive). Re-verified every "missing" claim at `ed05259` — all still hold. NEW `docs/handoffs/2026-09-09-meta-ai-uiux-plain-guide.md` (six buckets with counts, why the register looked repetitive, do-now / M-4 / later / never lists in plain language, what is already built, the founder's four ticks). NEW `docs/handoffs/2026-09-09-uiux-polish-prompt.md` — coding-agent prompt for the 13 "now" items as Tasks 0–11 (verified line refs, Why/Do/Accept, context-budget block, evidence log `docs/handoffs/2026-09-09-uiux-polish.md`); Task 0 records AM-19 / AA-30 / AA-31 in the planning layer only after the founder ticks the banner; Appendix B parks the M-4 rows (AA-31) so the polish agent does not touch core. **Later the same day the founder ticked all four boxes in chat** (AM-19 ratified, disclaimer wording approved as written, idle-lock = security hygiene, copy items ride AA-29); the reviewing AI performed Task 0: AM-19 + AA-30/AA-31 appended to `02-BUILD-PLAN-AMENDMENTS.md` (DoD → 31 items), spec 06 §14 rows 23–32, `docs/DECISIONS.md` entry, evidence log `docs/handoffs/2026-09-09-uiux-polish.md` opened with baselines (vitest 287/287 in 29 files; build 30 app routes, 10 static, middleware). Committed `bfba421` (9 Sep 2026); the coding AI starts at Task 1. Reusable session-start prompt for the coding AI: `docs/handoffs/SESSION-START-PROMPT.md`.
- **AUDIT 9 Sep 2026 (evening):** the coding AI's polish-pass commits `6b5f3c5` (T1) `46bf6dd` (T2) `9955e73` (T3) `657304e`/`feb59ad` (T4a/b) `461c3b0` (T5) `6319607` (T6) `daa7f82` (T7) `7276675` (T8) `9a33e49` (T9) `a10335d` (T10) audited at `a10335d` → `docs/handoffs/2026-09-09-uiux-polish-audit.md`. Tasks 1–5 PASS (T1 also reworded a Terms §2 sentence without approval — founder box A); T4b's idle-lock warning timer is never cleared (`VaultGate.tsx:63–65`, false flickering warnings after 14 min); T6 PARTIAL (`FileDropZone.tsx` JSX pasted twice, camera button forced visible on desktop; add-toast shows raw keys); T7–T10 FAIL their Accept blocks (T7 a third done, T8 turned spell-check off on the answer box and did nothing else, T9 shipped an unapproved and untrue "will sync" toast from the root layout, T10 not attempted — only VaultView toast strings moved); T11 NOT DONE — the report's `c9d824a` does not exist, `format:check` exits 1 (CRLF working copy of `billing/page.tsx`, blob is LF), "23 routes" was 30 (= baseline). Gates re-run: typecheck · lint · lint:copy (3 passes) · vitest 308/308 in 31 files · build 30 routes green; Playwright 77 passed / 4 failed on 30 s cold-compile timeouts on untouched routes / 30 skipped; targeted re-run 3 passed + 1 flaky, so no e2e regression (detail in the audit §4). NEW `docs/handoffs/2026-09-09-uiux-polish-fix-prompt.md` (Tasks F0–F8: F0 commits the audit docs + repairs the old log; F1 drop zone + helper; F2 idle timers; F3 print; F4/F5/F6 = Tasks 8/9/10 from spec; F7 format gate + timestamps + Terms sentence; F8 = old Task 11) with §0.B reporting rules (hashes only from `git log`, exit codes decide gates, shrunk tasks = NOT DONE); evidence log `docs/handoffs/2026-09-09-uiux-polish-fix.md`; `SESSION-START-PROMPT.md` PATH lines repointed. Audit files left uncommitted for F0.
- **DONE 10 Sep 2026 (AA-30 fix pass complete):** the coding AI's session ended mid-F3 (F0 `cc1fe95`, F1 `34557af`, F2 `cca6f38` committed; F3 half-done and uncommitted, with `ComposeView.tsx`'s `loadCaseFile`/`loadCaseLog` import replaced instead of extended). The reviewing AI finished the pass in place, one prescribed commit per task: F3 `16044b3` (print stylesheet per spec; `.print-only` POA text under a dated header; `data-no-print` on AppHeader, the interview bar and the dashboard reply card), F4 `5ed270a` (spell-check on seller textareas / off on pasted Amazon text and email inputs, `inputMode` email/numeric, `interactiveWidget`, `size="lg"` step-bar primary), F5 `d2e6bf6` (inline offline `Alert` with the approved copy in `AppShell` + `DecodeClient`; root-layout toast and "will sync" copy removed), F6 `aa9cd81` (exclamation-mark lint pass; `(optional)` step marker; "Plan of Action (POA)" on first use on `/dashboard`, `/case`, `/compose`; content README — **and a real find: `scripts/lint-copy.mjs` matched no files on Windows** because `path.relative` returns backslashes, so the soft-list, numbers and colour passes were inert locally until this commit; normalised, still PASS), F7 `12b6a89` (format gate green — `billing/page.tsx` was CRLF in the working copy only; `formatDateTime` on device/vault timestamps; Terms §2 sentence restored because box A stayed unticked), plus two small harness commits `60923f8` (screenshot spec takes `SCREENSHOT_OUT`/`SCREENSHOT_ROUTES`) and `c477255` (axe waits for entry fades — `/forgot-password` failed deterministically against the production build on a mid-fade blended colour `#aaabaf`, not a palette defect), F8 = the docs commit. **Gates at code-final `12b6a89`:** typecheck · lint 0 · lint:copy PASS (5 passes) · format:check exit 0 · vitest 309/309 · build 30 routes / 10 static / middleware 27.1 kB · Playwright 45 passed 0 failed · Lighthouse via direct `lighthouse` runs (`lhci collect` dies on a Windows temp-dir EPERM and then asserts 0 URLs) 1.0 in all four categories on 7 pages; **`/decode` a11y 0.96** from two pre-existing audits (`skip-link`: the bare decode page has no `<main id="main">`; `heading-order`: `EmptyState` renders `<h3>` under the `<h1>`) — both for the visual pass. AA-30 ticked in `02-BUILD-PLAN-AMENDMENTS.md`; evidence + Discovered/Deviations in `docs/handoffs/2026-09-09-uiux-polish-fix.md` (F0's `18da41e` was a dangling pre-amend object → `cc1fe95`; MSYS converts a lone `/` env value into the Git install path; the rendered Terms page says "Last updated 2026-08-25"; no approved gloss for "Seller Central"; two Accept-count mismatches recorded rather than gamed). 12 public screenshots in `docs/handoffs/screenshots/2026-09-10/`; auth-gated captures, the F2 idle-lock check and the fresh-browser loss-warning check **NOT RUN** (no dev credentials; the AI does not type passwords). `SESSION-START-PROMPT.md` repointed at the visual refresh v3 prompt, as that prompt's banner prescribes for F8.
- **BLOCKERS (founder):** Phase-0 credential rotation (Supabase `fogvzjtxbqgfppdrxqra`); Paddle/Polar/Wise/CWS/domain accounts; Agent-Policy/BSA text retrieval (B-08). NEW: ratify AM-16 + AM-17 together; sign off the 12 AA-30 screenshots in `docs/handoffs/screenshots/2026-09-10/` and run (or lend credentials for) the auth-gated captures + idle-lock check listed under "Founder sign-off" in `docs/handoffs/2026-09-09-uiux-polish-fix.md` (box A was left unticked, so F7 restored the Terms §2 sentence); decide the rendered Terms date (`src/content/legal.ts` says 2026-08-25) and whether "Seller Central" gets a gloss; decide on a `.gitattributes` `eol=lf` line (CRLF Prettier flake recurs on every checkout while `core.autocrlf=true`) and on `disconnected-chat!.txt`; push `master` so CI runs (origin is at `048d311`); add the two banned rows ("agency results at 1/10th the cost"; readiness-as-approval framing) to `Planning/07-REFERENCE/01-MARKET-EVIDENCE.md` §4; merge the 2 Sep competitor deltas into `02-COMPETITOR-DOSSIER.md` (07-REFERENCE is founder-gated).
- **NEXT 4 ACTIONS:** (1) Founder closes Phase-0 blockers + retrieves the in-force Agent Policy text (B-08) + ratifies AM-16/AM-17. (2) AI implements M4+M5 per AM-16+AM-17 (AA-19 evidenceModel/letters/router/readiness/two-mode composer + AA-22 caseState/objections/responseAnalyzer + AA-24 triage/novelty) with evidence-state + reply fixtures. (3) AI builds the Guided Interview + EF-5 outcome schema with the M-W backend work (AA-23, AA-21). (4) AI ships UI Wave A then Wave B per `06-PREMIUM-UI-UX-SPEC.md` §13 with the §15 gates; founder supplies the true founder-note text, ratifies AM-18, and signs off light+dark screenshots before first deploy; the AA-30 fix pass is complete (10 Sep 2026); the coding AI starts the visual refresh v3 from `docs/handoffs/SESSION-START-PROMPT.md` (`docs/handoffs/2026-09-09-visual-refresh-prompt.md`, Task V0 first — it commits the still-untracked visual docs, copy deck, brand kit and assets); the two `/decode` Lighthouse audits (shell → skip-link target; `EmptyState` heading level) ride that pass. Wave C-fix Tasks 8–10 remain open.

## 5. Key file links

> Repo layout: the app lives at repo root (`src/`, `package.json`, `legal/`, `docs/`); all playbook/planning docs live in `Planning/`; `.agents/` + `.claude/` skills stay at root.

- Build plan (canonical): `Planning/03-PHASE-2-BUILD/reference/APPEALDECK_BUILD_PLAN_v1.0.md`
- Amendments (authoritative over v1.0): `Planning/03-PHASE-2-BUILD/02-BUILD-PLAN-AMENDMENTS.md`
- Build sequence (order): `Planning/03-PHASE-2-BUILD/01-BUILD-SEQUENCE.md`
- Risk controls: `Planning/03-PHASE-2-BUILD/03-TECHNICAL-RISK-CONTROLS.md`
- Premium UI/UX spec (AM-18): `Planning/03-PHASE-2-BUILD/06-PREMIUM-UI-UX-SPEC.md`
- Decisions log + gates: `Planning/00-DECISION/`
- Team / continuity: `Planning/08-TEAM/`
- Reasoning ledger: `docs/DECISIONS.md`
- Coding-AI session-start prompt (founder pastes it into every new coding session; two PATH lines point at the current pass): `docs/handoffs/SESSION-START-PROMPT.md`
- Milestone/session handoffs: `docs/handoffs/` (4 Sep 2026 handoff prompt for the coding agent: `docs/handoffs/2026-09-04-premium-ui-ux-and-single-host.md`; 2 Sep 2026 session log: `docs/handoffs/2026-09-02-external-ai-review-and-case-os.md`; archived 2 Sep research: `Planning/03-PHASE-2-BUILD/reference/2026-09-02-*.md`)
